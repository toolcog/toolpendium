{
  "name": "CartCouponCount",
  "description": "Get cart coupons count.",
  "parameters": {
    "type": "object",
    "properties": {
      "store_id": {
        "type": "string",
        "description": "Store Id"
      },
      "date_start_from": {
        "type": "string",
        "description": "Filter entity by date_start (greater or equal)"
      },
      "date_start_to": {
        "type": "string",
        "description": "Filter entity by date_start (less or equal)"
      },
      "date_end_from": {
        "type": "string",
        "description": "Filter entity by date_end (greater or equal)"
      },
      "date_end_to": {
        "type": "string",
        "description": "Filter entity by date_end (less or equal)"
      },
      "avail": {
        "default": true,
        "type": "boolean",
        "description": "Defines category's visibility status"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/cart.coupon.count.json{?store_id,date_start_from,date_start_to,date_end_from,date_end_to,avail}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**coupons_count**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
