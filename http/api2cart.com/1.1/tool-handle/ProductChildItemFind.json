{
  "name": "ProductChildItemFind",
  "description": "Search product child item (bundled item or configurable product variant) in store catalog.",
  "parameters": {
    "type": "object",
    "properties": {
      "find_value": {
        "type": "string",
        "description": "Entity search that is specified by some value"
      },
      "find_where": {
        "default": "name",
        "type": "string",
        "description": "Entity search that is specified by the comma-separated unique fields"
      },
      "find_params": {
        "default": "whole_words",
        "type": "string",
        "description": "Entity search that is specified by comma-separated parameters"
      },
      "store_id": {
        "type": "string",
        "description": "Store Id"
      }
    },
    "required": [
      "find_value"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/product.child_item.find.json{?find_value,find_where,find_params,store_id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**children**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
