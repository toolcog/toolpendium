{
  "name": "OrderShipmentDelete",
  "description": "Delete order's shipment.",
  "parameters": {
    "type": "object",
    "properties": {
      "shipment_id": {
        "type": "string",
        "description": "Shipment id indicates the number of delivery"
      },
      "order_id": {
        "type": "string",
        "description": "Defines the order for which the shipment will be deleted"
      },
      "store_id": {
        "type": "string",
        "description": "Store Id"
      }
    },
    "required": [
      "shipment_id",
      "order_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/order.shipment.delete.json{?shipment_id,order_id,store_id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**status**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
