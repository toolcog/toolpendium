{
  "name": "ProductAttributeValueSet",
  "description": "Set attribute value to product.",
  "parameters": {
    "type": "object",
    "properties": {
      "product_id": {
        "type": "string",
        "description": "Defines product id where the attribute should be added"
      },
      "attribute_id": {
        "type": "string",
        "description": "Filter by attribute_id"
      },
      "attribute_group_id": {
        "type": "string",
        "description": "Filter by attribute_group_id"
      },
      "attribute_name": {
        "type": "string",
        "description": "Define attribute name"
      },
      "value": {
        "type": "string",
        "description": "Define attribute value"
      },
      "value_id": {
        "type": "integer",
        "description": "Define attribute value id"
      },
      "lang_id": {
        "type": "string",
        "description": "Language id"
      },
      "store_id": {
        "type": "string",
        "description": "Store Id"
      }
    },
    "required": [
      "product_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/product.attribute.value.set.json{?product_id,attribute_id,attribute_group_id,attribute_name,value,value_id,lang_id,store_id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**attribute_id**",
                  "**product_id**",
                  "**value_id**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
