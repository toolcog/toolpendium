{
  "name": "BasketLiveShippingServiceCreate",
  "description": "Create live shipping rate service.",
  "parameters": {
    "type": "object",
    "properties": {
      "store_id": {
        "type": "string",
        "description": "Store Id"
      },
      "name": {
        "type": "string",
        "description": "Shipping Service Name"
      },
      "callback": {
        "type": "string",
        "description": "Callback url that returns shipping rates. It should be able to accept POST requests with json data."
      }
    },
    "required": [
      "name",
      "callback"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/basket.live_shipping_service.create.json{?store_id,name,callback}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**id**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
