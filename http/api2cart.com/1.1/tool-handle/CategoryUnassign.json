{
  "name": "CategoryUnassign",
  "description": "Unassign category to product",
  "parameters": {
    "type": "object",
    "properties": {
      "category_id": {
        "type": "string",
        "description": "Defines category unassign, specified by category id"
      },
      "product_id": {
        "type": "string",
        "description": "Defines category unassign to the product, specified by product id"
      },
      "store_id": {
        "type": "string",
        "description": "Store Id"
      }
    },
    "required": [
      "category_id",
      "product_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/category.unassign.json{?category_id,product_id,store_id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**result**",
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
