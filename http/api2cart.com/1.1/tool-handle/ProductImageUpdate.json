{
  "name": "ProductImageUpdate",
  "description": "Update details of image",
  "parameters": {
    "type": "object",
    "properties": {
      "product_id": {
        "type": "string",
        "description": "Defines product id where the image should be updated"
      },
      "variant_ids": {
        "type": "string",
        "description": "Defines product's variants ids"
      },
      "image_name": {
        "type": "string",
        "description": "Defines image's name"
      },
      "type": {
        "default": "additional",
        "type": "string",
        "description": "Defines image's types that are specified by comma-separated list"
      },
      "label": {
        "type": "string",
        "description": "Defines alternative text that has to be attached to the picture"
      },
      "position": {
        "type": "integer",
        "description": "Defines image’s position in the list"
      },
      "id": {
        "type": "string",
        "description": "Defines image update specified by image id"
      },
      "store_id": {
        "type": "string",
        "description": "Store Id"
      },
      "lang_id": {
        "type": "string",
        "description": "Language id"
      },
      "hidden": {
        "type": "boolean",
        "description": "Define is hide image"
      }
    },
    "required": [
      "product_id",
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/product.image.update.json{?product_id,variant_ids,image_name,type,label,position,id,store_id,lang_id,hidden}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**updated**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
