{
  "name": "OrderShipmentInfo",
  "description": "Get information of shipment.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Entity id"
      },
      "order_id": {
        "type": "string",
        "description": "Defines the order id"
      },
      "start": {
        "default": 0,
        "type": "integer",
        "description": "This parameter sets the number from which you want to get entities"
      },
      "params": {
        "default": "id,order_id,items,tracking_numbers",
        "type": "string",
        "description": "Set this parameter in order to choose which entity fields you want to retrieve"
      },
      "response_fields": {
        "type": "string",
        "description": "Set this parameter in order to choose which entity fields you want to retrieve"
      },
      "exclude": {
        "type": "string",
        "description": "Set this parameter in order to choose which entity fields you want to ignore. Works only if parameter `params` equal force_all"
      },
      "store_id": {
        "type": "string",
        "description": "Store Id"
      }
    },
    "required": [
      "id",
      "order_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/order.shipment.info.json{?id,order_id,start,params,response_fields,exclude,store_id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**additional_fields**",
            "**custom_fields**",
            [
              "**pagination**",
              {
                "$ul": [
                  "**additional_fields**",
                  "**custom_fields**",
                  "**next**",
                  "**previous**"
                ]
              }
            ],
            [
              "**result**",
              {
                "$ul": [
                  "**additional_fields**",
                  "**custom_fields**",
                  "**shipment**",
                  "**shipment_count**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
