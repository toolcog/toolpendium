{
  "name": "CartMetaDataUnset",
  "description": "Unset meta data for a specific entity",
  "parameters": {
    "type": "object",
    "properties": {
      "entity_id": {
        "type": "string",
        "description": "Entity Id"
      },
      "entity": {
        "default": "product",
        "type": "string",
        "description": "Entity"
      },
      "store_id": {
        "type": "string",
        "description": "Store Id"
      },
      "key": {
        "type": "string",
        "description": "Key"
      },
      "id": {
        "type": "string",
        "description": "Entity id"
      }
    },
    "required": [
      "entity_id",
      "key",
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/cart.meta_data.unset.json{?entity_id,entity,store_id,key,id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**status**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
