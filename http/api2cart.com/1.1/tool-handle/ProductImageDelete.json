{
  "name": "ProductImageDelete",
  "description": "Delete image",
  "parameters": {
    "type": "object",
    "properties": {
      "product_id": {
        "type": "string",
        "description": "Defines product id where the image should be deleted"
      },
      "id": {
        "type": "string",
        "description": "Entity id"
      },
      "store_id": {
        "type": "string",
        "description": "Store Id"
      }
    },
    "required": [
      "product_id",
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/product.image.delete.json{?product_id,id,store_id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**deleted**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
