{
  "name": "ProductOptionValueAdd",
  "description": "Add product option item from option.",
  "parameters": {
    "type": "object",
    "properties": {
      "product_id": {
        "type": "string",
        "description": "Defines product id where the option value should be added"
      },
      "option_id": {
        "type": "string",
        "description": "Defines option id where the value has to be added"
      },
      "option_value": {
        "type": "string",
        "description": "Defines option value that has to be added"
      },
      "sort_order": {
        "default": 0,
        "type": "integer",
        "description": "Sort number in the list"
      },
      "clear_cache": {
        "default": true,
        "type": "boolean",
        "description": "Is cache clear required"
      }
    },
    "required": [
      "product_id",
      "option_id",
      "option_value"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/product.option.value.add.json{?product_id,option_id,option_value,sort_order,clear_cache}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**option_value_id**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
