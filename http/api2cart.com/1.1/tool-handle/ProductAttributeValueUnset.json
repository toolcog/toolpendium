{
  "name": "ProductAttributeValueUnset",
  "description": "Removes attribute value for a product.",
  "parameters": {
    "type": "object",
    "properties": {
      "product_id": {
        "type": "string",
        "description": "Product id"
      },
      "attribute_id": {
        "type": "string",
        "description": "Attribute Id"
      },
      "store_id": {
        "type": "string",
        "description": "Store Id"
      },
      "include_default": {
        "default": false,
        "type": "boolean",
        "description": "Boolean, whether or not to unset default value of the attribute, if applicable"
      },
      "reindex": {
        "default": true,
        "type": "boolean",
        "description": "Is reindex required"
      },
      "clear_cache": {
        "default": true,
        "type": "boolean",
        "description": "Is cache clear required"
      }
    },
    "required": [
      "product_id",
      "attribute_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/product.attribute.value.unset.json{?product_id,attribute_id,store_id,include_default,reindex,clear_cache}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**success**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
