{
  "name": "ProductOptionValueUpdate",
  "description": "Update product option item from option.",
  "parameters": {
    "type": "object",
    "properties": {
      "product_id": {
        "type": "string",
        "description": "Defines product id where the option value should be updated"
      },
      "option_id": {
        "type": "string",
        "description": "Defines option id where the value has to be updated"
      },
      "option_value_id": {
        "type": "integer",
        "description": "Defines value id that has to be assigned"
      },
      "option_value": {
        "type": "string",
        "description": "Defines option value that has to be added"
      },
      "price": {
        "type": "number",
        "description": "Defines new product option price"
      },
      "quantity": {
        "type": "number",
        "description": "Defines new products' options quantity"
      },
      "clear_cache": {
        "default": true,
        "type": "boolean",
        "description": "Is cache clear required"
      }
    },
    "required": [
      "product_id",
      "option_id",
      "option_value_id",
      "option_value"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/product.option.value.update.json{?product_id,option_id,option_value_id,option_value,price,quantity,clear_cache}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**updated_items**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
