{
  "name": "ProductVariantImageAdd",
  "description": "Add image to product",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/ProductVariantImageAdd"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "ProductVariantImageAdd": {
        "properties": {
          "content": {
            "description": "Content(body) encoded in base64 of image file",
            "type": "string"
          },
          "image_name": {
            "description": "Defines image's name",
            "type": "string"
          },
          "label": {
            "description": "Defines alternative text that has to be attached to the picture",
            "type": "string"
          },
          "mime": {
            "description": "Mime type of image http://en.wikipedia.org/wiki/Internet_media_type.",
            "type": "string"
          },
          "option_id": {
            "description": "Defines option id of the product variant for which the image will be added",
            "type": "string"
          },
          "position": {
            "default": 0,
            "description": "Defines image’s position in the list",
            "type": "integer"
          },
          "product_id": {
            "description": "Defines product id where the variant image has to be added",
            "type": "string"
          },
          "product_variant_id": {
            "description": "Defines product's variants specified by variant id",
            "type": "integer"
          },
          "store_id": {
            "description": "Store Id",
            "type": "string"
          },
          "type": {
            "default": "base",
            "description": "Defines image's types that are specified by comma-separated list",
            "enum": [
              "small",
              "base",
              "additional",
              "thumbnail"
            ],
            "type": "string"
          },
          "url": {
            "description": "Defines URL of the image that has to be added",
            "type": "string"
          }
        },
        "required": [
          "product_variant_id",
          "image_name",
          "type"
        ],
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/product.variant.image.add.json"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**id**",
                  "**image_path**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
