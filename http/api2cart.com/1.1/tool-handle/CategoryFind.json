{
  "name": "CategoryFind",
  "description": "Search category in store. \"Laptop\" is specified here by default.",
  "parameters": {
    "type": "object",
    "properties": {
      "find_value": {
        "type": "string",
        "description": "Entity search that is specified by some value"
      },
      "find_where": {
        "default": "name",
        "type": "string",
        "description": "Entity search that is specified by the comma-separated unique fields"
      },
      "find_params": {
        "default": "whole_words",
        "type": "string",
        "description": "Entity search that is specified by comma-separated parameters"
      },
      "store_id": {
        "type": "string",
        "description": "Store Id"
      },
      "lang_id": {
        "type": "string",
        "description": "Language id"
      }
    },
    "required": [
      "find_value"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/category.find.json{?find_value,find_where,find_params,store_id,lang_id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**category**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
