{
  "name": "CategoryInfo",
  "description": "Get category info about category ID*** or specify other category ID.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Retrieves category's info specified by category id"
      },
      "params": {
        "default": "id,parent_id,name,description",
        "type": "string",
        "description": "Set this parameter in order to choose which entity fields you want to retrieve"
      },
      "response_fields": {
        "type": "string",
        "description": "Set this parameter in order to choose which entity fields you want to retrieve"
      },
      "exclude": {
        "type": "string",
        "description": "Set this parameter in order to choose which entity fields you want to ignore. Works only if parameter `params` equal force_all"
      },
      "store_id": {
        "type": "string",
        "description": "Retrieves category info  specified by store id"
      },
      "lang_id": {
        "type": "string",
        "description": "Retrieves category info  specified by language id"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/category.info.json{?id,params,response_fields,exclude,store_id,lang_id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**additional_fields**",
                  "**avail**",
                  [
                    "**created_at**",
                    {
                      "$ul": [
                        "**additional_fields**",
                        "**custom_fields**",
                        "**format**",
                        "**value**"
                      ]
                    }
                  ],
                  "**custom_fields**",
                  "**description**",
                  "**id**",
                  "**images**",
                  "**keywords**",
                  "**meta_description**",
                  "**meta_title**",
                  [
                    "**modified_at**",
                    {
                      "$ul": [
                        "**additional_fields**",
                        "**custom_fields**",
                        "**format**",
                        "**value**"
                      ]
                    }
                  ],
                  "**name**",
                  "**parent_id**",
                  "**path**",
                  "**seo_url**",
                  "**short_description**",
                  "**sort_order**",
                  "**stores_ids**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
