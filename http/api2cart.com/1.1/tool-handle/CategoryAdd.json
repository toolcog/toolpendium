{
  "name": "CategoryAdd",
  "description": "Add new category in store",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Defines category's name that has to be added"
      },
      "parent_id": {
        "type": "string",
        "description": "Adds categories specified by parent id"
      },
      "stores_ids": {
        "default": 0,
        "type": "string",
        "description": "Create category in the stores that is specified by comma-separated stores' id"
      },
      "store_id": {
        "type": "string",
        "description": "Store Id"
      },
      "lang_id": {
        "type": "string",
        "description": "Language id"
      },
      "avail": {
        "default": true,
        "type": "boolean",
        "description": "Defines category's visibility status"
      },
      "sort_order": {
        "default": 0,
        "type": "integer",
        "description": "Sort number in the list"
      },
      "created_time": {
        "type": "string",
        "description": "Entity's date creation"
      },
      "modified_time": {
        "type": "string",
        "description": "Entity's date modification"
      },
      "description": {
        "type": "string",
        "description": "Defines category's description"
      },
      "meta_title": {
        "type": "string",
        "description": "Defines unique meta title for each entity"
      },
      "meta_description": {
        "type": "string",
        "description": "Defines unique meta description of a entity"
      },
      "meta_keywords": {
        "type": "string",
        "description": "Defines unique meta keywords for each entity"
      },
      "seo_url": {
        "type": "string",
        "description": "Defines unique category's URL for SEO"
      }
    },
    "required": [
      "name"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/category.add.json{?name,parent_id,stores_ids,store_id,lang_id,avail,sort_order,created_time,modified_time,description,meta_title,meta_description,meta_keywords,seo_url}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**category_id**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
