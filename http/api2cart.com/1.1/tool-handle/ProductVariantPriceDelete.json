{
  "name": "ProductVariantPriceDelete",
  "description": "Delete some prices of the product variant.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Defines the variant where the price has to be deleted"
      },
      "product_id": {
        "type": "string",
        "description": "Product id"
      },
      "group_prices": {
        "type": "string",
        "description": "Defines variants's group prices"
      }
    },
    "required": [
      "id",
      "product_id",
      "group_prices"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/product.variant.price.delete.json{?id,product_id,group_prices}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**deleted**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
