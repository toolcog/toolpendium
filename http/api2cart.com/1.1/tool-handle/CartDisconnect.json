{
  "name": "CartDisconnect",
  "description": "Disconnect with the store and clear store session data.",
  "parameters": {
    "type": "object",
    "properties": {
      "delete_bridge": {
        "default": false,
        "type": "boolean",
        "description": "Identifies if there is a necessity to delete bridge"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.api2cart.com/v1.1/cart.disconnect.json{?delete_bridge}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**result**",
              {
                "$ul": [
                  "**connection**"
                ]
              }
            ],
            "**return_code**",
            "**return_message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
