{
  "name": "deleteUserDefinedTagBulk",
  "description": "Delete a list of user-defined resource tags in one delete operation.\n",
  "parameters": {
    "type": "object",
    "properties": {
      "ids": {
        "items": {
          "type": "string"
        },
        "type": "array",
        "description": "An array of IDs of the user-defined resource tags to be deleted. Any non-existent ID in the array will be ignored.\n"
      }
    },
    "required": [
      "ids"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/user_defined_tag{?ids}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Lists the IDs of resource tags that were deleted successfully.\n",
        "**Key properties:**",
        {
          "$ul": [
            "**ids**: List of deleted IDs."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
