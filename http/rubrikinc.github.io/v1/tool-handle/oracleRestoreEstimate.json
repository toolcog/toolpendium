{
  "name": "oracleRestoreEstimate",
  "description": "The estimated size of the data to download from an archival location in order to perform a specified restore or export operation.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of the Oracle database."
      },
      "snapshot_id": {
        "type": "string",
        "description": "ID of the snapshot to recover to."
      },
      "recovery_time": {
        "format": "date-time",
        "type": "string",
        "description": "The date and time for the recovery restore point, specified in the ISO 8601 format, as in the example \"2016-01-01T01:23:45.678\"."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v1/oracle/db/{id}/restore_estimate{?snapshot_id,recovery_time}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**bytesToRestore**: The estimated size, in bytes required to restore an Oracle database."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
