{
  "name": "deleteSyslogExportRule",
  "description": "Delete the syslog export rule specified by the given id.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The ID of the syslog export rule."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/syslog/export/{id}"
    }
  },
  "responses": {}
}
