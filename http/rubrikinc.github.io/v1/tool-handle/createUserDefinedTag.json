{
  "name": "createUserDefinedTag",
  "description": "Create a user-defined resource tag for tagging cloud compute resources created by CloudOn and CloutOut.\n",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/ResourceTagDefinition",
        "description": "The definition of a new user-defined resource tag to be created.\n"
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "/api/v1/user_defined_tag"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Provides the details for a user-defined resource tag, which tags cloud compute resources created by CloudOn and CloudOut.\n",
        "**Key properties:**",
        {
          "$ul": [
            "**id**: The ID of the resource tag.",
            "**key**: The key of the resource tag.",
            "**lastUpdateTime**: A timestamp (in UTC) that indicates when the resource tag was last updated.",
            "**scopeRefId**: The ID of the archival location associated with the resource that has the specified user-defined resource tag.",
            "**value**: The value of the resource tag."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
