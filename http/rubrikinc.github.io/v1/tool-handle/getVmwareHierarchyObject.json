{
  "name": "getVmwareHierarchyObject",
  "description": "Get VMware Clusters, Hosts, and Resource Pool hierarchy object detail information by object ID.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Get VMware hierarchy objects of given ID."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v1/vmware/hierarchy/{id}/export"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**datastores**",
            "**hasChildren**",
            "**id**",
            "**location**",
            "**name**",
            "**objectType**: Vmware Hierarchy Object Type.",
            "**recoverableTo**",
            "**rootId**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
