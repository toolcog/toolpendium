{
  "name": "patchPasswordRequirements",
  "description": "Update user password requirements for a cluster.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "default": "me",
        "type": "string",
        "description": "ID of the Rubrik cluster or *me* for self."
      },
      "body": {
        "$ref": "#/components/schemas/PasswordRequirementsPatchRequest",
        "description": "Password requirements."
      }
    },
    "required": [
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PATCH",
    "url": {
      "$uri": "/api/v1/cluster/{id}/security/password_requirements"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**blockPreviousPasswords**",
            "**clearApiTokens**",
            "**clearWebSessions**",
            "**maxLength**",
            "**minLength**",
            "**minLowerCase**",
            "**minNumerics**",
            "**minSpecial**",
            "**minUpperCase**",
            "**useZxcvbn**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
