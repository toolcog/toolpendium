{
  "name": "updateMount",
  "description": "Power a specified Live Mount virtual machine on or off. Pass **_true_** to power the virtual machine on and pass **_false_** to power the virtual machine off.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of a Live Mount."
      },
      "body": {
        "$ref": "#/components/schemas/UpdateMountConfig",
        "description": "Power state configuration."
      }
    },
    "required": [
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PATCH",
    "url": {
      "$uri": "/api/v1/vmware/vm/snapshot/mount/{id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**attachingDiskCount**: An integer value that identifies how many disks are attached.",
            "**createDatastoreOnlyMount**: This boolean value determines whether or not the mount is created as a datastore only. When 'true,' the mount is created with datastore and not the associated virtual machine. When 'false,' the mount is created with both the datastore and the associated virtual machine.",
            "**datastoreName**: The name of the datastore that contains the mounted VMDK.",
            "**datastoreReady**: A boolean value that specifies whether the datastore is ready. When 'true,' the datastore is ready. When 'false,' the datastore is not ready.",
            "**hasAttachingDisk**: A Boolean value that determines whether this job is an attaching disk mount job. When 'true,' this is an attaching disk mount job. When 'false,' this is not an attaching disk mount job.",
            "**hostId**",
            "**id**",
            "**isReady**",
            "**mountRequestId**",
            "**mountTimestamp**: Gives the timestamp at which the mount was created.",
            "**mountedVmId**",
            "**snapshotDate**",
            "**unmountRequestId**",
            "**vmId**",
            "**powerStatus**: The power status of the mounted VM(ON,OFF,SLEEP etc.)."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
