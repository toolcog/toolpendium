{
  "name": "revokeRolesFromPrincipals",
  "description": "Revoke a set of roles from the specified principals.\n",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/RoleAssignmentRequest",
        "description": "A set of roles to revoke from a set of principals."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "/api/v1/principal/role/bulk_revoke"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
