{
  "name": "querySnapshotsForVms",
  "description": "Retrieve snapshot summaries for a list of virtual machines.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "items": {
          "description": "ID of the virtual machine.",
          "type": "string"
        },
        "type": "array",
        "description": "IDs of the virtual machines."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "/api/v1/vmware/vm/snapshots"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**responses**: The snapshot summaries for a list of virtual machines."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
