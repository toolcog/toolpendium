{
  "name": "deleteVappSnapshotsV1",
  "description": "Delete all snapshots for a specified vApp object.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID assigned to a vApp object."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/vcd/vapp/{id}/snapshot"
    }
  },
  "responses": {}
}
