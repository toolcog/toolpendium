{
  "name": "getWindowsCluster",
  "description": "Returns a detailed view of a Windows server failover cluster.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of the Windows cluster."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v1/windows_cluster/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**fqdn**: Fully qualified domain name of this cluster.",
            "**hostIds**: IDs of the hosts in this cluster.",
            "**id**",
            "**isAgentless**: Indicates whether the Rubrik Backup Service is installed on hosts in a Windows cluster. The value is true when all hosts in the cluster do not have the Rubrik Backup Service installed. The value is false when one or more of the hosts in the cluster have the Rubrik Backup Service installed.",
            "**name**",
            "**primaryClusterId**",
            "**naturalId**: Obtained from ClusterInstanceID under the registry key HKLM\\Cluster."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
