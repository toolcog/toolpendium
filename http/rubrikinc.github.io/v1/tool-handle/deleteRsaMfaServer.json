{
  "name": "deleteRsaMfaServer",
  "description": "Delete RSA server configuration.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of the RSA server."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/mfa/rsa/server/{id}"
    }
  },
  "responses": {}
}
