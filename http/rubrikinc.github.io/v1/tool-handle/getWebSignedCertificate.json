{
  "name": "getWebSignedCertificate",
  "description": "If the web server uses a signed certificate, fetch it.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "default": "me",
        "type": "string",
        "description": "ID of the Rubrik cluster or *me* for self."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v1/cluster/{id}/security/web_signed_cert"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**cert**",
              {
                "$ul": [
                  "**certId**: ID of the certificate.",
                  "**description**: User-friendly description for the certificate.",
                  "**expiration**: The expiration date for the certificate.",
                  "**hasKey**: A Boolean value that specifies whether or not the certificate is associated with a stored private key. When this value is 'true,' the private key for the certificate is stored. When this value is 'false,' the private key for the certificate is not stored.",
                  "**name**: Display name for the certificate.",
                  "**pemFile**: The certificates, in PEM format.",
                  "**usedBy**: A list of components using the certificate."
                ]
              }
            ],
            "**webServerConfiguredWithCASignedCertificate**: A Boolean value that indicates if the web server is configured to use a certificate signed by an external CA."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
