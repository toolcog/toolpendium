{
  "name": "createVappExportV1",
  "description": "Export the specified vApp snapshot into a new vApp or an existing vApp.",
  "parameters": {
    "type": "object",
    "properties": {
      "snapshot_id": {
        "type": "string",
        "description": "ID assigned to the vApp snapshot object."
      },
      "body": {
        "$ref": "#/components/schemas/VappExportSnapshotJobConfig",
        "description": "Configuration for the request to export the specified vApp snapshot."
      }
    },
    "required": [
      "snapshot_id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "/api/v1/vcd/vapp/snapshot/{snapshot_id}/export"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "202": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**endTime**: The end time of the request.",
            [
              "**error**",
              {
                "$ul": [
                  "**message**: The error message for failed IDs."
                ]
              }
            ],
            "**id**: The ID of the request object used to poll the status.",
            "**links**: References to any related objects.",
            "**nodeId**: The ID of the node where the job ran.",
            "**progress**: The current percentage progress of the asynchronous request.",
            "**startTime**: The start time of the request.",
            "**status**: Status of the ID."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
