{
  "name": "setPreferredCdpNetworkProtocol",
  "description": "Set the current preference of CDM between IPv4 and IPv6 for CDP data transfer.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/PreferredCdpNetworkProtocol",
        "description": "The preferred network protocol to use for transferring CDP data."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PATCH",
    "url": {
      "$uri": "/api/v1/vmware/config/cdp/set_preferred_cdp_network_protocol"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**preferredCdpNetworkProtocol**: The preferred network protocol to use for transferring CDP data."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
