{
  "name": "getPreferredCdpNetworkProtocol",
  "description": "Returns the current preference of CDM between IPv4 and IPv6 for CDP data transfer.",
  "parameters": {
    "type": "object"
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v1/vmware/config/cdp/get_preferred_cdp_network_protocol"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**preferredCdpNetworkProtocol**: The preferred network protocol to use for transferring CDP data."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
