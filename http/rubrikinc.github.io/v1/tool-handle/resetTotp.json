{
  "name": "resetTotp",
  "description": "Reset the TOTP settings to the default disabled state for the specified user account.\n",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The user account object ID."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/user/{id}/totp/config"
    }
  },
  "responses": {}
}
