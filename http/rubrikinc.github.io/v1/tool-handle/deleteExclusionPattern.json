{
  "name": "deleteExclusionPattern",
  "description": "Deletes an exclusion pattern if that pattern is mutable.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of the exclusion pattern."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/exclusion_pattern/{id}"
    }
  },
  "responses": {}
}
