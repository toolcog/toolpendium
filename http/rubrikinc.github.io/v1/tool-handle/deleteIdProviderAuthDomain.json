{
  "name": "deleteIdProviderAuthDomain",
  "description": "Delete an IdP authentication domain for the given ID.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of the IdP authentication domain to be deleted."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/idp_auth_domain/{id}"
    }
  },
  "responses": {}
}
