{
  "name": "queryUserDefinedTag",
  "description": "Get user-defined resource tags for the cloud compute resources created by CloudOn and CloudOut.\n",
  "parameters": {
    "type": "object",
    "properties": {
      "key": {
        "type": "string",
        "description": "Filter results by resource tag."
      },
      "scope_ref_id": {
        "type": "string",
        "description": "Filter results by archival location id."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v1/user_defined_tag{?key,scope_ref_id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Returns the results of a query on user-defined resource tags.\n",
        "**Key properties:**",
        {
          "$ul": [
            "**resourceTags**: List of resource tags."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
