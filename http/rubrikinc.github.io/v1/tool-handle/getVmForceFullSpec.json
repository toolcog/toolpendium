{
  "name": "getVmForceFullSpec",
  "description": "Retrieve the configuration that has been set for forcing a full snapshot for a VMware virtual machine.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of the VMware virtual machine."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v1/vmware/vm/{id}/request/force_full_snapshot"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**virtualDiskInfos**: List of objects that provide information about each virtual disk that has requested a forced full snapshot.",
            "**vmId**: VMware virtual machine ID."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
