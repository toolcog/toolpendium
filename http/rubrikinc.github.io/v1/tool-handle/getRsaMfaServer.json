{
  "name": "getRsaMfaServer",
  "description": "Get RSA server configuration.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of the RSA server."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v1/mfa/rsa/server/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**name**: Specifies the name to identify MFA server configuration.",
            "**timeout**: Specifies a number of seconds to wait for server response to a given authentication method.",
            "**assurancePolicyName**: The name of a Cloud Authentication Service policy. This setting is only required for RSA Cloud Service configurations.",
            "**baseUrl**: The base url for RSA REST API server including the host name and port number. A valid input looks like https://<SECURID_ACCESS_HOST >:<REST_API_PORT>/mfa/v1_1.",
            "**certificateId**: ID corresponding to the imported certificate.",
            "**clientId**: A unique name to identify the client. When the client is configured to use RSA Authentication Manager, the client ID must match an authentication agent name.",
            "**ldapUsernameAttribute**: LDAP attribute to query the username used for performing MFA.",
            "**id**: Unique server identifier."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
