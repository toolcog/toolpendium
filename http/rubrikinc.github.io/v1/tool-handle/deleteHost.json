{
  "name": "deleteHost",
  "description": "Delete host by specifying the host ID.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of the host to delete."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/host/{id}"
    }
  },
  "responses": {}
}
