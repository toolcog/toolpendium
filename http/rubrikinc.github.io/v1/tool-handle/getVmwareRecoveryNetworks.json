{
  "name": "getVmwareRecoveryNetworks",
  "description": "Get all the networks for snapshot recovery for the specified compute resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "compute_resource_id": {
        "type": "string",
        "description": "Get VMware recovery networks for the compute resource ID."
      },
      "compute_resource_type": {
        "enum": [
          "ComputeCluster",
          "Host",
          "ResourcePool"
        ],
        "type": "string",
        "description": "The type of the compute resource."
      }
    },
    "required": [
      "compute_resource_id",
      "compute_resource_type"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v1/vmware/config/recovery/networks{?compute_resource_id,compute_resource_type}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "A collection of VMware networks.",
        "**Key properties:**",
        {
          "$ul": [
            "**networks**: A list of VMware network information."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
