{
  "name": "logOut",
  "description": "Log Out",
  "parameters": {
    "type": "object",
    "properties": {
      "token": {
        "type": "string",
        "description": "token"
      }
    },
    "required": [
      "token"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://www.patientview.org//auth/logout/{token}"
    }
  },
  "responses": {}
}
