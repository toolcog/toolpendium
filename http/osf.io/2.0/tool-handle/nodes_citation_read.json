{
  "name": "nodes_citation_read",
  "description": "The citation for a node in a specific style.\n#### Returns\nReturns a JSON object with a `data` key that contains the representation of the node citation, in the requested style.",
  "parameters": {
    "type": "object",
    "properties": {
      "style_id": {
        "type": "string",
        "description": "The unique identifier of the citation style."
      },
      "node_id": {
        "type": "string",
        "description": "The unique identifier of the node."
      }
    },
    "required": [
      "style_id",
      "node_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.test.osf.io/v2/nodes/{node_id}/citation/{style_id}/"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Styled Citation"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**attributes**: The properties of the citation style entity.",
              {
                "$ul": [
                  "**citation**: The complete entity citation in the requested style."
                ]
              }
            ],
            "**id**: The identifier of the citation style, e.g. APA.",
            "**type**: The type identifier of the citation style entity (`styled-citations`)."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
