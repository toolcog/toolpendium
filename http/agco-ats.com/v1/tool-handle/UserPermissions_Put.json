{
  "name": "UserPermissions_Put",
  "description": "No Documentation Found.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int32",
        "type": "integer",
        "description": "The User's ID"
      },
      "body": {
        "items": {
          "$ref": "#/$defs/API.Models.UserRoleChange"
        },
        "type": "array",
        "description": "A list of changes to the User's Roles"
      }
    },
    "required": [
      "id",
      "body"
    ],
    "$defs": {
      "API.Models.UserRoleChange": {
        "properties": {
          "Action": {
            "description": "The action to take with the role",
            "enum": [
              "Grant",
              "Revoke"
            ],
            "type": "string"
          },
          "Name": {
            "description": "The name of the role",
            "type": "string"
          }
        },
        "required": [
          "Name",
          "Action"
        ],
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://secure.agco-ats.com/api/v2/Users/{id}/Roles"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**DeveloperMessage**",
            "**ErrorCode**",
            "**MoreInfo**",
            "**UserMessage**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
