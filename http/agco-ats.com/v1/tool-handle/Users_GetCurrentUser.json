{
  "name": "Users_GetCurrentUser",
  "description": "No Documentation Found.",
  "parameters": {
    "type": "object"
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://secure.agco-ats.com/api/v2/Users/Current"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**ChangePassword**: Never Returned.  When changing a user's password, this field must contain the new password.",
            "**Email**: The user's email address",
            "**Name**: The user's name",
            "**Password**: Never Returned.  Required when creating a new user or updating a user.  When changing a user's password this field must contain the current password.",
            "**UserID**: The user ID",
            "**Username**: The username used for authentication"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
