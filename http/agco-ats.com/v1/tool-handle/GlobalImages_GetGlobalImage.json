{
  "name": "GlobalImages_GetGlobalImage",
  "description": "No Documentation Found.",
  "parameters": {
    "type": "object",
    "properties": {
      "ID": {
        "type": "string",
        "description": "The GlobalImage's id."
      }
    },
    "required": [
      "ID"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://secure.agco-ats.com/api/v2/GlobalImages/{ID}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "An image from the Global Image library.",
        "**Key properties:**",
        {
          "$ul": [
            "**CRC**: The Hash of the file (SHA256, HEX-encoded).",
            "**Categories**: The category of the file.",
            "**Date**: The date of the file.",
            "**Description**: The description of the file.",
            "**Height**: The height of the file.",
            "**Id**: The Id of the GlobalImage Metadata.",
            "**Name**: The name of the file when downloaded.",
            "**Publisher**: The Publisher of the file.",
            "**Size**: The size of the file in bytes. Null until assigned by server when marked as 'Available'. Read Only",
            "**State**: Indicates the state of this file. Must be 'Created' when created. Read Only.",
            "**ThumbnailCRC**: The Hash of the thumbnail file (SHA256, HEX-encoded).",
            "**ThumbnailSize**: The size of the thumbnail file in bytes. Null until assigned by server when marked as 'Available'. Read Only",
            "**Width**: The width of the file."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**DeveloperMessage**",
            "**ErrorCode**",
            "**MoreInfo**",
            "**UserMessage**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
