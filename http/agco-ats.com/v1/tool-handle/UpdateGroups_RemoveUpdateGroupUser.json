{
  "name": "UpdateGroups_RemoveUpdateGroupUser",
  "description": "No Documentation Found.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The ID of the update group"
      },
      "userID": {
        "format": "int32",
        "type": "integer",
        "description": "The userID to link to the update group"
      }
    },
    "required": [
      "id",
      "userID"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://secure.agco-ats.com/api/v2/UpdateGroups/{id}/Users/{userID}"
    }
  },
  "responses": {
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**DeveloperMessage**",
            "**ErrorCode**",
            "**MoreInfo**",
            "**UserMessage**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
