{
  "name": "ContentRelease_PutContentDefinition",
  "description": "Updates a ContentReleaseVersion.  The body of the PUT is the updated ContentReleaseVersion.  \r\n            When successful, the response is empty.  If unsuccessful, an appropriate ApiError is returned.",
  "parameters": {
    "type": "object",
    "properties": {
      "ContentReleaseId": {
        "format": "int32",
        "type": "integer",
        "description": "The ID of the ContentReleaseVersion to update"
      },
      "body": {
        "$ref": "#/components/schemas/ContentSubmission.Shared.BusinessEntities.ContentReleaseVersion",
        "description": "The updated ContentReleaseVersion"
      }
    },
    "required": [
      "ContentReleaseId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://secure.agco-ats.com/api/v2/ContentReleases/{ContentReleaseId}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**DeveloperMessage**",
            "**ErrorCode**",
            "**MoreInfo**",
            "**UserMessage**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
