{
  "name": "Authentication_Default",
  "description": "No Documentation Found.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/API.Models.Credentials",
        "description": "Create a user account."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://secure.agco-ats.com/api/v2/Authentication"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**Email**: The user's email address",
            "**MACId**: The MAC identifier to use for API access",
            "**MACToken**: The MAC token to use for API access",
            "**Name**: The user's name",
            "**Token**: The token to use for API access",
            "**UserID**: The user ID",
            "**Username**: The username used for authentication"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
