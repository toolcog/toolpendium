{
  "name": "getRelationByENSGID",
  "description": "Get `relation` objects starting from diseases.\n",
  "parameters": {
    "type": "object",
    "properties": {
      "target": {
        "type": "string",
        "description": "An Ensembl gene identifier."
      }
    },
    "required": [
      "target"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//platform-api.opentargets.io/v3/platform/private/relation/target/{target}"
    }
  },
  "responses": {}
}
