{
  "name": "getDrugByID",
  "description": "Get `drug` objects.\n",
  "parameters": {
    "type": "object",
    "properties": {
      "drug_id": {
        "type": "string",
        "description": "An ID in the drug index."
      },
      "DRUG_ID": {
        "type": "string",
        "description": "Automatically added"
      }
    },
    "required": [
      "drug_id",
      "DRUG_ID"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//platform-api.opentargets.io/v3/platform/private/drug/{DRUG_ID}{?drug_id}"
    }
  },
  "responses": {}
}
