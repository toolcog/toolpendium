{
  "name": "getTargetByENSGID",
  "description": "Get `target` objects.\n",
  "parameters": {
    "type": "object",
    "properties": {
      "target": {
        "type": "string",
        "description": "An Ensembl gene ID for the target of interest."
      }
    },
    "required": [
      "target"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//platform-api.opentargets.io/v3/platform/private/target/{target}"
    }
  },
  "responses": {}
}
