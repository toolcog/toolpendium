{
  "name": "postBestHitSearch",
  "description": "Fire the search method for multiple strings\n",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "type": "string",
        "description": "list of strings to search for"
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//platform-api.opentargets.io/v3/platform/private/besthitsearch"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
