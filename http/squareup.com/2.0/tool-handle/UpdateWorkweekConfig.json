{
  "name": "UpdateWorkweekConfig",
  "description": "Updates a `WorkweekConfig`.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The UUID for the `WorkweekConfig` object being updated."
      },
      "body": {
        "$ref": "#/components/schemas/UpdateWorkweekConfigRequest",
        "description": "An object containing the fields to POST for the request.\n\nSee the corresponding object definition for field details."
      }
    },
    "required": [
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://connect.squareup.com/v2/labor/workweek-configs/{id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "The response to a request to update a `WorkweekConfig` object. The response contains\nthe updated `WorkweekConfig` object and might contain a set of `Error` objects if\nthe request resulted in errors.",
        "**Key properties:**",
        {
          "$ul": [
            "**errors**: Any errors that occurred during the request.",
            [
              "**workweek_config**: Sets the day of the week and hour of the day that a business starts a",
              {
                "$ul": [
                  "**created_at**: A read-only timestamp in RFC 3339 format; presented in UTC.",
                  "**id**: The UUID for this object.",
                  "**start_of_day_local_time**: The local time at which a business week ends. Represented as a",
                  "**start_of_week**: The day of the week on which a business week ends for",
                  "**updated_at**: A read-only timestamp in RFC 3339 format; presented in UTC.",
                  "**version**: Used for resolving concurrency issues. The request fails if the version"
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
