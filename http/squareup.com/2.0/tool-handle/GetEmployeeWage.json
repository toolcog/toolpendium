{
  "name": "GetEmployeeWage",
  "description": "Returns a single `EmployeeWage` specified by `id`.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The UUID for the `EmployeeWage` being retrieved."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://connect.squareup.com/v2/labor/employee-wages/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "A response to a request to get an `EmployeeWage`. The response contains\nthe requested `EmployeeWage` objects and might contain a set of `Error` objects if\nthe request resulted in errors.",
        "**Key properties:**",
        {
          "$ul": [
            [
              "**employee_wage**: The hourly wage rate that an employee earns on a `Shift` for doing the job",
              {
                "$ul": [
                  "**employee_id**: The `Employee` that this wage is assigned to.",
                  [
                    "**hourly_rate**: Represents an amount of money. `Money` fields can be signed or unsigned.",
                    {
                      "$ul": [
                        "**amount**: The amount of money, in the smallest denomination of the currency",
                        "**currency**: The type of currency, in __ISO 4217 format__. For example, the currency"
                      ]
                    }
                  ],
                  "**id**: The UUID for this object.",
                  "**title**: The job title that this wage relates to."
                ]
              }
            ],
            "**errors**: Any errors that occurred during the request."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
