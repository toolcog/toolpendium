{
  "name": "ObtainToken",
  "description": "Returns an OAuth access token.\n\nThe endpoint supports distinct methods of obtaining OAuth access tokens.\nApplications specify a method by adding the `grant_type` parameter\nin the request and also provide relevant information.\n\n__Note:__ Regardless of the method application specified,\nthe endpoint always returns two items; an OAuth access token and\na refresh token in the response.\n\n__OAuth tokens should only live on secure servers. Application clients\nshould never interact directly with OAuth tokens__.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/ObtainTokenRequest",
        "description": "An object containing the fields to POST for the request.\n\nSee the corresponding object definition for field details."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://connect.squareup.com/oauth2/token"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "",
        "**Key properties:**",
        {
          "$ul": [
            "**access_token**: A valid OAuth access token. OAuth access tokens are 64 bytes long.",
            "**expires_at**: The date when access_token expires, in [ISO 8601](http://www.iso.org/iso/home/standards/iso8601.htm) format.",
            "**id_token**: Then OpenID token belonging to this this person. Only present if the",
            "**merchant_id**: The ID of the authorizing merchant's business.",
            "**plan_id**: __LEGACY FIELD__. The ID of the subscription plan the merchant signed",
            "**refresh_token**: A refresh token. OAuth refresh tokens are 64 bytes long.",
            "**short_lived**: A boolean indicating the access token is a short-lived access token.",
            "**subscription_id**: __LEGACY FIELD__. The ID of a subscription plan the merchant signed up",
            "**token_type**: This value is always _bearer_."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
