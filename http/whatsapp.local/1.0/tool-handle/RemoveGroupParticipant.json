{
  "name": "RemoveGroupParticipant",
  "description": "Remove-Group-Participant",
  "parameters": {
    "type": "object",
    "properties": {
      "GroupId": {
        "type": "string",
        "description": ""
      },
      "body": {
        "$ref": "#/components/schemas/Remove-Group-Participant-Request-Body",
        "description": ""
      }
    },
    "required": [
      "GroupId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "http://whatsapp.local/groups/{GroupId}/participants"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
