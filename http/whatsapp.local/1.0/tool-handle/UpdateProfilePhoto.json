{
  "name": "UpdateProfilePhoto",
  "description": "Update-Profile-Photo",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "properties": {
          "File": {
            "format": "binary",
            "type": "string"
          }
        },
        "required": [
          "File"
        ],
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://whatsapp.local/settings/profile/photo"
    },
    "body": {
      "$": "body",
      "encode": "multipart"
    }
  },
  "responses": {}
}
