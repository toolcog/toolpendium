{
  "name": "getQueryJobResultsForProject",
  "description": "Fetch the results for a specific project. The endpoint succeeds only if the query was successful,  and returns a `404` error otherwise.  By default, the endpoint provides only results that are within the source tree. To obtain all results, specify the `nofilter` parameter.\n",
  "parameters": {
    "type": "object",
    "properties": {
      "queryjob-id": {
        "type": "string",
        "description": "The identifier of the query job, from the `task-result` given in the response to the initial `POST /queryjobs` request."
      },
      "project-id": {
        "type": "string",
        "description": "The identifier for the project."
      },
      "start": {
        "format": "int32",
        "type": "integer",
        "description": "Start point for the page of results."
      },
      "limit": {
        "default": 100,
        "format": "int32",
        "type": "integer",
        "description": "The maximum number of results to display (less than 100)."
      },
      "nofilter": {
        "default": false,
        "type": "boolean",
        "description": "Include results that are not part of the source tree. These results are filtered out by default."
      }
    },
    "required": [
      "queryjob-id",
      "project-id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://lgtm.com/api/v1.0/queryjobs/{queryjob-id}/results/{project-id}{?start,limit,nofilter}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**columns**: Columns returned by the query.",
            "**data**: Results of the query divided by row.",
            "**next**: URL for retrieving the next part of the results (if applicable).",
            [
              "**project**",
              {
                "$ul": [
                  "**id**: The numeric identifier of the project.",
                  "**name**: The display name of the project.",
                  "**url**: The full URL of the project on LGTM.",
                  "**url-identifier**: The URL identifier of the project."
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
