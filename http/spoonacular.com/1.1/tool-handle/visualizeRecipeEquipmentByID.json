{
  "name": "visualizeRecipeEquipmentByID",
  "description": "Visualize a recipe's equipment list.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "example": 44860,
        "type": "number",
        "description": "The recipe id."
      },
      "defaultCss": {
        "default": true,
        "example": false,
        "type": "boolean",
        "description": "Whether the default CSS should be added to the response."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.spoonacular.com/recipes/{id}/equipmentWidget{?defaultCss}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
