{
  "name": "visualizeRecipeIngredientsByID",
  "description": "Visualize a recipe's ingredient list.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "example": 1082038,
        "type": "number",
        "description": "The recipe id."
      },
      "defaultCss": {
        "default": true,
        "example": false,
        "type": "boolean",
        "description": "Whether the default CSS should be added to the response."
      },
      "measure": {
        "enum": [
          "us",
          "metric"
        ],
        "example": "metric",
        "type": "string",
        "description": "Whether the the measures should be 'us' or 'metric'."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.spoonacular.com/recipes/{id}/ingredientWidget{?defaultCss,measure}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
