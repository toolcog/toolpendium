{
  "name": "appliedInventoriesTagsForServiceOffering",
  "description": "Returns an array of inventories tags",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "pattern": "^\\d+$",
        "type": "string",
        "description": "ID of the resource"
      },
      "body": {
        "$ref": "#/components/schemas/AppliedInventoriesParametersServicePlan",
        "description": "Parameters defining input data for computing inventories"
      }
    },
    "required": [
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://cloud.redhat.com/%2Fapi%2Fcatalog-inventory%2Fv1.0/service_offerings/{id}/applied_inventories_tags"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**tag**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": "Item"
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
