{
  "name": "DeleteItemAssignment",
  "description": "Remove an Assignment Item Instance.",
  "parameters": {
    "type": "object",
    "properties": {
      "BundleSid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^BU[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The unique string that we created to identify the Bundle resource."
      },
      "Sid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^BV[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The unique string that we created to identify the Identity resource."
      }
    },
    "required": [
      "BundleSid",
      "Sid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://numbers.twilio.com/v2/RegulatoryCompliance/Bundles/{BundleSid}/ItemAssignments/{Sid}"
    }
  },
  "responses": {}
}
