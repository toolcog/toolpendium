{
  "name": "DeleteHostedNumberOrder",
  "description": "Cancel the HostedNumberOrder (only available when the status is in `received`).",
  "parameters": {
    "type": "object",
    "properties": {
      "Sid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^HR[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "A 34 character string that uniquely identifies this HostedNumberOrder."
      }
    },
    "required": [
      "Sid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://numbers.twilio.com/v2/HostedNumber/Orders/{Sid}"
    }
  },
  "responses": {}
}
