{
  "name": "FetchEvaluation",
  "description": "Fetch specific Evaluation Instance.",
  "parameters": {
    "type": "object",
    "properties": {
      "BundleSid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^BU[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The unique string that we created to identify the Bundle resource."
      },
      "Sid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^EL[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The unique string that identifies the Evaluation resource."
      }
    },
    "required": [
      "BundleSid",
      "Sid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://numbers.twilio.com/v2/RegulatoryCompliance/Bundles/{BundleSid}/Evaluations/{Sid}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**account_sid**: The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Bundle resource.",
            "**bundle_sid**: The unique string that we created to identify the Bundle resource.",
            "**date_created**",
            "**regulation_sid**: The unique string of a regulation that is associated to the Bundle resource.",
            "**results**: The results of the Evaluation which includes the valid and invalid attributes.",
            "**sid**: The unique string that identifies the Evaluation resource.",
            "**status**",
            "**url**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
