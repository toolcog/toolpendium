{
  "name": "FetchDocumentPermission",
  "description": "Fetch a specific Sync Document Permission.",
  "parameters": {
    "type": "object",
    "properties": {
      "ServiceSid": {
        "type": "string",
        "description": "The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) with the Document Permission resource to fetch."
      },
      "DocumentSid": {
        "type": "string",
        "description": "The SID of the Sync Document with the Document Permission resource to fetch. Can be the Document resource's `sid` or its `unique_name`."
      },
      "Identity": {
        "type": "string",
        "description": "The application-defined string that uniquely identifies the User's Document Permission resource to fetch."
      }
    },
    "required": [
      "ServiceSid",
      "DocumentSid",
      "Identity"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://sync.twilio.com/v1/Services/{ServiceSid}/Documents/{DocumentSid}/Permissions/{Identity}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**account_sid**: The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Document Permission resource.",
            "**document_sid**: The SID of the Sync Document to which the Document Permission applies.",
            "**identity**: The application-defined string that uniquely identifies the resource's User within the Service to an FPA token.",
            "**manage**: Whether the identity can delete the Sync Document.",
            "**read**: Whether the identity can read the Sync Document.",
            "**service_sid**: The SID of the [Sync Service](https://www.twilio.com/docs/sync/api/service) the resource is associated with.",
            "**url**: The absolute URL of the Sync Document Permission resource.",
            "**write**: Whether the identity can update the Sync Document."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
