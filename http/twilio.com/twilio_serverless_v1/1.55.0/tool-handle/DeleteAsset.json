{
  "name": "DeleteAsset",
  "description": "Delete an Asset resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "ServiceSid": {
        "type": "string",
        "description": "The SID of the Service to delete the Asset resource from."
      },
      "Sid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^ZH[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The SID that identifies the Asset resource to delete."
      }
    },
    "required": [
      "ServiceSid",
      "Sid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://serverless.twilio.com/v1/Services/{ServiceSid}/Assets/{Sid}"
    }
  },
  "responses": {}
}
