{
  "name": "ListMediaProcessor",
  "description": "Returns a list of MediaProcessors.",
  "parameters": {
    "type": "object",
    "properties": {
      "Order": {
        "$ref": "#/components/schemas/media_processor_enum_order",
        "type": "string",
        "description": "The sort order of the list by `date_created`. Can be: `asc` (ascending) or `desc` (descending) with `desc` as the default."
      },
      "Status": {
        "$ref": "#/components/schemas/media_processor_enum_status",
        "type": "string",
        "description": "Status to filter by, with possible values `started`, `ended` or `failed`."
      },
      "PageSize": {
        "maximum": 1000,
        "minimum": 1,
        "type": "integer",
        "description": "How many resources to return in each list page. The default is 50, and the maximum is 1000."
      },
      "Page": {
        "minimum": 0,
        "type": "integer",
        "description": "The page index. This value is simply for client state."
      },
      "PageToken": {
        "type": "string",
        "description": "The page token. This is provided by the API."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://media.twilio.com/v1/MediaProcessors{?Order,Status,PageSize,Page,PageToken}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "ListMediaProcessorResponse"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**media_processors**",
            [
              "**meta**",
              {
                "$ul": [
                  "**first_page_url**",
                  "**key**",
                  "**next_page_url**",
                  "**page**",
                  "**page_size**",
                  "**previous_page_url**",
                  "**url**"
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
