{
  "name": "FetchMediaRecording",
  "description": "Returns a single MediaRecording resource identified by a SID.",
  "parameters": {
    "type": "object",
    "properties": {
      "Sid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^KV[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The SID of the MediaRecording resource to fetch."
      }
    },
    "required": [
      "Sid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://media.twilio.com/v1/MediaRecordings/{Sid}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**account_sid**: The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the MediaRecording resource.",
            "**date_created**: The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.",
            "**date_updated**: The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.",
            "**duration**: The duration of the MediaRecording in seconds.",
            "**format**",
            "**links**: The URLs of related resources.",
            "**media_size**: The size of the recording media in bytes.",
            "**processor_sid**: The SID of the MediaProcessor resource which produced the MediaRecording.",
            "**resolution**: The dimensions of the video image in pixels expressed as columns (width) and rows (height).",
            "**sid**: The unique string generated to identify the MediaRecording resource.",
            "**source_sid**: The SID of the resource that generated the original media track(s) of the MediaRecording.",
            "**status**",
            "**status_callback**: The URL to which Twilio will send asynchronous webhook requests for every MediaRecording event. See [Status Callbacks](/docs/live/api/status-callbacks) for more details.",
            "**status_callback_method**: The HTTP method Twilio should use to call the `status_callback` URL. Can be `POST` or `GET` and the default is `POST`.",
            "**url**: The absolute URL of the resource."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
