{
  "name": "DeleteRole",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "ServiceSid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^IS[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The SID of the [Service](https://www.twilio.com/docs/chat/rest/service-resource) to delete the Role resource from."
      },
      "Sid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^RL[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The SID of the Role resource to delete."
      }
    },
    "required": [
      "ServiceSid",
      "Sid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://chat.twilio.com/v2/Services/{ServiceSid}/Roles/{Sid}"
    }
  },
  "responses": {}
}
