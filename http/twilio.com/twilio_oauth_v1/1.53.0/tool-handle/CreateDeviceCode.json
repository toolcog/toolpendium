{
  "name": "CreateDeviceCode",
  "description": "Issues a new Access token (optionally identity_token & refresh_token) in exchange of Oauth grant",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "properties": {
          "Audiences": {
            "description": "An array of intended audiences for token requests",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "ClientSid": {
            "description": "A 34 character string that uniquely identifies this OAuth App.",
            "type": "string"
          },
          "Scopes": {
            "description": "An Array of scopes for authorization request",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "ClientSid",
          "Scopes"
        ],
        "title": "CreateDeviceCodeRequest",
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://oauth.twilio.com/v1/device/code"
    },
    "body": {
      "$": "body",
      "encode": "urlencoded"
    }
  },
  "responses": {
    "201": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**device_code**: The device verification code.",
            "**expires_in**: The expiration time of the device_code and user_code in seconds.",
            "**interval**: The minimum amount of time in seconds that the client should wait between polling requests to the token endpoint.",
            "**user_code**: The verification code which end user uses to verify authorization request.",
            "**verification_uri**: The URI that the end user visits to verify authorization request.",
            "**verification_uri_complete**: The URI with user_code that the end-user alternatively visits to verify authorization request."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
