{
  "name": "FetchSyncDocumentPermission",
  "description": "Fetch a specific Sync Document Permission.",
  "parameters": {
    "type": "object",
    "properties": {
      "ServiceSid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^IS[0-9a-fA-F]{32}$",
        "type": "string",
        "description": ""
      },
      "DocumentSid": {
        "type": "string",
        "description": "Identifier of the Sync Document. Either a SID or a unique name."
      },
      "Identity": {
        "type": "string",
        "description": "Arbitrary string identifier representing a user associated with an FPA token, assigned by the developer."
      }
    },
    "required": [
      "ServiceSid",
      "DocumentSid",
      "Identity"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://preview.twilio.com/Sync/Services/{ServiceSid}/Documents/{DocumentSid}/Permissions/{Identity}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**account_sid**: The unique SID identifier of the Twilio Account.",
            "**document_sid**: The unique SID identifier of the Sync Document to which the Permission applies.",
            "**identity**: Arbitrary string identifier representing a human user associated with an FPA token, assigned by the developer.",
            "**manage**: Boolean flag specifying whether the identity can delete the Sync Document.",
            "**read**: Boolean flag specifying whether the identity can read the Sync Document.",
            "**service_sid**: The unique SID identifier of the Sync Service Instance.",
            "**url**: Contains an absolute URL for this Sync Document Permission.",
            "**write**: Boolean flag specifying whether the identity can update the Sync Document."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
