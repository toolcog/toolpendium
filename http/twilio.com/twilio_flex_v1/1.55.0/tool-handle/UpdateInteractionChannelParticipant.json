{
  "name": "UpdateInteractionChannelParticipant",
  "description": "Update an existing Channel Participant.",
  "parameters": {
    "type": "object",
    "properties": {
      "InteractionSid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^KD[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The Interaction Sid for this channel."
      },
      "ChannelSid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^UO[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The Channel Sid for this Participant."
      },
      "Sid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^UT[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The unique string created by Twilio to identify an Interaction Channel resource."
      },
      "body": {
        "properties": {
          "Status": {
            "$ref": "#/$defs/interaction_channel_participant_enum_status"
          }
        },
        "required": [
          "Status"
        ],
        "title": "UpdateInteractionChannelParticipantRequest",
        "type": "object"
      }
    },
    "required": [
      "InteractionSid",
      "ChannelSid",
      "Sid"
    ],
    "$defs": {
      "interaction_channel_participant_enum_status": {
        "enum": [
          "closed",
          "wrapup"
        ],
        "type": "string"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://flex-api.twilio.com/v1/Interactions/{InteractionSid}/Channels/{ChannelSid}/Participants/{Sid}"
    },
    "body": {
      "$": "body",
      "encode": "urlencoded"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**channel_sid**: The Channel Sid for this Participant.",
            "**interaction_sid**: The Interaction Sid for this channel.",
            "**routing_properties**: The Participant's routing properties.",
            "**sid**: The unique string created by Twilio to identify an Interaction Channel Participant resource.",
            "**type**",
            "**url**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
