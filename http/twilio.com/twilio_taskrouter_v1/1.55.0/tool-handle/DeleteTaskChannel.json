{
  "name": "DeleteTaskChannel",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "WorkspaceSid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^WS[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The SID of the Workspace with the Task Channel to delete."
      },
      "Sid": {
        "type": "string",
        "description": "The SID of the Task Channel resource to delete."
      }
    },
    "required": [
      "WorkspaceSid",
      "Sid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://taskrouter.twilio.com/v1/Workspaces/{WorkspaceSid}/TaskChannels/{Sid}"
    }
  },
  "responses": {}
}
