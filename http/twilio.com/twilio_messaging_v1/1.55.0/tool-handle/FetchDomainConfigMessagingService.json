{
  "name": "FetchDomainConfigMessagingService",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "MessagingServiceSid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^MG[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "Unique string used to identify the Messaging service that this domain should be associated with."
      }
    },
    "required": [
      "MessagingServiceSid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://messaging.twilio.com/v1/LinkShortening/MessagingService/{MessagingServiceSid}/DomainConfig"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**callback_url**: URL to receive click events to your webhook whenever the recipients click on the shortened links.",
            "**config_sid**: The unique string that we created to identify the Domain config (prefix ZK).",
            "**continue_on_failure**: Boolean field to set customer delivery preference when there is a failure in linkShortening service",
            "**date_created**: Date this Domain Config was created.",
            "**date_updated**: Date that this Domain Config was last updated.",
            "**domain_sid**: The unique string that we created to identify the Domain resource.",
            "**fallback_url**: Any requests we receive to this domain that do not match an existing shortened message will be redirected to the fallback url. These will likely be either expired messages, random misdirected traffic, or intentional scraping.",
            "**messaging_service_sid**: The unique string that identifies the messaging service",
            "**url**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
