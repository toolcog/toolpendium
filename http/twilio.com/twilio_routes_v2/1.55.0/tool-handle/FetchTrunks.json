{
  "name": "FetchTrunks",
  "description": "Fetch the Inbound Processing Region assigned to a SIP Trunk.",
  "parameters": {
    "type": "object",
    "properties": {
      "SipTrunkDomain": {
        "type": "string",
        "description": "The absolute URL of the SIP Trunk"
      }
    },
    "required": [
      "SipTrunkDomain"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://routes.twilio.com/v2/Trunks/{SipTrunkDomain}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**account_sid**: The unique SID identifier of the Account.",
            "**date_created**: The date that this SIP Trunk was assigned an Inbound Processing Region, given in ISO 8601 format.",
            "**date_updated**: The date that the Inbound Processing Region was updated for this SIP Trunk, given in ISO 8601 format.",
            "**friendly_name**: A human readable description of the Inbound Processing Region assignments for this SIP Trunk, up to 64 characters.",
            "**sid**: A 34 character string that uniquely identifies the Inbound Processing Region assignments for this SIP Trunk.",
            "**sip_trunk_domain**: The absolute URL of the SIP Trunk",
            "**url**: The absolute URL of the resource.",
            "**voice_region**: The Inbound Processing Region used for this SIP Trunk for voice."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
