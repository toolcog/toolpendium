{
  "name": "DeleteRoomRecording",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "RoomSid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^RM[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The SID of the room with the RoomRecording resource to delete."
      },
      "Sid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^RT[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The SID of the RoomRecording resource to delete."
      }
    },
    "required": [
      "RoomSid",
      "Sid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://video.twilio.com/v1/Rooms/{RoomSid}/Recordings/{Sid}"
    }
  },
  "responses": {}
}
