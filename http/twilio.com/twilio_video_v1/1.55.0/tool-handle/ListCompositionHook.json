{
  "name": "ListCompositionHook",
  "description": "List of all Recording CompositionHook resources.",
  "parameters": {
    "type": "object",
    "properties": {
      "Enabled": {
        "type": "boolean",
        "description": "Read only CompositionHook resources with an `enabled` value that matches this parameter."
      },
      "DateCreatedAfter": {
        "format": "date-time",
        "type": "string",
        "description": "Read only CompositionHook resources created on or after this [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) datetime with time zone."
      },
      "DateCreatedBefore": {
        "format": "date-time",
        "type": "string",
        "description": "Read only CompositionHook resources created before this [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) datetime with time zone."
      },
      "FriendlyName": {
        "type": "string",
        "description": "Read only CompositionHook resources with friendly names that match this string. The match is not case sensitive and can include asterisk `*` characters as wildcard match."
      },
      "PageSize": {
        "maximum": 1000,
        "minimum": 1,
        "type": "integer",
        "description": "How many resources to return in each list page. The default is 50, and the maximum is 1000."
      },
      "Page": {
        "minimum": 0,
        "type": "integer",
        "description": "The page index. This value is simply for client state."
      },
      "PageToken": {
        "type": "string",
        "description": "The page token. This is provided by the API."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://video.twilio.com/v1/CompositionHooks{?Enabled,DateCreatedAfter,DateCreatedBefore,FriendlyName,PageSize,Page,PageToken}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "ListCompositionHookResponse"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**composition_hooks**",
            [
              "**meta**",
              {
                "$ul": [
                  "**first_page_url**",
                  "**key**",
                  "**next_page_url**",
                  "**page**",
                  "**page_size**",
                  "**previous_page_url**",
                  "**url**"
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
