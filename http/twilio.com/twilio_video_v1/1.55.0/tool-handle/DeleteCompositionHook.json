{
  "name": "DeleteCompositionHook",
  "description": "Delete a Recording CompositionHook resource identified by a `CompositionHook SID`.",
  "parameters": {
    "type": "object",
    "properties": {
      "Sid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^HK[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The SID of the CompositionHook resource to delete."
      }
    },
    "required": [
      "Sid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://video.twilio.com/v1/CompositionHooks/{Sid}"
    }
  },
  "responses": {}
}
