{
  "name": "DeleteSubscription",
  "description": "Delete a specific Subscription.",
  "parameters": {
    "type": "object",
    "properties": {
      "Sid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^DF[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "A 34 character string that uniquely identifies this Subscription."
      }
    },
    "required": [
      "Sid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://events.twilio.com/v1/Subscriptions/{Sid}"
    }
  },
  "responses": {}
}
