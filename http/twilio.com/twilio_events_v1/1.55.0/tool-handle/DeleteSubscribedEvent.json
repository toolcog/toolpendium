{
  "name": "DeleteSubscribedEvent",
  "description": "Remove an event type from a Subscription.",
  "parameters": {
    "type": "object",
    "properties": {
      "SubscriptionSid": {
        "maxLength": 34,
        "minLength": 34,
        "pattern": "^DF[0-9a-fA-F]{32}$",
        "type": "string",
        "description": "The unique SID identifier of the Subscription."
      },
      "Type": {
        "type": "string",
        "description": "Type of event being subscribed to."
      }
    },
    "required": [
      "SubscriptionSid",
      "Type"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://events.twilio.com/v1/Subscriptions/{SubscriptionSid}/SubscribedEvents/{Type}"
    }
  },
  "responses": {}
}
