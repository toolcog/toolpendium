{
  "name": "Customers_GetAccountTrans",
  "description": "Returns a list of Customer's account transactions.",
  "parameters": {
    "type": "object",
    "properties": {
      "itemId": {
        "format": "int64",
        "type": "integer",
        "description": "Id of Customer to return account transaction."
      }
    },
    "required": [
      "itemId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://app.bigredcloud.com/api/v1/customers/{itemId}/accountTrans"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**bookTranId**",
            "**bookTranTypeId**",
            "**bookTransactionReference**",
            "**bookTypeDesc**",
            "**credit**",
            "**debit**",
            "**id**",
            "**procDate**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": "Item"
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
