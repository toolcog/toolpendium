{
  "name": "Email_SendQuote",
  "description": "Sends a Quote email.\r\nIf \"toAddress\" is not empty then email will be sent to this address. Otherwise email will be sent to Statement Customer's address.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/EmailQuoteDto"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "EmailQuoteDto": {
        "example": {
          "bccAddresses": [
            "bcc1@email.com",
            "bcc2@email.com",
            "bcc3@email.com"
          ],
          "messageBody": "Email message",
          "quoteId": 1,
          "toAddress": "to@email.com"
        },
        "properties": {
          "bccAddresses": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "messageBody": {
            "type": "string"
          },
          "quoteId": {
            "format": "int64",
            "type": "integer"
          },
          "toAddress": {
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://app.bigredcloud.com/api/v1/email/sendQuote"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
