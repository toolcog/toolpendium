{
  "name": "Payments_Delete",
  "description": "Removes an existing Payment.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int64",
        "type": "integer",
        "description": "Id of Payment to remove."
      },
      "timestamp": {
        "type": "string",
        "description": "Timestamp of Payment to remove. Should be encoded in Base64."
      }
    },
    "required": [
      "id",
      "timestamp"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://app.bigredcloud.com/api/v1/payments/{id}{?timestamp}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
