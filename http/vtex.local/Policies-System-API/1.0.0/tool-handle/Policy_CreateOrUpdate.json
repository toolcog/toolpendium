{
  "name": "Policy_CreateOrUpdate",
  "description": "Creates a new policy from scratch.",
  "parameters": {
    "type": "object",
    "properties": {
      "Content-Type": {
        "default": "application/json",
        "type": "string",
        "description": "Describes the type of the content being sent"
      },
      "Accept": {
        "default": "application/json",
        "type": "string",
        "description": "HTTP Client Negotiation _Accept_ Header. Indicates the types of responses the client can understand "
      },
      "id": {
        "default": "pa_test_001",
        "type": "string",
        "description": "Policy ID"
      },
      "body": {
        "$ref": "#/components/schemas/PolicySaveRequest",
        "description": ""
      }
    },
    "required": [
      "Content-Type",
      "Accept",
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://vtex.local/api/policy-engine/policies/{id}"
    },
    "headers": {
      "Content-Type": {
        "$": "Content-Type"
      },
      "Accept": {
        "$": "Accept"
      }
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "PolicyGetResponse list"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**description**: Policy description. This description is only for internal use of identification",
            "**id**: Policy ID",
            "**name**: Policy Name",
            "**statements**: Requirements to the Policy be applied"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.name"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
