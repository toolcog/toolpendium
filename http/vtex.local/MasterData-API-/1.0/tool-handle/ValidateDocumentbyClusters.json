{
  "name": "ValidateDocumentbyClusters",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "Accept": {
        "default": "application/vnd.vtex.ds.v10+json",
        "type": "string",
        "description": "HTTP Client Negotiation _Accept_ Header. Indicates the types of responses the client can understand "
      },
      "acronym": {
        "default": "{{acronym}}",
        "type": "string",
        "description": "Two letter word that identifies the data structure"
      },
      "id": {
        "default": "{{id}}",
        "type": "string",
        "description": "Id of the document"
      },
      "body": {
        "items": {},
        "type": "array",
        "description": ""
      }
    },
    "required": [
      "Accept",
      "acronym",
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://vtex.local/api/dataentities/{acronym}/documents/{id}/clusters"
    },
    "headers": {
      "Accept": {
        "$": "Accept"
      }
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
