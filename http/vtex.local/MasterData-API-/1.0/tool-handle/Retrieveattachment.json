{
  "name": "Retrieveattachment",
  "description": "Use this API to retrieve a file.\r\n\r\nBe sure to include the file extension in the name. Like in this example:\r\n```\r\n/dataentities/CL/documents/123/file/attachments/image.png\r\n```",
  "parameters": {
    "type": "object",
    "properties": {
      "acronym": {
        "default": "{{acronym}}",
        "type": "string",
        "description": "Two letter word that identifies the data structure"
      },
      "id": {
        "default": "{{id}}",
        "type": "string",
        "description": "Id of the document"
      },
      "field": {
        "default": "{{field}}",
        "type": "string",
        "description": "Field to attach the file to, as described in admin"
      },
      "file-name": {
        "default": "{{file-name}}",
        "type": "string",
        "description": ""
      }
    },
    "required": [
      "acronym",
      "id",
      "field",
      "file-name"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://vtex.local/api/dataentities/{acronym}/documents/{id}/{field}/attachments/{file-name}"
    }
  },
  "responses": {}
}
