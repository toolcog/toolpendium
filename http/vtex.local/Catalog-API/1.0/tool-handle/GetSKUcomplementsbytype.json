{
  "name": "GetSKUcomplementsbytype",
  "description": "Retrieves all the existing SKU complements with the same complement type ID of a specific SKU. \r\n \r\n## Response body example\r\n\r\n```json\r\n{\r\n    \"ParentSkuId\": 1,\r\n    \"ComplementSkuIds\": [\r\n        7\r\n    ],\r\n    \"Type\": \"1\"\r\n}\r\n```",
  "parameters": {
    "type": "object",
    "properties": {
      "Content-Type": {
        "default": "application/json",
        "type": "string",
        "description": "Type of the content being sent."
      },
      "Accept": {
        "default": "application/json",
        "type": "string",
        "description": "HTTP Client Negotiation _Accept_ Header. Indicates the types of responses the client can understand."
      },
      "parentSkuId": {
        "example": 1,
        "type": "integer",
        "description": "ID of the Parent SKU, where the Complement is inserted."
      },
      "type": {
        "example": 1,
        "type": "integer",
        "description": "Complement Type ID. This represents the type of the complement. The possible values are: `1` for Accessory; `2` for Suggestion; `3` for Similar Product; `5` for Show Together."
      }
    },
    "required": [
      "Content-Type",
      "Accept",
      "parentSkuId",
      "type"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://vtex.local/api/catalog_system/pvt/sku/complements/{parentSkuId}/{type}"
    },
    "headers": {
      "Content-Type": {
        "$": "Content-Type"
      },
      "Accept": {
        "$": "Accept"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": ""
        },
        "**Key properties:**",
        {
          "$ul": [
            "**ComplementSkuIds**: Array with SKU complements IDs.",
            "**ParentSkuId**: ID of the Parent SKU, where the Complement is inserted.",
            "**Type**: Complement Type ID. This represents the type of the complement. The possible values are: `1` for Accessory; `2` for Suggestion; `3` for Similar Product; `5` for Show Together."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
