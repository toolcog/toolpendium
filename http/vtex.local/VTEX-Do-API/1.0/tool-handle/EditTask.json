{
  "name": "EditTask",
  "description": "Updates a given task's status, for example, filtering by `taskId`.",
  "parameters": {
    "type": "object",
    "properties": {
      "Accept": {
        "example": "application/json",
        "type": "string",
        "description": "HTTP Client Negotiation Accept Header. Indicates the types of responses the client can understand."
      },
      "Content-Type": {
        "example": "application/json",
        "type": "string",
        "description": "Type of the content being sent."
      },
      "taskId": {
        "example": "123456abc",
        "type": "string",
        "description": "Task ID."
      },
      "body": {
        "$ref": "#/components/schemas/EditTaskRequest",
        "description": ""
      }
    },
    "required": [
      "Accept",
      "Content-Type",
      "taskId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://vtex.local/tasks/{taskId}"
    },
    "headers": {
      "Accept": {
        "$": "Accept"
      },
      "Content-Type": {
        "$": "Content-Type"
      }
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
