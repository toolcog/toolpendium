{
  "name": "StatusCompleted",
  "description": "Retrieves a list of all order reports that are `completed`, by `accountName`. Be aware that the scope of the export log is per user. \r\n\r\n> This endpoint is for VTEX internal use, and it is not meant to be used by clients.",
  "parameters": {
    "type": "object",
    "properties": {
      "Content-Type": {
        "default": "application/json",
        "type": "string",
        "description": "Type of the content being sent."
      },
      "Accept": {
        "default": "application/json",
        "type": "string",
        "description": "HTTP Client Negotiation Accept Header. Indicates the types of responses the client can understand."
      }
    },
    "required": [
      "Content-Type",
      "Accept"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://vtex.local/api/oms/pvt/admin/reports/completed"
    },
    "headers": {
      "Content-Type": {
        "$": "Content-Type"
      },
      "Accept": {
        "$": "Accept"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "ExportCompletedResponse list"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**cancelled**: If the export request was cancelled (`true`) or not (`false`).",
            "**completedDate**: Order export process completed date.",
            "**dateOfRequest**: Order export request date.",
            "**email**: Order export requester email.",
            "**filter**: Order export request filter.",
            "**hostUri**: Host URL.",
            "**id**: Order export request ID.",
            "**instanceId**: Instance ID.",
            "**lasUpdateTime**: Order export process update time.",
            "**linkToDownload**: Link to order export download.",
            "**publishId**: Order export publish ID.",
            "**query**: Order export request filter query.",
            "**rowNumber**: Order export total rows.",
            "**rowsProcessed**: Order export total processed rows.",
            "**startDate**: Order export process start date.",
            "**utcTime**: UTC time."
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.id"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
