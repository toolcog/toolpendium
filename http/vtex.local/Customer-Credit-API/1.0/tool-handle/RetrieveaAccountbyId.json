{
  "name": "RetrieveaAccountbyId",
  "description": "Retrieve an account by id.",
  "parameters": {
    "type": "object",
    "properties": {
      "Content-Type": {
        "default": "application/json",
        "type": "string",
        "description": "The Media type of the body of the request. Default value for payment provider protocol is application/json"
      },
      "Accept": {
        "default": "application/json",
        "type": "string",
        "description": "Media type(s) that is/are acceptable for the response. Default value for payment provider protocol is application/json"
      },
      "creditAccountId": {
        "default": "insert identifier here",
        "type": "string",
        "description": ""
      }
    },
    "required": [
      "Content-Type",
      "Accept",
      "creditAccountId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://vtex.local/api/creditcontrol/accounts/{creditAccountId}"
    },
    "headers": {
      "Content-Type": {
        "$": "Content-Type"
      },
      "Accept": {
        "$": "Accept"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Getaccount"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**account**",
            "**balance**",
            "**creditLimit**",
            "**description**",
            "**email**",
            "**lastUpdate**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
