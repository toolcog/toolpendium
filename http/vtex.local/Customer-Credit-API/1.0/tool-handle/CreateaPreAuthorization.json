{
  "name": "CreateaPreAuthorization",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "creditAccountId": {
        "default": "insert identifier here",
        "type": "string",
        "description": "Credit account's identification"
      },
      "Accept": {
        "default": "application/json",
        "type": "string",
        "description": "Media type(s) that is/are acceptable for the response. Default value for payment provider protocol is application/json"
      },
      "Content-Type": {
        "default": "application/json",
        "type": "string",
        "description": "The Media type of the body of the request. Default value for payment provider protocol is application/json"
      },
      "body": {
        "example": {
          "expirationDate": "date in ISO8601 (UTC) dateformat (optional default is 1(one) day)",
          "installments": "integer (optional default is 1)",
          "settle": false,
          "value": "decimal (required)"
        },
        "properties": {
          "expirationDate": {
            "default": "1",
            "description": "date in ISO8601 (UTC) dateformat (optional default is 1(one) day)",
            "type": "string"
          },
          "installments": {
            "default": "1",
            "type": "string"
          },
          "settle": {
            "default": false,
            "type": "boolean"
          },
          "value": {
            "default": "490.0",
            "type": "string"
          }
        },
        "required": [
          "value",
          "settle",
          "installments",
          "expirationDate"
        ],
        "title": "CreateaPreAuthorizationRequest",
        "type": "object",
        "description": ""
      }
    },
    "required": [
      "creditAccountId",
      "Accept",
      "Content-Type",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://vtex.local/api/creditcontrol/accounts/{creditAccountId}/transaction"
    },
    "headers": {
      "Accept": {
        "$": "Accept"
      },
      "Content-Type": {
        "$": "Content-Type"
      }
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
