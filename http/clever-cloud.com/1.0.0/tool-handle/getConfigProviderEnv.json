{
  "name": "getConfigProviderEnv",
  "description": "Get provider's addon environment",
  "parameters": {
    "type": "object",
    "properties": {
      "configurationProviderId": {
        "type": "string",
        "description": "Automatically added"
      }
    },
    "required": [
      "configurationProviderId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.clever-cloud.com/v2/v4/addon-providers/config-provider/addons/{configurationProviderId}/env"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.item"
            }
          }
        }
      ]
    }
  }
}
