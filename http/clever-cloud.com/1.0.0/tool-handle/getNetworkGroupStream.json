{
  "name": "getNetworkGroupStream",
  "description": "Retrieves the current Network Group details as a Server Sent Event.",
  "parameters": {
    "type": "object",
    "properties": {
      "ownerId": {
        "type": "string",
        "description": "Automatically added"
      },
      "networkGroupId": {
        "type": "string",
        "description": "Automatically added"
      }
    },
    "required": [
      "ownerId",
      "networkGroupId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.clever-cloud.com/v2/v4/networkgroups/organisations/{ownerId}/networkgroups/{networkGroupId}/stream"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
