{
  "name": "getConfigProvider",
  "description": "Get Addon provider configuration",
  "parameters": {
    "type": "object",
    "properties": {
      "configurationProviderId": {
        "type": "string",
        "description": "Automatically added"
      }
    },
    "required": [
      "configurationProviderId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.clever-cloud.com/v2/v4/addon-providers/config-provider/addons/{configurationProviderId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
