{
  "name": "createNetworkGroupMember",
  "description": "Adds a member to a Network Group.",
  "parameters": {
    "type": "object",
    "properties": {
      "ownerId": {
        "type": "string",
        "description": "Automatically added"
      },
      "networkGroupId": {
        "type": "string",
        "description": "Automatically added"
      },
      "body": {
        "$ref": "#/$defs/schema2"
      }
    },
    "required": [
      "ownerId",
      "networkGroupId"
    ],
    "$defs": {
      "schema2": {
        "properties": {
          "email": {
            "type": "string"
          },
          "job": {
            "type": "string"
          },
          "role": {
            "type": "string"
          }
        },
        "required": [
          "email",
          "role"
        ],
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.clever-cloud.com/v2/v4/networkgroups/organisations/{ownerId}/networkgroups/{networkGroupId}/members"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
