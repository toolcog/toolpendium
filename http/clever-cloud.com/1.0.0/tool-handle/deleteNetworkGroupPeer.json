{
  "name": "deleteNetworkGroupPeer",
  "description": "Removes a peer from a Network Group.",
  "parameters": {
    "type": "object",
    "properties": {
      "ownerId": {
        "type": "string",
        "description": "Automatically added"
      },
      "networkGroupId": {
        "type": "string",
        "description": "Automatically added"
      },
      "peerId": {
        "type": "string",
        "description": "Automatically added"
      }
    },
    "required": [
      "ownerId",
      "networkGroupId",
      "peerId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.clever-cloud.com/v2/v4/networkgroups/organisations/{ownerId}/networkgroups/{networkGroupId}/peers/{peerId}"
    }
  },
  "responses": {}
}
