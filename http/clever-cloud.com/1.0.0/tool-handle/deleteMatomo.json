{
  "name": "deleteMatomo",
  "description": "Delete Matomo addon",
  "parameters": {
    "type": "object",
    "properties": {
      "matomoId": {
        "type": "string",
        "description": "Automatically added"
      }
    },
    "required": [
      "matomoId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.clever-cloud.com/v2/v2/providers/addon-matomo/resources/{matomoId}"
    }
  },
  "responses": {
    "default": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
