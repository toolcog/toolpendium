{
  "name": "retrieve_an_inbound_ach_transfer_return",
  "description": "Retrieve an Inbound ACH Transfer Return",
  "parameters": {
    "type": "object",
    "properties": {
      "inbound_ach_transfer_return_id": {
        "description": "The identifier of the Inbound ACH Transfer Return to retrieve.",
        "type": "string"
      }
    },
    "required": [
      "inbound_ach_transfer_return_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.increase.com/inbound_ach_transfer_returns/{inbound_ach_transfer_return_id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Inbound ACH Transfer Return"
        },
        "If unauthorized activity occurs via ACH, you can create an Inbound ACH Transfer Return and we'll reverse the transaction. You can create an Inbound ACH Transfer return the first two days after receiving an Inbound ACH Transfer.",
        "**Key properties:**",
        {
          "$ul": [
            "**id**: The ID of the Inbound ACH Transfer Return.",
            "**inbound_ach_transfer_transaction_id**: The ID for the Transaction that is being returned.",
            "**reason**: The reason why this transfer will be returned. This is sent to the initiating bank.",
            "**status**: The lifecycle status of the transfer.",
            [
              "**submission**: After the return is submitted to FedACH, this will contain supplemental details.",
              {
                "$ul": [
                  "**submitted_at**: When the ACH transfer return was sent to FedACH.",
                  "**trace_number**: The trace number for the submission."
                ]
              }
            ],
            "**transaction_id**: The ID for the transaction refunding the transfer.",
            "**type**: A constant representing the object's type. For this resource it will always be `inbound_ach_transfer_return`."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "default": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
