{
  "name": "simulate_a_tax_document_being_created",
  "description": "Simulates an tax document being created for an account.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/simulate_a_tax_document_being_created_parameters"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "simulate_a_tax_document_being_created_parameters": {
        "example": {
          "account_id": "account_in71c4amph0vgo2qllky"
        },
        "properties": {
          "account_id": {
            "description": "The identifier of the Account the tax document is for.",
            "type": "string"
          }
        },
        "required": [
          "account_id"
        ],
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.increase.com/simulations/documents"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Document"
        },
        "Increase generates certain documents / forms automatically for your application; they can be listed here. Currently the only supported document type is IRS Form 1099-INT.",
        "**Key properties:**",
        {
          "$ul": [
            "**category**: The type of document.",
            "**created_at**: The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) time at which the Document was created.",
            "**entity_id**: The identifier of the Entity the document was generated for.",
            "**file_id**: The identifier of the File containing the Document's contents.",
            "**id**: The Document identifier.",
            "**type**: A constant representing the object's type. For this resource it will always be `document`."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "default": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
