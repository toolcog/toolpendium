{
  "name": "retrieve_an_account_statement",
  "description": "Retrieve an Account Statement",
  "parameters": {
    "type": "object",
    "properties": {
      "account_statement_id": {
        "description": "The identifier of the Account Statement to retrieve.",
        "type": "string"
      }
    },
    "required": [
      "account_statement_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.increase.com/account_statements/{account_statement_id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Account Statement"
        },
        "Account Statements are generated monthly for every active Account. You can access the statement's data via the API or retrieve a PDF with its details via its associated File.",
        "**Key properties:**",
        {
          "$ul": [
            "**account_id**: The identifier for the Account this Account Statement belongs to.",
            "**created_at**: The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) time at which the Account Statement was created.",
            "**ending_balance**: The Account's balance at the start of its statement period.",
            "**file_id**: The identifier of the File containing a PDF of the statement.",
            "**id**: The Account Statement identifier.",
            "**starting_balance**: The Account's balance at the start of its statement period.",
            "**statement_period_end**: The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) time representing the end of the period the Account Statement covers.",
            "**statement_period_start**: The [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) time representing the start of the period the Account Statement covers.",
            "**type**: A constant representing the object's type. For this resource it will always be `account_statement`."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "default": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
