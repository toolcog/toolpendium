{
  "name": "getAnnotationsByRgdIdAndOntologyUsingGET",
  "description": "Returns a list of annotations by RGD ID and ontology prefix",
  "parameters": {
    "type": "object",
    "properties": {
      "rgdId": {
        "format": "int32",
        "type": "integer",
        "description": "RGD ID"
      },
      "ontologyPrefix": {
        "type": "string",
        "description": "Ontology Prefix.  The prefix can be found left of the semicolon in an ontology term accession ID.  As an example, term accession PW:0000034 has the ontology prefix PW"
      }
    },
    "required": [
      "rgdId",
      "ontologyPrefix"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//rest.rgd.mcw.edu/rgdws/annotations/rgdId/{rgdId}/{ontologyPrefix}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Annotation list"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**annotatedObjectRgdId**",
            "**annotationExtension**",
            "**aspect**",
            "**createdBy**",
            "**createdDate**",
            "**dataSrc**",
            "**evidence**",
            "**geneProductFormId**",
            "**key**",
            "**lastModifiedBy**",
            "**lastModifiedDate**",
            "**notes**",
            "**objectName**",
            "**objectSymbol**",
            "**originalCreatedDate**",
            "**qualifier**",
            "**refRgdId**",
            "**relativeTo**",
            "**rgdObjectKey**",
            "**speciesTypeKey**",
            "**term**",
            "**termAcc**",
            "**withInfo**",
            "**xrefSource**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": "Item"
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
