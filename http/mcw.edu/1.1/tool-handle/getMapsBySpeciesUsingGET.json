{
  "name": "getMapsBySpeciesUsingGET",
  "description": "Return a list of assemblies",
  "parameters": {
    "type": "object",
    "properties": {
      "speciesTypeKey": {
        "format": "int32",
        "type": "integer",
        "description": "species Key"
      }
    },
    "required": [
      "speciesTypeKey"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//rest.rgd.mcw.edu/rgdws/maps/{speciesTypeKey}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Map list"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**dbsnpVersion**",
            "**description**",
            "**key**",
            "**methodKey**",
            "**name**",
            "**notes**",
            "**primaryRefAssembly**",
            "**rank**",
            "**refSeqAssemblyAcc**",
            "**refSeqAssemblyName**",
            "**rgdId**",
            "**source**",
            "**speciesTypeKey**",
            "**ucscAssemblyId**",
            "**unit**",
            "**version**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.name"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
