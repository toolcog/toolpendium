{
  "name": "Get_Channels_",
  "description": "Get the list of all the channels TV & iPlayer.",
  "parameters": {
    "type": "object",
    "properties": {
      "region": {
        "type": "string",
        "description": "The region to get the channels for."
      },
      "lang": {
        "enum": [
          "en",
          "cy",
          "ga",
          "gd",
          "pi"
        ],
        "type": "string",
        "description": "The language for any applicable localised strings."
      }
    },
    "required": [
      "lang"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://ibl.api.bbci.co.uk/ibl/v1/channels{?region,lang}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
