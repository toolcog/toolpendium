{
  "name": "delete-asset",
  "description": "Deletes a video asset and all its data.",
  "parameters": {
    "type": "object",
    "properties": {
      "ASSET_ID": {
        "type": "string",
        "description": "The asset ID."
      }
    },
    "required": [
      "ASSET_ID"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.mux.com/video/v1/assets/{ASSET_ID}"
    }
  },
  "responses": {}
}
