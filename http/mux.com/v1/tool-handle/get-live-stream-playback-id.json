{
  "name": "get-live-stream-playback-id",
  "description": "Fetches information about a live stream's playback ID, through which a viewer can watch the streamed content from this live stream.",
  "parameters": {
    "type": "object",
    "properties": {
      "LIVE_STREAM_ID": {
        "type": "string",
        "description": "The live stream ID"
      },
      "PLAYBACK_ID": {
        "type": "string",
        "description": "The live stream's playback ID."
      }
    },
    "required": [
      "LIVE_STREAM_ID",
      "PLAYBACK_ID"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.mux.com/video/v1/live-streams/{LIVE_STREAM_ID}/playback-ids/{PLAYBACK_ID}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**data**",
              {
                "$ul": [
                  "**id**: Unique identifier for the PlaybackID",
                  "**policy**: * `public` playback IDs are accessible by constructing an HLS URL like `https://stream.mux.com/${PLAYBACK_ID}`"
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
