{
  "name": "list-related-incidents",
  "description": "Returns all the incidents that seem related to a specific incident.",
  "parameters": {
    "type": "object",
    "properties": {
      "INCIDENT_ID": {
        "type": "string",
        "description": "ID of the Incident"
      },
      "limit": {
        "default": 25,
        "format": "int32",
        "type": "integer",
        "description": "Number of items to include in the response"
      },
      "page": {
        "default": 1,
        "format": "int32",
        "type": "integer",
        "description": "Offset by this many pages, of the size of `limit`"
      },
      "order_by": {
        "enum": [
          "negative_impact",
          "value",
          "views",
          "field"
        ],
        "type": "string",
        "description": "Value to order the results by"
      },
      "order_direction": {
        "enum": [
          "asc",
          "desc"
        ],
        "type": "string",
        "description": "Sort order."
      }
    },
    "required": [
      "INCIDENT_ID"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.mux.com/data/v1/incidents/{INCIDENT_ID}/related{?limit,page,order_by,order_direction}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**data**",
            "**timeframe**",
            "**total_row_count**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
