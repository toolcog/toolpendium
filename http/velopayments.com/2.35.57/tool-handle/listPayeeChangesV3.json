{
  "name": "listPayeeChangesV3",
  "description": "<p>Use v4 instead</p>\n<p>Get a paginated response listing payee changes.</p>\n",
  "parameters": {
    "type": "object",
    "properties": {
      "payorId": {
        "format": "uuid",
        "type": "string",
        "description": "The Payor ID to find associated Payees"
      },
      "updatedSince": {
        "format": "date-time",
        "type": "string",
        "description": "The updatedSince filter in the format YYYY-MM-DDThh:mm:ss+hh:mm"
      },
      "page": {
        "default": 1,
        "example": 1,
        "format": "int32",
        "type": "integer",
        "description": "Page number. Default is 1."
      },
      "pageSize": {
        "default": 100,
        "example": 100,
        "format": "int32",
        "type": "integer",
        "description": "Page size. Default is 100. Max allowable is 1000."
      }
    },
    "required": [
      "payorId",
      "updatedSince"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.sandbox.velopayments.com//v3/payees/deltas{?payorId,updatedSince,page,pageSize}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "List Payee Changes Response Object",
        "**Key properties:**",
        {
          "$ul": [
            "**content**",
            "**links**",
            [
              "**page**",
              {
                "$ul": [
                  "**numberOfElements**",
                  "**page**",
                  "**pageSize**",
                  "**totalElements**",
                  "**totalPages**"
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
