{
  "name": "removeAsset",
  "description": "Remove an asset",
  "parameters": {
    "type": "object",
    "properties": {
      "asset_id": {
        "format": "uuid",
        "type": "string",
        "description": "UUID of the asset to remove"
      }
    },
    "required": [
      "asset_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://console.rumble.run/api/v1.0/org/assets/{asset_id}"
    }
  },
  "responses": {}
}
