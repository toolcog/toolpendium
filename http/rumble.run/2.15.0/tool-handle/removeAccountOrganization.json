{
  "name": "removeAccountOrganization",
  "description": "Remove this organization",
  "parameters": {
    "type": "object",
    "properties": {
      "org_id": {
        "format": "uuid",
        "type": "string",
        "description": "UUID of the organization to retrieve"
      }
    },
    "required": [
      "org_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://console.rumble.run/api/v1.0/account/orgs/{org_id}"
    }
  },
  "responses": {}
}
