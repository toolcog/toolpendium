{
  "name": "removeService",
  "description": "Remove a service",
  "parameters": {
    "type": "object",
    "properties": {
      "service_id": {
        "format": "uuid",
        "type": "string",
        "description": "UUID of the service to remove"
      }
    },
    "required": [
      "service_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://console.rumble.run/api/v1.0/org/services/{service_id}"
    }
  },
  "responses": {}
}
