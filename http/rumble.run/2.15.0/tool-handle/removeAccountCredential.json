{
  "name": "removeAccountCredential",
  "description": "Remove this credential",
  "parameters": {
    "type": "object",
    "properties": {
      "credential_id": {
        "format": "uuid",
        "type": "string",
        "description": "UUID of the credential to delete"
      }
    },
    "required": [
      "credential_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://console.rumble.run/api/v1.0/account/credentials/{credential_id}"
    }
  },
  "responses": {}
}
