{
  "name": "removeAccountGroupMapping",
  "description": "Remove this SSO group mapping",
  "parameters": {
    "type": "object",
    "properties": {
      "group_mapping_id": {
        "format": "uuid",
        "type": "string",
        "description": "UUID of the SSO group mapping"
      }
    },
    "required": [
      "group_mapping_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://console.rumble.run/api/v1.0/account/sso/groups/{group_mapping_id}"
    }
  },
  "responses": {}
}
