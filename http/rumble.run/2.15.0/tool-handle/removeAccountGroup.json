{
  "name": "removeAccountGroup",
  "description": "Remove this group",
  "parameters": {
    "type": "object",
    "properties": {
      "group_id": {
        "format": "uuid",
        "type": "string",
        "description": "UUID of the group"
      }
    },
    "required": [
      "group_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://console.rumble.run/api/v1.0/account/groups/{group_id}"
    }
  },
  "responses": {}
}
