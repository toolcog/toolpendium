{
  "name": "deleteAccountOrganizationExportToken",
  "description": "Removes the export token from the specified organization",
  "parameters": {
    "type": "object",
    "properties": {
      "org_id": {
        "format": "uuid",
        "type": "string",
        "description": "UUID of the organization to retrieve"
      }
    },
    "required": [
      "org_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://console.rumble.run/api/v1.0/account/orgs/{org_id}/exportToken"
    }
  },
  "responses": {}
}
