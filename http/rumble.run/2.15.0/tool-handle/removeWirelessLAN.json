{
  "name": "removeWirelessLAN",
  "description": "Remove a wireless LAN",
  "parameters": {
    "type": "object",
    "properties": {
      "wireless_id": {
        "format": "uuid",
        "type": "string",
        "description": "UUID of the wireless LAN to remove"
      }
    },
    "required": [
      "wireless_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://console.rumble.run/api/v1.0/org/wireless/{wireless_id}"
    }
  },
  "responses": {}
}
