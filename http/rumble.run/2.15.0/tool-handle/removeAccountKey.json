{
  "name": "removeAccountKey",
  "description": "Remove this key",
  "parameters": {
    "type": "object",
    "properties": {
      "key_id": {
        "format": "uuid",
        "type": "string",
        "description": "UUID of the key to retrieve"
      }
    },
    "required": [
      "key_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://console.rumble.run/api/v1.0/account/keys/{key_id}"
    }
  },
  "responses": {}
}
