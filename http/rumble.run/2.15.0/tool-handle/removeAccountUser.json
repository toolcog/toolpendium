{
  "name": "removeAccountUser",
  "description": "Remove this user",
  "parameters": {
    "type": "object",
    "properties": {
      "user_id": {
        "format": "uuid",
        "type": "string",
        "description": "UUID of the user to delete"
      }
    },
    "required": [
      "user_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://console.rumble.run/api/v1.0/account/users/{user_id}"
    }
  },
  "responses": {}
}
