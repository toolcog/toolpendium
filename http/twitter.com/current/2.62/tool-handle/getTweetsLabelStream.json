{
  "name": "getTweetsLabelStream",
  "description": "Streams 100% of labeling events applied to Tweets",
  "parameters": {
    "type": "object",
    "properties": {
      "backfill_minutes": {
        "format": "int32",
        "maximum": 5,
        "minimum": 0,
        "type": "integer",
        "description": "The number of minutes of backfill requested."
      },
      "start_time": {
        "format": "date-time",
        "type": "string",
        "description": "YYYY-MM-DDTHH:mm:ssZ. The earliest UTC timestamp from which the Tweet labels will be provided."
      },
      "end_time": {
        "format": "date-time",
        "type": "string",
        "description": "YYYY-MM-DDTHH:mm:ssZ. The latest UTC timestamp from which the Tweet labels will be provided."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.twitter.com/2/tweets/label/stream{?backfill_minutes,start_time,end_time}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    },
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**code**",
            "**message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
