{
  "name": "delete-product",
  "description": "This endpoint removes a Product identified by the `productId` parameter.",
  "parameters": {
    "type": "object",
    "properties": {
      "productId": {
        "format": "uuid",
        "type": "string",
        "description": "The identifier of the Product."
      }
    },
    "required": [
      "productId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.configcat.com/v1/products/{productId}"
    }
  },
  "responses": {}
}
