{
  "name": "delete-product-member",
  "description": "This endpoint removes a Member identified by the `userId` from the \ngiven Product identified by the `productId` parameter.",
  "parameters": {
    "type": "object",
    "properties": {
      "productId": {
        "format": "uuid",
        "type": "string",
        "description": "The identifier of the Product."
      },
      "userId": {
        "type": "string",
        "description": "The identifier of the Member."
      }
    },
    "required": [
      "productId",
      "userId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.configcat.com/v1/products/{productId}/members/{userId}"
    }
  },
  "responses": {}
}
