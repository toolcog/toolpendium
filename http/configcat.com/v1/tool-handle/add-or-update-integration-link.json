{
  "name": "add-or-update-integration-link",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "environmentId": {
        "format": "uuid",
        "type": "string",
        "description": "The identifier of the Environment."
      },
      "settingId": {
        "format": "int32",
        "type": "integer",
        "description": "The id of the Setting."
      },
      "integrationLinkType": {
        "$ref": "#/components/schemas/IntegrationLinkType",
        "description": "The integration link's type."
      },
      "key": {
        "type": "string",
        "description": "The key of the integration link."
      },
      "body": {
        "$ref": "#/$defs/AddOrUpdateIntegrationLinkModel"
      }
    },
    "required": [
      "environmentId",
      "settingId",
      "integrationLinkType",
      "key"
    ],
    "$defs": {
      "AddOrUpdateIntegrationLinkModel": {
        "additionalProperties": false,
        "properties": {
          "description": {
            "maxLength": 1000,
            "nullable": true,
            "type": "string"
          },
          "url": {
            "maxLength": 1000,
            "nullable": true,
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.configcat.com/v1/environments/{environmentId}/settings/{settingId}/integrationLinks/{integrationLinkType}/{key}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**description**",
            "**integrationLinkType**",
            "**key**",
            "**url**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
