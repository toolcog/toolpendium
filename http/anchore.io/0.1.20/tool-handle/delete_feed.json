{
  "name": "delete_feed",
  "description": "Delete the groups and data for the feed and disable the feed itself",
  "parameters": {
    "type": "object",
    "properties": {
      "feed": {
        "type": "string"
      }
    },
    "required": [
      "feed"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//system/feeds/{feed}"
    }
  },
  "responses": {}
}
