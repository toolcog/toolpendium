{
  "name": "delete_feed_group",
  "description": "Delete the group data and disable the group itself",
  "parameters": {
    "type": "object",
    "properties": {
      "feed": {
        "type": "string"
      },
      "group": {
        "type": "string"
      }
    },
    "required": [
      "feed",
      "group"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//system/feeds/{feed}/{group}"
    }
  },
  "responses": {}
}
