{
  "name": "health_check",
  "description": "Health check, returns 200 and no body if service is running",
  "parameters": {
    "type": "object"
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//health"
    }
  },
  "responses": {}
}
