{
  "name": "list_import_dockerfiles",
  "description": "List uploaded dockerfiles",
  "parameters": {
    "type": "object",
    "properties": {
      "operation_id": {
        "type": "string"
      }
    },
    "required": [
      "operation_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//imports/images/{operation_id}/dockerfile"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "String digest of an uploaded content",
        {
          "$each": "$.body.*",
          "$as": "item",
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.item"
            }
          }
        }
      ]
    }
  }
}
