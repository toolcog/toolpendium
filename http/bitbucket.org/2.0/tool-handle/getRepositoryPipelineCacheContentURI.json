{
  "name": "getRepositoryPipelineCacheContentURI",
  "description": "Retrieve the URI of the content of the specified cache.",
  "parameters": {
    "type": "object",
    "properties": {
      "workspace": {
        "type": "string",
        "description": "The account."
      },
      "repo_slug": {
        "type": "string",
        "description": "The repository."
      },
      "cache_uuid": {
        "type": "string",
        "description": "The UUID of the cache."
      }
    },
    "required": [
      "workspace",
      "repo_slug",
      "cache_uuid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.bitbucket.org/2.0/repositories/{workspace}/{repo_slug}/pipelines-config/caches/{cache_uuid}/content-uri"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Pipeline Cache Content URI"
        },
        "A representation of the location of pipeline cache content.",
        "**Key properties:**",
        {
          "$ul": [
            "**uri**: The uri for pipeline cache content."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "404": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Error"
        },
        "Base type for most resource objects. It defines the common `type` element that identifies an object's type. It also identifies the element as Swagger's `discriminator`.",
        "**Key properties:**",
        {
          "$ul": [
            [
              "**error**",
              {
                "$ul": [
                  "**data**: Optional structured data that is endpoint-specific.",
                  "**detail**",
                  "**message**"
                ]
              }
            ],
            "**type**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
