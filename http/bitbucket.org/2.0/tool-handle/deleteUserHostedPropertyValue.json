{
  "name": "deleteUserHostedPropertyValue",
  "description": "Delete an [application property](/cloud/bitbucket/application-properties/) value stored against a user.",
  "parameters": {
    "type": "object",
    "properties": {
      "selected_user": {
        "type": "string",
        "description": "Either the UUID of the account surrounded by curly-braces, for example `{account UUID}`, OR an Atlassian Account ID."
      },
      "app_key": {
        "type": "string",
        "description": "The key of the Connect app."
      },
      "property_name": {
        "type": "string",
        "description": "The name of the property."
      }
    },
    "required": [
      "selected_user",
      "app_key",
      "property_name"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.bitbucket.org/2.0/users/{selected_user}/properties/{app_key}/{property_name}"
    }
  },
  "responses": {}
}
