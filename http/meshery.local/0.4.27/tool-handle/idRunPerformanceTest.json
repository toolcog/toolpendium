{
  "name": "idRunPerformanceTest",
  "description": "Runs the load test with the given parameters",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Automatically added"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://meshery.local/api/user/performance/profiles/{id}/run"
    }
  },
  "responses": {}
}
