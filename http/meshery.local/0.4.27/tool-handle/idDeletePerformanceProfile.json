{
  "name": "idDeletePerformanceProfile",
  "description": "Deletes a performance profile with the given id",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "uuid",
        "type": "string",
        "description": "id for a specific"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "http://meshery.local/api/user/performance/profiles/{id}"
    }
  },
  "responses": {}
}
