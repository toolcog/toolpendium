{
  "name": "idGetMesheryPattern",
  "description": "Fetches the pattern with the given id",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "uuid",
        "type": "string",
        "description": "id for a specific"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://meshery.local/api/pattern/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "MesheryPattern represents the patterns that needs to be saved",
        "**Key properties:**",
        {
          "$ul": [
            "**created_at**",
            "**id**",
            "**location**: It implements native SQL driver interfaces and hence can",
            "**name**",
            "**pattern_file**",
            "**updated_at**",
            "**user_id**: Meshery doesn't have the user id fields"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
