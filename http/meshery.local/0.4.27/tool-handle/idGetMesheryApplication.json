{
  "name": "idGetMesheryApplication",
  "description": "Fetches the list of all applications saved by the current user",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "uuid",
        "type": "string",
        "description": "id for a specific"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://meshery.local/api/application/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "MesheryApplication represents the applications that needs to be saved",
        "**Key properties:**",
        {
          "$ul": [
            "**application_file**",
            "**created_at**",
            "**id**",
            "**location**: It implements native SQL driver interfaces and hence can",
            "**name**",
            "**updated_at**",
            "**user_id**: Meshery doesn't have the user id fields"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
