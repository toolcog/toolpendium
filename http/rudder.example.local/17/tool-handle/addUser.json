{
  "name": "addUser",
  "description": "Add a new user",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/users"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "users": {
        "properties": {
          "isPreHahed": {
            "description": "If you want to provide hashed password set this property to `true` otherwise we will hash the plain password and store the hash",
            "enum": [
              false,
              true
            ],
            "type": "boolean"
          },
          "password": {
            "description": "this password will be hashed for you if the `isPreHashed` is set on false",
            "example": "passwdWillBeStoredHashed",
            "type": "string"
          },
          "role": {
            "description": "Defined user's permissions",
            "items": {
              "example": "user",
              "type": "string"
            },
            "type": "array"
          },
          "username": {
            "example": "John Doe",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://rudder.example.local/rudder/api/latest/usermanagement"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**action**: The id of the action",
            [
              "**data**",
              {
                "$ul": [
                  [
                    "**addedUser**",
                    {
                      "$ul": [
                        "**password**: this password will be hashed for you if the `isPreHashed` is set on false",
                        "**role**: defined user's permissions",
                        "**username**"
                      ]
                    }
                  ]
                ]
              }
            ],
            "**result**: Result of the request"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
