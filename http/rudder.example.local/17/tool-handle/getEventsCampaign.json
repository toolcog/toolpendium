{
  "name": "getEventsCampaign",
  "description": "Get campaign events for a campaign",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "example": "0076a379-f32d-4732-9e91-33ab219d8fde",
        "format": "uuid",
        "type": "string",
        "description": "Id of the campaign"
      },
      "campaignType": {
        "enum": [
          "system-update",
          "software-update"
        ],
        "example": "system-update",
        "type": "string",
        "description": "Type of the campaigns we want"
      },
      "state": {
        "enum": [
          "scheduled",
          "running",
          "finished",
          "skipped"
        ],
        "example": "enabled",
        "type": "string",
        "description": "Status of the campaign events we want"
      },
      "limit": {
        "type": "integer",
        "description": "Max number of elements in response"
      },
      "offset": {
        "type": "integer",
        "description": "Offset of data in response (skip X elements)"
      },
      "before": {
        "format": "date",
        "type": "string"
      },
      "after": {
        "format": "date",
        "type": "string"
      },
      "order": {
        "type": "string"
      },
      "asc": {
        "enum": [
          "asc",
          "desc"
        ],
        "type": "string"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://rudder.example.local/rudder/api/latest/campaigns/{id}/events{?campaignType,state,limit,offset,before,after,order,asc}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**action**: The id of the action",
            [
              "**data**",
              {
                "$ul": [
                  "**campaignEvents**"
                ]
              }
            ],
            "**result**: Result of the request"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
