{
  "name": "ReloadOneDatasourceAllNodes",
  "description": "Update properties from all data source on all nodes. The call is asynchronous.",
  "parameters": {
    "type": "object",
    "properties": {
      "datasourceId": {
        "example": "test-data-source",
        "type": "string",
        "description": "Id of the data source"
      }
    },
    "required": [
      "datasourceId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://rudder.example.local/rudder/api/latest/datasources/reload/{datasourceId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**action**: The id of the action",
            "**data**",
            "**result**: Result of the request"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
