{
  "name": "getCampaignHistory",
  "description": "Get a campaign result history",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "example": "0076a379-f32d-4732-9e91-33ab219d8fde",
        "format": "uuid",
        "type": "string",
        "description": "Id of the campaign"
      },
      "limit": {
        "type": "integer",
        "description": "Max number of elements in response"
      },
      "offset": {
        "type": "integer",
        "description": "Offset of data in response (skip X elements)"
      },
      "before": {
        "format": "date",
        "type": "string"
      },
      "after": {
        "format": "date",
        "type": "string"
      },
      "order": {
        "type": "string"
      },
      "asc": {
        "enum": [
          "asc",
          "desc"
        ],
        "type": "string"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://rudder.example.local/rudder/api/latest/systemUpdate/campaigns/{id}/history{?limit,offset,before,after,order,asc}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**action**: The id of the action",
            [
              "**data**",
              {
                "$ul": [
                  "**campaignHistory**"
                ]
              }
            ],
            "**result**: Result of the request"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
