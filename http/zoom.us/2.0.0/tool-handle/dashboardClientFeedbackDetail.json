{
  "name": "dashboardClientFeedbackDetail",
  "description": "Retrieve detailed information on a [Zoom meetings client feedback](https://support.zoom.us/hc/en-us/articles/115005855266-End-of-Meeting-Feedback-Survey#h_e30d552b-6d8e-4e0a-a588-9ca8180c4dbf). <br> You can specify a monthly date range for the dashboard data using the `from` and `to` query parameters. The month should fall within the last six months.\n\n**Prerequisites:**\n* Business or higher account\n* [Feedback to Zoom](https://support.zoom.us/hc/en-us/articles/115005838023) enabled.\n\n**Scope:** `dashboard_home:read:admin`<br>\n **[Rate Limit Label](https://marketplace.zoom.us/docs/api-reference/rate-limits#rate-limits):** `Heavy`\n\n`",
  "parameters": {
    "type": "object",
    "properties": {
      "feedbackId": {
        "type": "string",
        "description": "Feedback Detail Id"
      },
      "from": {
        "format": "date",
        "type": "string"
      },
      "to": {
        "format": "date",
        "type": "string"
      },
      "page_size": {
        "default": 30,
        "type": "integer"
      },
      "next_page_token": {
        "type": "string"
      }
    },
    "required": [
      "feedbackId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.zoom.us/v2/metrics/client/feedback/{feedbackId}{?from,to,page_size,next_page_token}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Pagination object.",
        "**Key properties:**",
        {
          "$ul": [
            "**from**: Start date for this report",
            "**to**: End date for this report",
            "**next_page_token**: The next page token is used to paginate through large result sets. A next page token will be returned whenever the set of the available result list exceeds the page size. The expiration period is 15 minutes.",
            "**page_size**: The amount of records returns within a single API call.  (default: 30)",
            "**client_feedback_details**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
