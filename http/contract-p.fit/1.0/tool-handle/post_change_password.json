{
  "name": "post_change_password",
  "description": "Update the current authenticated user password",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/ChangePasswordModelPost"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "ChangePasswordModelPost": {
        "properties": {
          "new_password": {
            "description": "the new user's password",
            "type": "string"
          },
          "old_password": {
            "description": "the old user's password",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//cfportal.contract-p.fit/api/users/me/change_password"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
