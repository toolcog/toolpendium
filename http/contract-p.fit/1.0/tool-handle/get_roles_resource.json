{
  "name": "get_roles_resource",
  "description": "Return the list of roles",
  "parameters": {
    "type": "object",
    "properties": {
      "X-Fields": {
        "format": "mask",
        "type": "string",
        "description": "An optional fields mask"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//cfportal.contract-p.fit/api/roles"
    },
    "headers": {
      "X-Fields": {
        "$": "X-Fields"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**name**",
            [
              "**permissions**",
              {
                "$ul": [
                  "**create_dropbox_user** (default: false)",
                  "**create_inbox** (default: false)",
                  "**create_webhook** (default: false)",
                  "**de_escalate_document** (default: false)",
                  "**delete_tenant** (default: false)",
                  "**delete_webhook** (default: false)",
                  "**edit_backend_settings** (default: false)",
                  "**edit_beats** (default: false)",
                  "**edit_dashboard_settings** (default: false)",
                  "**edit_data_retention_settings** (default: false)",
                  "**edit_flow_settings** (default: false)",
                  "**edit_format_settings** (default: false)",
                  "**edit_integration_settings** (default: false)",
                  "**edit_integrations** (default: false)",
                  "**edit_predictor_settings** (default: false)",
                  "**edit_reports** (default: false)",
                  "**edit_retention_settings** (default: false)",
                  "**edit_review_settings** (default: false)",
                  "**edit_roles** (default: false)",
                  "**edit_sampling_settings** (default: false)",
                  "**edit_security_settings** (default: false)",
                  "**edit_templates** (default: false)",
                  "**edit_thresholds_settings** (default: false)",
                  "**edit_translations** (default: false)",
                  "**edit_users** (default: false)",
                  "**escalate_document** (default: false)",
                  "**never_twice** (default: false)",
                  "**pick_next_escalated** (default: false)",
                  "**read_beats** (default: false)",
                  "**read_data_retention_settings** (default: false)",
                  "**read_feedback** (default: false)",
                  "**read_integrations** (default: false)",
                  "**read_release_notes** (default: false)",
                  "**read_reports** (default: false)",
                  "**read_thresholds_settings** (default: false)",
                  "**read_webhook** (default: false)",
                  "**reject_document** (default: false)",
                  "**release_lock** (default: false)",
                  "**review** (default: false)",
                  "**submit** (default: false)",
                  "**update_webhook** (default: false)",
                  "**upload** (default: false)",
                  "**versions** (default: false)",
                  "**view_api_keys** (default: false)",
                  "**view_list** (default: false)",
                  "**view_projects** (default: false)",
                  "**view_statistics** (default: false)",
                  "**view_templates** (default: false)",
                  "**write_feedback** (default: false)",
                  "**write_release_notes** (default: false)"
                ]
              }
            ],
            "**id**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.name"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
