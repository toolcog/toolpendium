{
  "name": "post_debug_where_to_search",
  "description": "Returns a list of strings per granularity. If no granularity is specified. It is considered to be the whole document\n\n**Permission required:** edit_predictor_settings",
  "parameters": {
    "type": "object",
    "properties": {
      "document_id": {
        "type": "string"
      },
      "body": {
        "$ref": "#/$defs/dictionary"
      }
    },
    "required": [
      "document_id",
      "body"
    ],
    "$defs": {
      "dictionary": {
        "properties": {},
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//cfportal.contract-p.fit/api/rule_config/where_to_search/{document_id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
