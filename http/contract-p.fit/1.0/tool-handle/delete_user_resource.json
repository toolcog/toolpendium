{
  "name": "delete_user_resource",
  "description": "Delete the given user.\n\n**WARNING** This will completely remove the user, this is not recoverable",
  "parameters": {
    "type": "object",
    "properties": {
      "user_id": {
        "type": "string"
      }
    },
    "required": [
      "user_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//cfportal.contract-p.fit/api/users/{user_id}"
    }
  },
  "responses": {}
}
