{
  "name": "delete_api_key_resource",
  "description": "Completely remove an API Key, thus disabling it. The token won't be recoverable\n\n**Permission required:** edit_backend_settings",
  "parameters": {
    "type": "object",
    "properties": {
      "key": {
        "type": "string"
      }
    },
    "required": [
      "key"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//cfportal.contract-p.fit/api/auth/api-key/{key}"
    }
  },
  "responses": {}
}
