{
  "name": "get_document_last_version_resource",
  "description": "Get the latest possible version for this document, optionally with constraints on the source or name\n\n**Permission required:** review",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Return a specific named version, default is latest"
      },
      "source": {
        "type": "string",
        "description": "human or machine (default either)"
      },
      "is_evaluated": {
        "default": false,
        "type": "boolean",
        "description": "Do you want to receive unevaluated version (for display, default) or evaluated (for stats)"
      },
      "document_id": {
        "type": "string"
      }
    },
    "required": [
      "document_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//cfportal.contract-p.fit/api/documents/{document_id}/last_version{?name,source,is_evaluated}"
    }
  },
  "responses": {}
}
