{
  "name": "patch_document_move_resource",
  "description": "Move documents in the provided list to another inbox.\n\nIf the destination inbox is the same as the original inbox, the request will be aborted.\n\n\n**Permission required:** review",
  "parameters": {
    "type": "object",
    "properties": {
      "inbox_id": {
        "type": "string",
        "description": "The uuid of the inbox where the document(s) have to be moved"
      },
      "body": {
        "$ref": "#/$defs/DocumentIds"
      }
    },
    "required": [
      "inbox_id",
      "body"
    ],
    "$defs": {
      "DocumentIds": {
        "properties": {
          "document_ids": {
            "items": {
              "example": "abcdef123456789abcdef123",
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "PATCH",
    "url": {
      "$uri": "//cfportal.contract-p.fit/api/documents/{inbox_id}/move_inbox"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
