{
  "name": "post_reset_password",
  "description": "Reset the user password",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/ResetPasswordModelPost"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "ResetPasswordModelPost": {
        "properties": {
          "new_password": {
            "description": "the new user's password",
            "type": "string"
          },
          "token": {
            "description": "the token to reset the password",
            "type": "string"
          },
          "user_name": {
            "description": "the username",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//cfportal.contract-p.fit/api/auth/reset_password"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
