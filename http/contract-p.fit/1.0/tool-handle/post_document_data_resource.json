{
  "name": "post_document_data_resource",
  "parameters": {
    "type": "object",
    "properties": {
      "data_type": {
        "default": "document",
        "type": "string",
        "description": "Define the data that you want to get out. Possible options: ['document', 'reverse', 'text']"
      },
      "body": {
        "$ref": "#/$defs/DocumentIds"
      }
    },
    "required": [
      "data_type",
      "body"
    ],
    "$defs": {
      "DocumentIds": {
        "properties": {
          "document_ids": {
            "items": {
              "example": "abcdef123456789abcdef123",
              "type": "string"
            },
            "type": "array"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//cfportal.contract-p.fit/api/documents/file_query/{data_type}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
