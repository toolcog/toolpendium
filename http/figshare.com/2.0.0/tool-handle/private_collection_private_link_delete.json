{
  "name": "private_collection_private_link_delete",
  "description": "Disable/delete private link for this collection",
  "parameters": {
    "type": "object",
    "properties": {
      "collection_id": {
        "format": "int64",
        "minimum": 1,
        "type": "integer",
        "description": "Collection unique identifier"
      },
      "link_id": {
        "type": "string",
        "description": "Private link token"
      }
    },
    "required": [
      "collection_id",
      "link_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.figshare.com/v2/account/collections/{collection_id}/private_links/{link_id}"
    }
  },
  "responses": {
    "403": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**code**: A machine friendly error code, used by the dev team to identify the error.",
            "**message**: A human friendly message explaining the error."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
