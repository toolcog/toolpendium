{
  "name": "private_institution_accounts_update",
  "description": "Update Institution Account",
  "parameters": {
    "type": "object",
    "properties": {
      "account_id": {
        "format": "int64",
        "minimum": 1,
        "type": "integer",
        "description": "Account identifier the user is associated to"
      },
      "body": {
        "$ref": "#/components/schemas/AccountUpdate",
        "description": "Account description"
      }
    },
    "required": [
      "account_id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://api.figshare.com/v2/account/institution/accounts/{account_id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "403": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**code**: A machine friendly error code, used by the dev team to identify the error.",
            "**message**: A human friendly message explaining the error."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
