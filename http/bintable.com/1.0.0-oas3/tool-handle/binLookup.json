{
  "name": "binLookup",
  "description": "By passing in the appropriate BIN, you can lookup for\ncard meta data in bintable.com API\n",
  "parameters": {
    "type": "object",
    "properties": {
      "bin": {
        "type": "string",
        "description": "pass the required BIN code"
      },
      "api_key": {
        "type": "string",
        "description": "The API key, which you can get from bintable.com website."
      }
    },
    "required": [
      "bin",
      "api_key"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.bintable.com/v1/{bin}{?api_key}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**data**",
            "**message**",
            "**result**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": "Item"
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
