{
  "name": "updateDomain",
  "description": "Update information about a Domain in Linode's DNS Manager.\n",
  "parameters": {
    "type": "object",
    "properties": {
      "domainId": {
        "type": "integer",
        "description": "The ID of the Domain to access."
      },
      "body": {
        "$ref": "#/components/schemas/Domain",
        "description": "The Domain information to update."
      }
    },
    "required": [
      "domainId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://api.linode.com/v4/domains/{domainId}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "A domain zonefile in our DNS system.  You must own the domain name and tell your registrar to use Linode's nameservers in order for a domain in our system to be treated as authoritative.\n",
        "**Key properties:**",
        {
          "$ul": [
            "**axfr_ips**: The list of IPs that may perform a zone transfer for this Domain. The total combined length of all data within this array cannot exceed 1000 characters.",
            "**description**: A description for this Domain. This is for display purposes only.",
            "**domain**: The domain this Domain represents. Domain labels cannot be longer than 63 characters and must conform to [RFC1035](https://tools.ietf.org/html/rfc1035). Domains must be unique on Linode's platform, including across different Linode accounts; there cannot be two Domains representing the same domain.",
            "**expire_sec**: The amount of time in seconds that may pass before this Domain is no longer (default: 0)",
            "**group**: The group this Domain belongs to.  This is for display purposes only.",
            "**id**: This Domain's unique ID",
            "**master_ips**: The IP addresses representing the master DNS for this Domain. At least one value is required for `type` slave Domains. The total combined length of all data within this array cannot exceed 1000 characters.",
            "**refresh_sec**: The amount of time in seconds before this Domain should be refreshed. (default: 0)",
            "**retry_sec**: The interval, in seconds, at which a failed refresh should be retried. (default: 0)",
            "**soa_email**: Start of Authority email address. This is required for `type` master Domains.",
            "**status**: Used to control whether this Domain is currently being rendered. (default: active)",
            "**tags**: An array of tags applied to this object.  Tags are for organizational purposes only.",
            "**ttl_sec**: \"Time to Live\" - the amount of time in seconds that this Domain's records may be cached by resolvers or other domain servers. (default: 0)",
            "**type**: Whether this Domain represents the authoritative source of information for the domain it describes (\"master\"), or whether it is a read-only copy of a master (\"slave\")."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**errors**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
