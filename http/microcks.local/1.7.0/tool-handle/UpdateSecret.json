{
  "name": "UpdateSecret",
  "description": "Update a Secret",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Unique identifier of Secret to manage"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "http://microcks.local/secrets/{id}"
    }
  },
  "responses": {}
}
