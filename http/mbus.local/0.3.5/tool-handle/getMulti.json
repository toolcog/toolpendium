{
  "name": "getMulti",
  "description": "Gets data from the slave identified by {address}, and supports multiple responses from the slave",
  "parameters": {
    "type": "object",
    "properties": {
      "device": {
        "$ref": "#/components/schemas/device",
        "description": "The serial device to scan - /dev/ is pre-pended to {device} by M-Bus HTTPD before scanning"
      },
      "baudrate": {
        "$ref": "#/components/schemas/baudrate",
        "description": "Baudrate to communicate with M-Bus devices"
      },
      "address": {
        "$ref": "#/components/schemas/address",
        "description": "The slave device to get data from"
      },
      "maxframes": {
        "$ref": "#/components/schemas/maxframes",
        "description": "The slave device to get data from"
      }
    },
    "required": [
      "device",
      "baudrate",
      "address",
      "maxframes"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://mbus.local/mbus/getMulti/{device}/{baudrate}/{address}/{maxframes}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    },
    "400": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    },
    "404": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
