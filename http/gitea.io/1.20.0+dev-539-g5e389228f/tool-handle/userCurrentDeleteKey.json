{
  "name": "userCurrentDeleteKey",
  "description": "Delete a public key",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int64",
        "type": "integer",
        "description": "id of key to delete"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/user/keys/{id}"
    }
  },
  "responses": {}
}
