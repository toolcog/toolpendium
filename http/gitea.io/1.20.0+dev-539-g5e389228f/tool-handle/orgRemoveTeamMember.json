{
  "name": "orgRemoveTeamMember",
  "description": "Remove a team member",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int64",
        "type": "integer",
        "description": "id of the team"
      },
      "username": {
        "type": "string",
        "description": "username of the user to remove"
      }
    },
    "required": [
      "id",
      "username"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/teams/{id}/members/{username}"
    }
  },
  "responses": {}
}
