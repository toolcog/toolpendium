{
  "name": "issueDeleteTime",
  "description": "Delete specific tracked time",
  "parameters": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "description": "owner of the repo"
      },
      "repo": {
        "type": "string",
        "description": "name of the repo"
      },
      "index": {
        "format": "int64",
        "type": "integer",
        "description": "index of the issue"
      },
      "id": {
        "format": "int64",
        "type": "integer",
        "description": "id of time to delete"
      }
    },
    "required": [
      "owner",
      "repo",
      "index",
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/repos/{owner}/{repo}/issues/{index}/times/{id}"
    }
  },
  "responses": {}
}
