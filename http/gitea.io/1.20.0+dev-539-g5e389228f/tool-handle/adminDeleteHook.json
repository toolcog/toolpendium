{
  "name": "adminDeleteHook",
  "description": "Delete a hook",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int64",
        "type": "integer",
        "description": "id of the hook to delete"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/amdin/hooks/{id}"
    }
  },
  "responses": {}
}
