{
  "name": "adminDeleteUserPublicKey",
  "description": "Delete a user's public key",
  "parameters": {
    "type": "object",
    "properties": {
      "username": {
        "type": "string",
        "description": "username of user"
      },
      "id": {
        "format": "int64",
        "type": "integer",
        "description": "id of the key to delete"
      }
    },
    "required": [
      "username",
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/admin/users/{username}/keys/{id}"
    }
  },
  "responses": {}
}
