{
  "name": "orgDeleteTeam",
  "description": "Delete a team",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int64",
        "type": "integer",
        "description": "id of the team to delete"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/teams/{id}"
    }
  },
  "responses": {}
}
