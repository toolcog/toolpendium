{
  "name": "userDeleteOAuth2Application",
  "description": "delete an OAuth2 Application",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int64",
        "type": "integer",
        "description": "token to be deleted"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/user/applications/oauth2/{id}"
    }
  },
  "responses": {}
}
