{
  "name": "adminCronRun",
  "description": "Run cron task",
  "parameters": {
    "type": "object",
    "properties": {
      "task": {
        "type": "string",
        "description": "task to run"
      }
    },
    "required": [
      "task"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "/api/v1/admin/cron/{task}"
    }
  },
  "responses": {}
}
