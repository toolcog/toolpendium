{
  "name": "issueRemoveLabel",
  "description": "Remove a label from an issue",
  "parameters": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "description": "owner of the repo"
      },
      "repo": {
        "type": "string",
        "description": "name of the repo"
      },
      "index": {
        "format": "int64",
        "type": "integer",
        "description": "index of the issue"
      },
      "id": {
        "format": "int64",
        "type": "integer",
        "description": "id of the label to remove"
      }
    },
    "required": [
      "owner",
      "repo",
      "index",
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/repos/{owner}/{repo}/issues/{index}/labels/{id}"
    }
  },
  "responses": {}
}
