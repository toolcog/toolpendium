{
  "name": "repoDeleteTeam",
  "description": "Delete a team from a repository",
  "parameters": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "description": "owner of the repo"
      },
      "repo": {
        "type": "string",
        "description": "name of the repo"
      },
      "team": {
        "type": "string",
        "description": "team name"
      }
    },
    "required": [
      "owner",
      "repo",
      "team"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/repos/{owner}/{repo}/teams/{team}"
    }
  },
  "responses": {}
}
