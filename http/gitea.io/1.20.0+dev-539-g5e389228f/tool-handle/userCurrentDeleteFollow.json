{
  "name": "userCurrentDeleteFollow",
  "description": "Unfollow a user",
  "parameters": {
    "type": "object",
    "properties": {
      "username": {
        "type": "string",
        "description": "username of user to unfollow"
      }
    },
    "required": [
      "username"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/user/following/{username}"
    }
  },
  "responses": {}
}
