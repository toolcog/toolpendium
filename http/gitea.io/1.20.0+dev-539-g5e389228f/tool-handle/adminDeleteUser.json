{
  "name": "adminDeleteUser",
  "description": "Delete a user",
  "parameters": {
    "type": "object",
    "properties": {
      "username": {
        "type": "string",
        "description": "username of user to delete"
      },
      "purge": {
        "type": "boolean",
        "description": "purge the user from the system completely"
      }
    },
    "required": [
      "username"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/admin/users/{username}{?purge}"
    }
  },
  "responses": {}
}
