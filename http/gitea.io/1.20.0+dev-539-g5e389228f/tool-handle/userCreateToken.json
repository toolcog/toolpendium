{
  "name": "userCreateToken",
  "description": "Create an access token",
  "parameters": {
    "type": "object",
    "properties": {
      "username": {
        "type": "string",
        "description": "username of user"
      },
      "body": {
        "$ref": "#/$defs/CreateAccessTokenOption"
      }
    },
    "required": [
      "username"
    ],
    "$defs": {
      "CreateAccessTokenOption": {
        "description": "CreateAccessTokenOption options when create access token",
        "properties": {
          "name": {
            "type": "string",
            "x-go-name": "Name"
          },
          "scopes": {
            "items": {
              "type": "string"
            },
            "type": "array",
            "x-go-name": "Scopes"
          }
        },
        "required": [
          "name"
        ],
        "type": "object",
        "x-go-package": "code.gitea.io/gitea/modules/structs"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "/api/v1/users/{username}/tokens"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "201": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "AccessToken represents an API access token."
        },
        "**Key properties:**",
        {
          "$ul": [
            "**id**",
            "**name**",
            "**scopes**",
            "**sha1**",
            "**token_last_eight**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
