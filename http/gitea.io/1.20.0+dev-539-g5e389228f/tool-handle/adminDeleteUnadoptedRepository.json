{
  "name": "adminDeleteUnadoptedRepository",
  "description": "Delete unadopted files",
  "parameters": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "description": "owner of the repo"
      },
      "repo": {
        "type": "string",
        "description": "name of the repo"
      }
    },
    "required": [
      "owner",
      "repo"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/admin/unadopted/{owner}/{repo}"
    }
  },
  "responses": {}
}
