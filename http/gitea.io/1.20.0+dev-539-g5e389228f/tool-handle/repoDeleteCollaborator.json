{
  "name": "repoDeleteCollaborator",
  "description": "Delete a collaborator from a repository",
  "parameters": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string",
        "description": "owner of the repo"
      },
      "repo": {
        "type": "string",
        "description": "name of the repo"
      },
      "collaborator": {
        "type": "string",
        "description": "username of the collaborator to delete"
      }
    },
    "required": [
      "owner",
      "repo",
      "collaborator"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "/api/v1/repos/{owner}/{repo}/collaborators/{collaborator}"
    }
  },
  "responses": {}
}
