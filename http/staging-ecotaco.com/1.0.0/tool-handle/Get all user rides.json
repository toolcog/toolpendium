{
  "name": "Get_all_user_rides",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "page": {
        "type": "number",
        "description": "Number of the page to load"
      },
      "per_page": {
        "type": "number",
        "description": "Number of items per page to load"
      },
      "Accept": {
        "type": "string",
        "description": "e.g. application/json"
      },
      "Accept-Language": {
        "type": "string",
        "description": "e.g. en"
      }
    },
    "required": [
      "page",
      "per_page"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://api.staging-ecotaco.com/rides?page={page}&per_page={per_page}"
    },
    "headers": {
      "Accept": {
        "$": "Accept"
      },
      "Accept-Language": {
        "$": "Accept-Language"
      }
    }
  },
  "responses": {}
}
