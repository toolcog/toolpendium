{
  "name": "app_subscription_remove_source",
  "description": "Remove creditcard or sepadebit",
  "parameters": {
    "type": "object",
    "properties": {
      "appId": {
        "type": "integer",
        "description": "The identifier of the Sinao application you are using. Each application corresponds to a company and an accounting. A user can have several Sinao applications. To retrieve the identifiers of its applications, you must call the endpoint `/apps`."
      },
      "stripe_source": {
        "type": "string",
        "description": "Stripe source to pay"
      }
    },
    "required": [
      "appId",
      "stripe_source"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.sinao.app/v1/apps/{appId}/subscription/source{?stripe_source}"
    }
  },
  "responses": {}
}
