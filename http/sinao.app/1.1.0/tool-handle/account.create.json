{
  "name": "account_create",
  "description": "Create a new `User` from the registration form. The user will be created without an application or access, there is nothing he can do without another step such as creating an application or inviting access",
  "parameters": {
    "type": "object",
    "properties": {
      "email": {
        "format": "email",
        "type": "string",
        "description": "Email is the user's login, it must not have been registered"
      },
      "password": {
        "minimum": 8,
        "type": "string"
      },
      "cgu": {
        "type": "boolean",
        "description": "The user must have validated the T&Cs"
      },
      "firstname": {
        "minimum": 2,
        "type": "string"
      },
      "lastname": {
        "minimum": 2,
        "type": "string"
      },
      "metadata": {
        "items": {},
        "nullable": true,
        "type": "array"
      }
    },
    "required": [
      "email",
      "password",
      "cgu"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.sinao.app/v1/register{?email,password,cgu,firstname,lastname,metadata}"
    }
  },
  "responses": {}
}
