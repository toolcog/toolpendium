{
  "name": "app_openai_billings_estimate",
  "description": "Get the import state of Stripe data",
  "parameters": {
    "type": "object",
    "properties": {
      "appId": {
        "type": "integer",
        "description": "The identifier of the Sinao application you are using. Each application corresponds to a company and an accounting. A user can have several Sinao applications. To retrieve the identifiers of its applications, you must call the endpoint `/apps`."
      },
      "description": {
        "type": "string"
      }
    },
    "required": [
      "appId",
      "description"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.sinao.app/v1/apps/{appId}/openai/billing/estimate{?description}"
    }
  },
  "responses": {}
}
