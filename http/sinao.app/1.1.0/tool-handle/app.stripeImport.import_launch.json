{
  "name": "app_stripeImport_import_launch",
  "description": "Launch the import of Stripe data",
  "parameters": {
    "type": "object",
    "properties": {
      "appId": {
        "type": "integer",
        "description": "The identifier of the Sinao application you are using. Each application corresponds to a company and an accounting. A user can have several Sinao applications. To retrieve the identifiers of its applications, you must call the endpoint `/apps`."
      },
      "public_key": {
        "x-ype": "string"
      },
      "secret_key": {
        "type": "string"
      },
      "private_webhook": {
        "type": "string"
      },
      "start_at": {
        "type": "string"
      }
    },
    "required": [
      "appId",
      "public_key",
      "secret_key",
      "private_webhook",
      "start_at"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.sinao.app/v1/apps/{appId}/stripe/import/launch{?public_key,secret_key,private_webhook,start_at}"
    }
  },
  "responses": {}
}
