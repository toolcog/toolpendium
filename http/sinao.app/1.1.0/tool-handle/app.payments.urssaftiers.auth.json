{
  "name": "app_payments_urssaftiers_auth",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "appId": {
        "type": "integer",
        "description": "The identifier of the Sinao application you are using. Each application corresponds to a company and an accounting. A user can have several Sinao applications. To retrieve the identifiers of its applications, you must call the endpoint `/apps`."
      },
      "client_id": {
        "type": "string"
      },
      "client_secret": {
        "type": "string"
      }
    },
    "required": [
      "appId",
      "client_id",
      "client_secret"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.sinao.app/v1/apps/{appId}/urssaf/auth{?client_id,client_secret}"
    }
  },
  "responses": {}
}
