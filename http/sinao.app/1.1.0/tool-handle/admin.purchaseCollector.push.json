{
  "name": "admin_purchaseCollector_push",
  "description": "Push to purchase collector",
  "parameters": {
    "type": "object",
    "properties": {
      "app_identifiant": {
        "type": "string"
      },
      "collector_key": {
        "type": "string"
      },
      "invoice": {
        "format": "binary",
        "type": "string",
        "description": "Invoice or receipt file (pdf or image)"
      }
    },
    "required": [
      "app_identifiant",
      "collector_key",
      "invoice"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.sinao.app/v1/services/collector{?app_identifiant,collector_key,invoice}"
    }
  },
  "responses": {}
}
