{
  "name": "tokens_get_ephemeral_token",
  "description": "This endpoint returns a token that can be passed to an application for authorized access.  The lifetime of this token is 10 seconds.",
  "parameters": {
    "type": "object",
    "properties": {
      "redirect": {
        "$ref": "#/components/schemas/redirect",
        "description": "Include a redirect url to the application formatted with the ephemeral token."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.shipengine.com/v1/tokens/ephemeral{?redirect}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "tokens_get_ephemeral_token_response_body.yaml"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**redirect_url**: The redirect url formatted with the requested token.",
            "**token**: The requested token that expires in 10 seconds."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
