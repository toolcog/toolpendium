{
  "name": "value-estimate",
  "description": "Returns a property value estimate and comparable properties.",
  "parameters": {
    "type": "object",
    "properties": {
      "address": {
        "default": "5500 Grand Lake Drive, San Antonio, TX, 78244",
        "type": "string",
        "description": "The full property address, in the format of `Street, City, State, Zip`. You need to provide either the `address` or the `latitude`/`longitude` parameters"
      },
      "latitude": {
        "format": "float",
        "type": "number",
        "description": "The latitude of the property. The `latitude`/`longitude` can be provided instead of the `address` parameter"
      },
      "longitude": {
        "format": "float",
        "type": "number",
        "description": "The longitude of the property. The `latitude`/`longitude` can be provided instead of the `address` parameter"
      },
      "propertyType": {
        "default": "Single Family",
        "enum": [
          "Single Family",
          "Condo",
          "Townhouse",
          "Manufactured",
          "Multi-Family",
          "Apartment",
          "Land"
        ],
        "type": "string",
        "description": "The type of the property. See [explanation of property types](https://developers.rentcast.io/reference/property-types)"
      },
      "bedrooms": {
        "default": 4,
        "format": "float",
        "type": "number",
        "description": "The number of bedrooms in the property. Use `0` to indicate a studio layout"
      },
      "bathrooms": {
        "default": 2,
        "format": "float",
        "type": "number",
        "description": "The number of bathrooms in the property. Supports fractions to indicate partial bathrooms"
      },
      "squareFootage": {
        "default": 1600,
        "format": "float",
        "type": "number",
        "description": "The total living area size of the property, in square feet"
      },
      "maxRadius": {
        "format": "float",
        "type": "number",
        "description": "The maximum distance between comparable listings and the subject property, in miles"
      },
      "daysOld": {
        "format": "float",
        "type": "number",
        "description": "The maximum number of days since comparable listings were last seen on the market, with a minimum of 1"
      },
      "compCount": {
        "default": 5,
        "format": "int32",
        "type": "integer",
        "description": "The number of comparable listings to use when calculating the value estimate, between 5 and 25. Defaults to 10 if not provided"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.rentcast.io/v1/avm/value{?address,latitude,longitude,propertyType,bedrooms,bathrooms,squareFootage,maxRadius,daysOld,compCount}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**comparables**",
            "**latitude** (default: 0)",
            "**longitude** (default: 0)",
            "**price** (default: 0)",
            "**priceRangeHigh** (default: 0)",
            "**priceRangeLow** (default: 0)"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
