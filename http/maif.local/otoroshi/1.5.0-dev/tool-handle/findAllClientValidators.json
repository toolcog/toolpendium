{
  "name": "findAllClientValidators",
  "description": "Get all validation authoritiess",
  "parameters": {
    "type": "object"
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://otoroshi-api.oto.tools//api/client-validators"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "Settings to access a validation authority server",
        "**Key properties:**",
        {
          "$ul": [
            "**alwaysValid**: Bypass http calls, every certificates are valids",
            "**badTtl**: The TTL for invalid access response caching",
            "**description**: The description of the settings",
            "**goodTtl**: The TTL for valid access response caching",
            "**headers**: HTTP call headers",
            "**host**: The host of the server",
            "**id**: The id of the settings",
            "**method**: The HTTP method",
            "**name**: The name of the settings",
            "**noCache**: Avoid caching responses",
            "**path**: The URL path",
            "**timeout**: The call timeout",
            "**url**: The URL of the server"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.name"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
