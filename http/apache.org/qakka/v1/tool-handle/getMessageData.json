{
  "name": "getMessageData",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "queueName": {
        "type": "string",
        "description": "Name of Queue"
      },
      "queueMessageId": {
        "type": "string",
        "description": "ID of Queue Message for which data is to be returned"
      }
    },
    "required": [
      "queueName",
      "queueMessageId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://apache.local/queues/{queueName}/data/{queueMessageId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Response returned by most Queue API calls.",
        "**Key properties:**",
        {
          "$ul": [
            "**count**: Count of Queues or QueueMessages returned by the call.",
            "**message**: Informative message intended for client.",
            "**queueMessages**: Queues Messages returned by the call, or empty if not applicable.",
            "**queues**: Queues returned but the call, or empty if not applicable."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
