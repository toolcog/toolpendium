{
  "name": "get_session_matches",
  "description": "Return set of matching and non-matching hashes from a session.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string"
      }
    },
    "required": [
      "name"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//session/get/{name}"
    }
  },
  "responses": {}
}
