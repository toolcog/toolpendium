{
  "name": "get_lookup_sha256",
  "description": "Lookup SHA-256.",
  "parameters": {
    "type": "object",
    "properties": {
      "sha256": {
        "type": "string"
      }
    },
    "required": [
      "sha256"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//lookup/sha256/{sha256}"
    }
  },
  "responses": {}
}
