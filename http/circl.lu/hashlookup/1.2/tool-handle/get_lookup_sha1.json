{
  "name": "get_lookup_sha1",
  "description": "Lookup SHA-1.",
  "parameters": {
    "type": "object",
    "properties": {
      "sha1": {
        "type": "string"
      }
    },
    "required": [
      "sha1"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//lookup/sha1/{sha1}"
    }
  },
  "responses": {}
}
