{
  "name": "storageGetFileView",
  "description": "Get a file content by its unique ID. This endpoint is similar to the download method but returns with no  'Content-Disposition: attachment' header.",
  "parameters": {
    "type": "object",
    "properties": {
      "fileId": {
        "type": "string",
        "x-example": "[FILE_ID]",
        "description": "File unique ID."
      }
    },
    "required": [
      "fileId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://appwrite.io/v1/storage/files/{fileId}/view"
    }
  },
  "responses": {}
}
