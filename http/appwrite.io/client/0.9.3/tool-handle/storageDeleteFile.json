{
  "name": "storageDeleteFile",
  "description": "Delete a file by its unique ID. Only users with write permissions have access to delete this resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "fileId": {
        "type": "string",
        "x-example": "[FILE_ID]",
        "description": "File unique ID."
      }
    },
    "required": [
      "fileId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://appwrite.io/v1/storage/files/{fileId}"
    }
  },
  "responses": {}
}
