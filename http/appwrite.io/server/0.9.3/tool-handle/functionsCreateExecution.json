{
  "name": "functionsCreateExecution",
  "description": "Trigger a function execution. The returned object will return you the current execution status. You can ping the `Get Execution` endpoint to get updates on the current execution status. Once this endpoint is called, your function execution process will start asynchronously.",
  "parameters": {
    "type": "object",
    "properties": {
      "functionId": {
        "type": "string",
        "x-example": "[FUNCTION_ID]",
        "description": "Function unique ID."
      },
      "body": {
        "properties": {
          "data": {
            "description": "String of custom data to send to function.",
            "type": "string",
            "x-example": null
          }
        },
        "type": "object"
      }
    },
    "required": [
      "functionId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://appwrite.io/v1/functions/{functionId}/executions"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "201": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Execution",
        "**Key properties:**",
        {
          "$ul": [
            "**$id**: Execution ID.",
            "**dateCreated**: The execution creation date in Unix timestamp.",
            "**exitCode**: The script exit code.",
            "**functionId**: Function ID.",
            "**status**: The status of the function execution. Possible values can be: `waiting`, `processing`, `completed`, or `failed`.",
            "**stderr**: The script stderr output string. Logs the last 4,000 characters of the execution stderr output",
            "**stdout**: The script stdout output string. Logs the last 4,000 characters of the execution stdout output.",
            "**time**: The script execution time in seconds.",
            "**trigger**: The trigger that caused the function to execute. Possible values can be: `http`, `schedule`, or `event`."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
