{
  "name": "usersDeleteSession",
  "description": "Delete a user sessions by its unique ID.",
  "parameters": {
    "type": "object",
    "properties": {
      "userId": {
        "type": "string",
        "x-example": "[USER_ID]",
        "description": "User unique ID."
      },
      "sessionId": {
        "type": "string",
        "x-example": "[SESSION_ID]",
        "description": "User unique session ID."
      }
    },
    "required": [
      "userId",
      "sessionId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://appwrite.io/v1/users/{userId}/sessions/{sessionId}"
    }
  },
  "responses": {}
}
