{
  "name": "api_transport-pushy_id_delete",
  "description": "Removes the TransportPushy resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportPushy identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-pushy/{id}"
    }
  },
  "responses": {}
}
