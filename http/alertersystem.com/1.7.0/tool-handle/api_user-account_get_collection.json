{
  "name": "api_user-account_get_collection",
  "description": "Retrieves the collection of UserAccount resources.",
  "parameters": {
    "type": "object",
    "properties": {
      "page": {
        "default": 1,
        "type": "integer",
        "description": "The collection page number"
      },
      "properties[]": {
        "items": {
          "type": "string"
        },
        "type": "array",
        "description": "Allows you to reduce the response to contain only the properties you need. If your desired property is nested, you can address it using nested arrays. Example: properties[]={propertyName}&properties[]={anotherPropertyName}&properties[{nestedPropertyParent}][]={nestedProperty}"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api/user-account{?page,properties[]}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "The UserAccount resource contains basic information regarding the authenticated user account.",
        "**Key properties:**",
        {
          "$ul": [
            "**accountLevelCode**: The account level of the user.",
            "**creditsOveragePercentTripSwitch**: If the credits consumed in the billing period are this percentage above the account plan's included credits, cease further consumption of credits until the end of the billing period. Any integer between 1 and 1,000. Optional. Leave blank for no limit.",
            "**email**: The email address of the user.",
            "**firstName**: The first name of the user.",
            "**id**: The unique identifier of the resource instance.",
            "**isDelinquent**: Whether the user account has overdue payments.",
            "**lastName**: The last name of the user.",
            "**timezoneCode**: The timezone of the user."
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.id"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
