{
  "name": "api_partition_id_delete",
  "description": "Removes the Partition resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Partition identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/partition/{id}"
    }
  },
  "responses": {}
}
