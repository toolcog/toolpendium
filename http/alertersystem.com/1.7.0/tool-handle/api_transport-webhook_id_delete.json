{
  "name": "api_transport-webhook_id_delete",
  "description": "Removes the TransportWebhook resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportWebhook identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-webhook/{id}"
    }
  },
  "responses": {}
}
