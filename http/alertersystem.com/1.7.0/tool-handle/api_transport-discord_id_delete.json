{
  "name": "api_transport-discord_id_delete",
  "description": "Removes the TransportDiscord resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportDiscord identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-discord/{id}"
    }
  },
  "responses": {}
}
