{
  "name": "api_transport-line-notify_id_delete",
  "description": "Removes the TransportLineNotify resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportLineNotify identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-line-notify/{id}"
    }
  },
  "responses": {}
}
