{
  "name": "api_transport-message-bird_id_put",
  "description": "Replaces the TransportMessageBird resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportMessageBird identifier"
      },
      "body": {
        "$ref": "#/components/schemas/TransportMessageBird-put",
        "description": "The updated TransportMessageBird resource"
      }
    },
    "required": [
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "//api/transport-message-bird/{id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "The TransportMessageBird resource is a collection of transports that carry dispatched alerts to the external MessageBird service.",
        "**Key properties:**",
        {
          "$ul": [
            "**createdAt**: When the resource instance was created. This date-time is in the UTC timezone.",
            "**dataSegmentCode**: User-provided string on which to segment and filter data. Max 50 characters.",
            "**id**: The unique identifier of the resource instance.",
            "**messageBirdFrom**: The sender value for the MessageBird service.",
            "**messageBirdToken**: The token for the MessageBird service. Stored in encrypted format.",
            "**partition**: The partition that contains this resource instance. The resource cannot be moved to another partition.",
            "**resourceOwner**: The name of the person who owns this resource.",
            "**transportName**: The name of the transport."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
