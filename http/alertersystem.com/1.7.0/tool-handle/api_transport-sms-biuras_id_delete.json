{
  "name": "api_transport-sms-biuras_id_delete",
  "description": "Removes the TransportSmsBiuras resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportSmsBiuras identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-sms-biuras/{id}"
    }
  },
  "responses": {}
}
