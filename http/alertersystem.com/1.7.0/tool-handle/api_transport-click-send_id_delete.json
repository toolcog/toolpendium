{
  "name": "api_transport-click-send_id_delete",
  "description": "Removes the TransportClickSend resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportClickSend identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-click-send/{id}"
    }
  },
  "responses": {}
}
