{
  "name": "api_transport-mattermost_id_delete",
  "description": "Removes the TransportMattermost resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportMattermost identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-mattermost/{id}"
    }
  },
  "responses": {}
}
