{
  "name": "api_transport-message-bird_id_delete",
  "description": "Removes the TransportMessageBird resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportMessageBird identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-message-bird/{id}"
    }
  },
  "responses": {}
}
