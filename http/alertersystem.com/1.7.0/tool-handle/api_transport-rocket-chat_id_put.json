{
  "name": "api_transport-rocket-chat_id_put",
  "description": "Replaces the TransportRocketChat resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportRocketChat identifier"
      },
      "body": {
        "$ref": "#/components/schemas/TransportRocketChat-put",
        "description": "The updated TransportRocketChat resource"
      }
    },
    "required": [
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "//api/transport-rocket-chat/{id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "The TransportRocketChat resource is a collection of transports that carry dispatched alerts to the external Rocket Chat service.",
        "**Key properties:**",
        {
          "$ul": [
            "**createdAt**: When the resource instance was created. This date-time is in the UTC timezone.",
            "**dataSegmentCode**: User-provided string on which to segment and filter data. Max 50 characters.",
            "**id**: The unique identifier of the resource instance.",
            "**partition**: The partition that contains this resource instance. The resource cannot be moved to another partition.",
            "**resourceOwner**: The name of the person who owns this resource.",
            "**rocketChatChannel**: The channel for the Rocket Chat service.",
            "**rocketChatToken**: The access token for the Rocket Chat service. Stored in encrypted format.",
            "**transportName**: The name of the transport."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
