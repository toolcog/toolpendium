{
  "name": "api_transport-one-signal_post",
  "description": "Creates a TransportOneSignal resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/TransportOneSignal-post",
        "description": "The new TransportOneSignal resource"
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//api/transport-one-signal"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "201": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "The TransportOneSignal resource is a collection of transports that carry dispatched alerts to the external One Signal service.",
        "**Key properties:**",
        {
          "$ul": [
            "**createdAt**: When the resource instance was created. This date-time is in the UTC timezone.",
            "**dataSegmentCode**: User-provided string on which to segment and filter data. Max 50 characters.",
            "**id**: The unique identifier of the resource instance.",
            "**oneSignalApiKey**: The API (auth) key for the One Signal service. Stored in encrypted format.",
            "**oneSignalAppId**: The App ID for the One Signal service.",
            "**oneSignalDefaultRecipientId**: The optional default recipient ID for the One Signal service.",
            "**partition**: The partition that contains this resource instance. The resource cannot be moved to another partition.",
            "**resourceOwner**: The name of the person who owns this resource.",
            "**transportName**: The name of the transport."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
