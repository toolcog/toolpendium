{
  "name": "api_transport-pushover_id_delete",
  "description": "Removes the TransportPushover resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportPushover identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-pushover/{id}"
    }
  },
  "responses": {}
}
