{
  "name": "api_transport-google-chat_id_delete",
  "description": "Removes the TransportGoogleChat resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportGoogleChat identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-google-chat/{id}"
    }
  },
  "responses": {}
}
