{
  "name": "api_transport-plivo_id_delete",
  "description": "Removes the TransportPlivo resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportPlivo identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-plivo/{id}"
    }
  },
  "responses": {}
}
