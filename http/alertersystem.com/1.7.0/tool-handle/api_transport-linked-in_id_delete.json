{
  "name": "api_transport-linked-in_id_delete",
  "description": "Removes the TransportLinkedIn resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportLinkedIn identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-linked-in/{id}"
    }
  },
  "responses": {}
}
