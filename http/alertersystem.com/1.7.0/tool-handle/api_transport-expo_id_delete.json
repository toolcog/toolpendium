{
  "name": "api_transport-expo_id_delete",
  "description": "Removes the TransportExpo resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportExpo identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-expo/{id}"
    }
  },
  "responses": {}
}
