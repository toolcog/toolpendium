{
  "name": "api_timezone-code_id_get",
  "description": "Retrieves a TimezoneCode resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TimezoneCode identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api/timezone-code/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "The TimezoneCode resource is a set of codes that describes the different international timezones.",
        "**Key properties:**",
        {
          "$ul": [
            "**codeName**: The name of the code.",
            "**id**: The unique identifier of the resource instance.",
            "**offsetFromUtc**: The numeric offset from the UTC timezone.",
            "**timezoneDateString**: The text offset from the UTC timezone that forms part of the ISO 8601 date time format."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
