{
  "name": "api_user-account_id_patch",
  "description": "Updates the UserAccount resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "UserAccount identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PATCH",
    "url": {
      "$uri": "//api/user-account/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "The UserAccount resource contains basic information regarding the authenticated user account.",
        "**Key properties:**",
        {
          "$ul": [
            "**accountLevelCode**: The account level of the user.",
            "**creditsOveragePercentTripSwitch**: If the credits consumed in the billing period are this percentage above the account plan's included credits, cease further consumption of credits until the end of the billing period. Any integer between 1 and 1,000. Optional. Leave blank for no limit.",
            "**email**: The email address of the user.",
            "**firstName**: The first name of the user.",
            "**id**: The unique identifier of the resource instance.",
            "**isDelinquent**: Whether the user account has overdue payments.",
            "**lastName**: The last name of the user.",
            "**timezoneCode**: The timezone of the user."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
