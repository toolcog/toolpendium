{
  "name": "api_transport-one-signal_id_delete",
  "description": "Removes the TransportOneSignal resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportOneSignal identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-one-signal/{id}"
    }
  },
  "responses": {}
}
