{
  "name": "api_transport-slack_id_delete",
  "description": "Removes the TransportSlack resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportSlack identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-slack/{id}"
    }
  },
  "responses": {}
}
