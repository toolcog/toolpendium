{
  "name": "api_transport-smsmode_id_delete",
  "description": "Removes the TransportSmsmode resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportSmsmode identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-smsmode/{id}"
    }
  },
  "responses": {}
}
