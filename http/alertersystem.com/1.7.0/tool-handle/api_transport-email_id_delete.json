{
  "name": "api_transport-email_id_delete",
  "description": "Removes the TransportEmail resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportEmail identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-email/{id}"
    }
  },
  "responses": {}
}
