{
  "name": "api_transport-line-notify_id_get",
  "description": "Retrieves a TransportLineNotify resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportLineNotify identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api/transport-line-notify/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "The TransportLineNotify resource is a collection of transports that carry dispatched alerts to the external LINE Notify service.",
        "**Key properties:**",
        {
          "$ul": [
            "**createdAt**: When the resource instance was created. This date-time is in the UTC timezone.",
            "**dataSegmentCode**: User-provided string on which to segment and filter data. Max 50 characters.",
            "**id**: The unique identifier of the resource instance.",
            "**lineNotifyAccessToken**: The access token for the LINE Notify service. Stored in encrypted format.",
            "**lineNotifyStickerId**: The sticker ID value for the LINE Notify service.",
            "**lineNotifyStickerPackageId**: The sticker package ID value for the LINE Notify service.",
            "**partition**: The partition that contains this resource instance. The resource cannot be moved to another partition.",
            "**resourceOwner**: The name of the person who owns this resource.",
            "**transportName**: The name of the transport."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
