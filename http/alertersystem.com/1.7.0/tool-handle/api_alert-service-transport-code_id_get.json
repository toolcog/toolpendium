{
  "name": "api_alert-service-transport-code_id_get",
  "description": "Retrieves a AlertServiceTransportCode resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "AlertServiceTransportCode identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api/alert-service-transport-code/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "The AlertServiceTransportCode resource is a set of codes that describes the type of transport that is used by an alert service.",
        "**Key properties:**",
        {
          "$ul": [
            "**codeName**",
            "**id**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
