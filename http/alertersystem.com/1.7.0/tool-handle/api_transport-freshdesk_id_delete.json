{
  "name": "api_transport-freshdesk_id_delete",
  "description": "Removes the TransportFreshdesk resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportFreshdesk identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-freshdesk/{id}"
    }
  },
  "responses": {}
}
