{
  "name": "api_monitor_id_delete",
  "description": "Removes the Monitor resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Monitor identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/monitor/{id}"
    }
  },
  "responses": {}
}
