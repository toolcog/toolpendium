{
  "name": "api_transport-chatwork_id_delete",
  "description": "Removes the TransportChatwork resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportChatwork identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-chatwork/{id}"
    }
  },
  "responses": {}
}
