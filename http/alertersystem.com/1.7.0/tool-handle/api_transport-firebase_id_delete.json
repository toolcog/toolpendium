{
  "name": "api_transport-firebase_id_delete",
  "description": "Removes the TransportFirebase resource.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "TransportFirebase identifier"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api/transport-firebase/{id}"
    }
  },
  "responses": {}
}
