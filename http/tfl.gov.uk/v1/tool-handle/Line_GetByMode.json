{
  "name": "Line_GetByMode",
  "description": "Gets lines that serve the given modes.",
  "parameters": {
    "type": "object",
    "properties": {
      "modes": {
        "items": {
          "type": "string"
        },
        "type": "array",
        "description": "A comma-separated list of modes e.g. tube,dlr"
      }
    },
    "required": [
      "modes"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.digital.tfl.gov.uk/Line/Mode/{modes}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**created**",
            [
              "**crowding**",
              {
                "$ul": [
                  "**passengerFlows**: Busiest times at a station (static information)",
                  "**trainLoadings**: Train Loading on a scale 1-6, 1 being \"Very quiet\" and 6 being \"Exceptionally busy\" (static information)"
                ]
              }
            ],
            "**disruptions**",
            "**id**",
            "**lineStatuses**",
            "**modeName**",
            "**modified**",
            "**name**",
            "**routeSections**",
            "**serviceTypes**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.name"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
