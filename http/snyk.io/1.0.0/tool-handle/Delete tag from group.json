{
  "name": "Delete_tag_from_group",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "groupId": {
        "type": "string",
        "description": "The group ID. The `API_KEY` must have access admin to this group."
      },
      "body": {
        "example": {
          "force": false,
          "key": "",
          "value": ""
        },
        "properties": {
          "force": {
            "description": "force delete tag that has entities (default is `false`).",
            "type": "boolean"
          },
          "key": {
            "description": "Valid tag key.",
            "type": "string"
          },
          "value": {
            "description": "Valid tag value.",
            "type": "string"
          }
        },
        "type": "object"
      }
    },
    "required": [
      "groupId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.snyk.io/v1/group/{groupId}/tags/delete"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**force**: force delete tag that has entities (default is `false`).",
            "**key**: Valid tag key.",
            "**value**: Valid tag value."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
