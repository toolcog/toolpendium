{
  "name": "List_all_ignores",
  "description": "Temporary ignores include an `expires` attribute, while permanent ignores do not.",
  "parameters": {
    "type": "object",
    "properties": {
      "orgId": {
        "type": "string",
        "description": "The organization ID to list ignores for. The `API_KEY` must have access to this organization."
      },
      "projectId": {
        "type": "string",
        "description": "The project ID to list ignores for."
      }
    },
    "required": [
      "orgId",
      "projectId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.snyk.io/v1/org/{orgId}/project/{projectId}/ignores"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
