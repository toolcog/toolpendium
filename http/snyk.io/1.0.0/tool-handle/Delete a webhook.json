{
  "name": "Delete_a_webhook",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "orgId": {
        "type": "string",
        "description": "Automatically added"
      },
      "webhookId": {
        "type": "string",
        "description": "Automatically added"
      }
    },
    "required": [
      "orgId",
      "webhookId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.snyk.io/v1/org/{orgId}/webhooks/{webhookId}"
    }
  },
  "responses": {}
}
