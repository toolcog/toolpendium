{
  "name": "Test_for_issues_in_a_public_gem_by_name_and_version",
  "description": "You can test `rubygems` packages for issues according to their name and version.",
  "parameters": {
    "type": "object",
    "properties": {
      "gemName": {
        "type": "string",
        "description": "The gem name."
      },
      "version": {
        "type": "string",
        "description": "The gem version to test."
      },
      "org": {
        "type": "string",
        "description": "The organization to test the package with. See \"The Snyk organization for a request\" above."
      }
    },
    "required": [
      "gemName",
      "version"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.snyk.io/v1/test/rubygems/{gemName}/{version}{?org}"
    }
  },
  "responses": {}
}
