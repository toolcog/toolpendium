{
  "name": "List_all_tags_in_a_group",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "groupId": {
        "type": "string",
        "description": "The group ID. The `API_KEY` must have access admin to this group."
      },
      "perPage": {
        "type": "number",
        "description": "The number of results to return (the default is 1000)."
      },
      "page": {
        "type": "number",
        "description": "The offset from which to start returning results from."
      }
    },
    "required": [
      "groupId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.snyk.io/v1/group/{groupId}/tags{?perPage,page}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**tags**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
