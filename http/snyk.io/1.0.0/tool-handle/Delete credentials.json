{
  "name": "Delete_credentials",
  "description": "Removes any credentials set for this integration. If this is a brokered connection the operation will have no effect.",
  "parameters": {
    "type": "object",
    "properties": {
      "orgId": {
        "type": "string",
        "description": "The organization ID. The `API_KEY` must have access to this organization."
      },
      "integrationId": {
        "type": "string",
        "description": "The integration ID."
      }
    },
    "required": [
      "orgId",
      "integrationId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.snyk.io/v1/org/{orgId}/integrations/{integrationId}/authentication"
    }
  },
  "responses": {}
}
