{
  "name": "Get_an_organization_s_entitlement_value",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "orgId": {
        "type": "string",
        "description": "The organization ID to query the entitlement for. The `API_KEY` must have access to this organization."
      },
      "entitlementKey": {
        "enum": [
          "licenses",
          "reports",
          "fullVulnDB"
        ],
        "type": "string",
        "description": "The entitlement to query."
      }
    },
    "required": [
      "orgId",
      "entitlementKey"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.snyk.io/v1/org/{orgId}/entitlement/{entitlementKey}"
    }
  },
  "responses": {}
}
