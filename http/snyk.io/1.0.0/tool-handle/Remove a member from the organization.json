{
  "name": "Remove_a_member_from_the_organization",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "orgId": {
        "type": "string",
        "description": "The organization ID. The `API_KEY` must admin have access to this organization."
      },
      "userId": {
        "type": "string",
        "description": "The user ID we want to remove."
      }
    },
    "required": [
      "orgId",
      "userId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.snyk.io/v1/org/{orgId}/members/{userId}"
    }
  },
  "responses": {}
}
