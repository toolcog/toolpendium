{
  "name": "Watermark_PutWatermark",
  "description": "Returns information about the watermark for the account.",
  "parameters": {
    "type": "object",
    "properties": {
      "accountId": {
        "type": "string",
        "description": "The external account number (int) or account ID GUID."
      },
      "body": {
        "$ref": "#/components/schemas/watermark",
        "description": "When **true,** the account has the watermark feature enabled, and the envelope is not complete, then the watermark for the account is added to the PDF documents. This option can remove the watermark. "
      }
    },
    "required": [
      "accountId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://www.docusign.net/restapi/v2.1/accounts/{accountId}/watermark"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "",
        "**Key properties:**",
        {
          "$ul": [
            "**displayAngle**: The display angle of the watermark. Valid values:",
            "**enabled**: When **true,** the watermark will be applied to documents that are still in progress. When **false,** the watermark will not be applied.",
            "**font**: The watermark's font. Valid values:",
            "**fontColor**: The watermark's font color. Valid values:",
            "**fontSize**: The watermark's font size. Valid values:",
            "**id**: The ID of the watermark.",
            "**imageBase64**: The Base64 representation of the watermark image.",
            "**transparency**: The transparency percentage of the watermark image. Valid values:",
            "**watermarkText**: The text for the watermark."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "400": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "This object describes errors that occur. It is only valid for responses and ignored in requests.",
        "**Key properties:**",
        {
          "$ul": [
            "**errorCode**: The code associated with the error condition.",
            "**message**: A brief message describing the error condition."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
