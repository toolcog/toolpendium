{
  "name": "PicoLoadmanagementGroup_Get",
  "description": "GET: api/pico/loadmanagementgroup\r\n            \r\n            Returns a pico load management group by it's id",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://smart-me.com:443/api/pico/loadmanagementgroup/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "DTO for the pico loadmanagement group",
        "**Key properties:**",
        {
          "$ul": [
            "**Id**: The ID of the loadmanagement group",
            "**MaxCurrent**: The max current of this loadmanagement group",
            "**Name**: The name of the station",
            "**NumberOfStations**: The number of assigned stations"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
