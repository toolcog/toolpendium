{
  "name": "MeterFolderInformation_Post",
  "description": "Sets the Name of a Meter or a Folder",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/MeterFolderInformationToPost"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "MeterFolderInformationToPost": {
        "description": "Container for a Meter or Folder Information to Post",
        "properties": {
          "Id": {
            "description": "The ID of the device or folder",
            "type": "string"
          },
          "Name": {
            "description": "Name of the Meter or Folder",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://smart-me.com:443/api/MeterFolderInformation"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
