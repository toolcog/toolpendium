{
  "name": "UserToFolderAssign_Post",
  "description": "Assign a user to a folder",
  "parameters": {
    "type": "object",
    "properties": {
      "source": {
        "type": "string",
        "description": "The ID of the user that should be assign"
      },
      "target": {
        "type": "string",
        "description": "The ID of the folder that should be the parent"
      },
      "oldFolder": {
        "type": "string",
        "description": "The ID of the old folder (in case of a drag and drop to a new folder)"
      }
    },
    "required": [
      "source",
      "target",
      "oldFolder"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://smart-me.com:443/api/folder/user/assign{?source,target,oldFolder}"
    }
  },
  "responses": {}
}
