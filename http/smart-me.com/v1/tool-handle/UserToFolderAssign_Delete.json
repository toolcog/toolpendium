{
  "name": "UserToFolderAssign_Delete",
  "description": "Deletes a user to folder assignement",
  "parameters": {
    "type": "object",
    "properties": {
      "source": {
        "type": "string",
        "description": "The ID of the user that should be de-assign"
      },
      "target": {
        "type": "string",
        "description": "The ID of the folder"
      }
    },
    "required": [
      "source",
      "target"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://smart-me.com:443/api/folder/user/assign{?source,target}"
    }
  },
  "responses": {}
}
