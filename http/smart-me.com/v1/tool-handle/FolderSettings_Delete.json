{
  "name": "FolderSettings_Delete",
  "description": "Deletes a folder",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The ID of the folder"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://smart-me.com:443/api/folder/settings/{id}"
    }
  },
  "responses": {}
}
