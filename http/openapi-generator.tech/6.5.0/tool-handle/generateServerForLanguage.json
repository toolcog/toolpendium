{
  "name": "generateServerForLanguage",
  "description": "Accepts a `GeneratorInput` options map for spec location and generation options.",
  "parameters": {
    "type": "object",
    "properties": {
      "framework": {
        "type": "string",
        "description": "framework"
      },
      "body": {
        "$ref": "#/components/schemas/GeneratorInput",
        "description": "parameters"
      }
    },
    "required": [
      "framework",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//api.openapi-generator.tech/api/gen/servers/{framework}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "ResponseCode"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**code**: File download code",
            "**link**: URL for fetching the generated client"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
