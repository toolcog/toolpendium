{
  "name": "messages_fetchAll",
  "description": "Fetch messages",
  "parameters": {
    "type": "object",
    "properties": {
      "accountId": {
        "type": "string",
        "description": "Account to apply operations to"
      },
      "offset": {
        "format": "int64",
        "minimum": 0,
        "type": "integer",
        "description": "Results to skip when paginating through a result set"
      },
      "limit": {
        "format": "int64",
        "maximum": 100,
        "minimum": 1,
        "type": "integer",
        "description": "Maximum number of results to return"
      },
      "contactId": {
        "type": "string",
        "description": "ID of contact"
      },
      "conversationId": {
        "type": "string",
        "description": "ID of conversation"
      }
    },
    "required": [
      "accountId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.sakari.io/v1/accounts/{accountId}/messages{?offset,limit,contactId,conversationId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**error**",
              {
                "$ul": [
                  "**code**",
                  "**message**"
                ]
              }
            ],
            [
              "**pagination**",
              {
                "$ul": [
                  "**limit**",
                  "**offset**",
                  "**totalCount**"
                ]
              }
            ],
            "**success**",
            "**data**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
