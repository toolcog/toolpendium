{
  "name": "webhooks_unsubscribe",
  "description": "Delete subscription for receiving notifications",
  "parameters": {
    "type": "object",
    "properties": {
      "accountId": {
        "type": "string",
        "description": "Account to apply operations to"
      },
      "url": {
        "type": "string",
        "description": "Account to apply operations to"
      }
    },
    "required": [
      "accountId",
      "url"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.sakari.io/v1/accounts/{accountId}/webhooks/{url}"
    }
  },
  "responses": {}
}
