{
  "name": "ips_list",
  "description": "List all ip addresses (and prefixes).",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "example": "id1,id2,id3",
        "items": {
          "type": "string"
        },
        "type": "array",
        "description": "Filter by id"
      },
      "managing_account": {
        "type": "string",
        "description": "Filter by managing_account"
      },
      "consuming_account": {
        "type": "string",
        "description": "Filter by consuming_account"
      },
      "external_ref": {
        "type": "string",
        "description": "Filter by external_ref"
      },
      "network_service": {
        "type": "string",
        "description": "Filter by network_service"
      },
      "network_service_config": {
        "type": "string",
        "description": "Filter by network_service_config"
      },
      "network_feature": {
        "type": "string",
        "description": "Filter by network_feature"
      },
      "network_feature_config": {
        "type": "string",
        "description": "Filter by network_feature_config"
      },
      "version": {
        "type": "integer",
        "description": "Filter by version"
      },
      "fqdn": {
        "type": "string",
        "description": "Filter by fqdn"
      },
      "prefix_length": {
        "type": "integer",
        "description": "Filter by prefix_length"
      },
      "valid_not_before": {
        "type": "string",
        "description": "Filter by valid_not_before"
      },
      "valid_not_after": {
        "type": "string",
        "description": "Filter by valid_not_after"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v2/ips{?id,managing_account,consuming_account,external_ref,network_service,network_service_config,network_feature,network_feature_config,version,fqdn,prefix_length,valid_not_before,valid_not_after}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "PartialIpAddress list"
        },
        "IP-Address",
        "**Key properties:**",
        {
          "$ul": [
            "**address**: IPv4 or IPv6 Address in the following format:",
            "**consuming_account**: The `id` of the account consuming a service.",
            "**external_ref**: Reference field, free to use for the API user.",
            "**fqdn**",
            "**id**",
            "**managing_account**: The `id` of the account responsible for managing the service via",
            "**prefix_length**: The CIDR ip prefix length",
            "**valid_not_after**",
            "**valid_not_before**",
            "**version**: The version of the internet protocol."
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.id"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    },
    "400": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    },
    "401": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    },
    "403": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
