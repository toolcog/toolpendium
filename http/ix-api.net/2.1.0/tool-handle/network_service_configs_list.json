{
  "name": "network_service_configs_list",
  "description": "Get all `network-service-config`s.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "example": "id1,id2,id3",
        "items": {
          "type": "string"
        },
        "type": "array",
        "description": "Filter by id"
      },
      "state": {
        "type": "string",
        "description": "Filter by state"
      },
      "state__is_not": {
        "type": "string",
        "description": "Filter by state__is_not"
      },
      "managing_account": {
        "type": "string",
        "description": "Filter by managing_account"
      },
      "consuming_account": {
        "type": "string",
        "description": "Filter by consuming_account"
      },
      "external_ref": {
        "type": "string",
        "description": "Filter by external_ref"
      },
      "type": {
        "enum": [
          "exchange_lan",
          "p2p_vc",
          "p2mp_vc",
          "mp2mp_vc",
          "cloud_vc"
        ],
        "type": "string",
        "description": "Filter by type"
      },
      "inner_vlan": {
        "type": "integer",
        "description": "Filter by inner_vlan"
      },
      "outer_vlan": {
        "type": "integer",
        "description": "Filter by outer_vlan"
      },
      "capacity": {
        "type": "integer",
        "description": "Filter by capacity"
      },
      "network_service": {
        "type": "string",
        "description": "Filter by network_service"
      },
      "connection": {
        "type": "string",
        "description": "Filter by connection"
      },
      "product_offering": {
        "type": "string",
        "description": "Filter by product_offering"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v2/network-service-configs{?id,state,state__is_not,managing_account,consuming_account,external_ref,type,inner_vlan,outer_vlan,capacity,network_service,connection,product_offering}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "NetworkServiceConfig list"
        },
        "Polymorphic Network Service Config",
        {
          "$each": "$.body.*",
          "$as": "item",
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.item"
            }
          }
        }
      ]
    },
    "400": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    },
    "401": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    },
    "403": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
