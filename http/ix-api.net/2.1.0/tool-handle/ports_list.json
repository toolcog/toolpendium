{
  "name": "ports_list",
  "description": "List all ports.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "example": "id1,id2,id3",
        "items": {
          "type": "string"
        },
        "type": "array",
        "description": "Filter by id"
      },
      "state": {
        "type": "string",
        "description": "Filter by state"
      },
      "state__is_not": {
        "type": "string",
        "description": "Filter by state__is_not"
      },
      "media_type": {
        "type": "string",
        "description": "Filter by media_type"
      },
      "pop": {
        "type": "string",
        "description": "Filter by pop"
      },
      "name": {
        "type": "string",
        "description": "Filter by name"
      },
      "external_ref": {
        "type": "string",
        "description": "Filter by external_ref"
      },
      "device": {
        "type": "string",
        "description": "Filter by device"
      },
      "speed": {
        "type": "string",
        "description": "Filter by speed"
      },
      "connection": {
        "type": "string",
        "description": "Filter by connection"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v2/ports{?id,state,state__is_not,media_type,pop,name,external_ref,device,speed,connection}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "PartialPort list"
        },
        "Port",
        "**Key properties:**",
        {
          "$ul": [
            "**billing_account**: An account requires billing_information to be used as a `billing_account`.",
            "**connection**",
            "**consuming_account**: The `id` of the account consuming a service.",
            "**contract_ref**: A reference to a contract. If no specific contract is used, a default",
            "**device**: The device the port.",
            "**external_ref**: Reference field, free to use for the API user.",
            "**id**",
            "**managing_account**: The `id` of the account responsible for managing the service via",
            "**media_type**: The media type of the port.",
            "**name**: Name of the port (set by the exchange) (default: )",
            "**pop**: Same as the `pop` of the `device`.",
            "**purchase_order**: Purchase Order ID which will be displayed on the invoice. (default: )",
            "**role_assignments**: A set of `RoleAssignment`s. See the documentation",
            "**speed**",
            "**state**",
            "**status**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.name"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    },
    "400": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    },
    "401": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    },
    "403": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
