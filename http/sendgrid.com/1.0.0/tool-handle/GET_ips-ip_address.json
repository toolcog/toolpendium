{
  "name": "GET_ips-ip_address",
  "description": "**This endpoint allows you to see which IP pools a particular IP address has been added to.**\n\nThe same IP address can be added to multiple IP pools.\n\nA single IP address or a range of IP addresses may be dedicated to an account in order to send email for multiple domains. The reputation of this IP is based on the aggregate performance of all the senders who use it.",
  "parameters": {
    "type": "object",
    "properties": {
      "ip_address": {
        "type": "string",
        "description": "The IP address you are retrieving the IP pools for."
      }
    },
    "required": [
      "ip_address"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://api.sendgrid.com/v3/ips/{ip_address}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**ip**: The IP address.",
            "**pools**: The list of IP pools that this IP address belongs to.",
            "**rdns**: The reverse DNS record for this IP address.",
            "**start_date**: The date that the IP address was entered into warmup.",
            "**subusers**: The subusers that can send email using this IP address.",
            "**warmup**: Indicates if this IP address is currently warming up.",
            "**whitelabeled**: Indicates if this IP address is associated with a reverse DNS record."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
