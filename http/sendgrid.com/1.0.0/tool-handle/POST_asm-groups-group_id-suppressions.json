{
  "name": "POST_asm-groups-group_id-suppressions",
  "description": "**This endpoint allows you to add email addresses to an unsubscribe group.**\n\nIf you attempt to add suppressions to a group that has been deleted or does not exist, the suppressions will be added to the global suppressions list.",
  "parameters": {
    "type": "object",
    "properties": {
      "on-behalf-of": {
        "default": "The subuser's username. This header generates the API call as if the subuser account was making the call.",
        "type": "string"
      },
      "group_id": {
        "type": "string",
        "description": "The id of the unsubscribe group that you are adding suppressions to."
      },
      "body": {
        "$ref": "#/$defs/suppressions-request"
      }
    },
    "required": [
      "group_id"
    ],
    "$defs": {
      "suppressions-request": {
        "example": {
          "recipient_emails": [
            "test1@example.com",
            "test2@example.com"
          ]
        },
        "properties": {
          "recipient_emails": {
            "description": "The array of email addresses to add or find.",
            "items": {
              "format": "email",
              "type": "string"
            },
            "type": "array"
          }
        },
        "required": [
          "recipient_emails"
        ],
        "title": "Suppressions Request Body",
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://api.sendgrid.com/v3/asm/groups/{group_id}/suppressions"
    },
    "headers": {
      "on-behalf-of": {
        "$": "on-behalf-of"
      }
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "201": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**recipient_emails**: The email addresses you added to the unsubscribe group"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
