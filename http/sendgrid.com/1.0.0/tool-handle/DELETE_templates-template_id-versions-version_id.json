{
  "name": "DELETE_templates-template_id-versions-version_id",
  "description": "**This endpoint allows you to delete a transactional template version.**",
  "parameters": {
    "type": "object",
    "properties": {
      "on-behalf-of": {
        "default": "The subuser's username. This header generates the API call as if the subuser account was making the call.",
        "type": "string"
      },
      "template_id": {
        "format": "uuid",
        "type": "string",
        "description": " The ID of the original template"
      },
      "version_id": {
        "format": "uuid",
        "type": "string",
        "description": "The ID of the template version"
      }
    },
    "required": [
      "template_id",
      "version_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "http://api.sendgrid.com/v3/templates/{template_id}/versions/{version_id}"
    },
    "headers": {
      "on-behalf-of": {
        "$": "on-behalf-of"
      }
    }
  },
  "responses": {}
}
