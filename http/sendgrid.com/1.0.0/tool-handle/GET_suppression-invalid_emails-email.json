{
  "name": "GET_suppression-invalid_emails-email",
  "description": "**This endpoint allows you to retrieve a specific invalid email addresses.**",
  "parameters": {
    "type": "object",
    "properties": {
      "on-behalf-of": {
        "default": "The subuser's username. This header generates the API call as if the subuser account was making the call.",
        "type": "string"
      },
      "email": {
        "type": "string",
        "description": "The specific email address of the invalid email entry that you want to retrieve."
      }
    },
    "required": [
      "email"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://api.sendgrid.com/v3/suppression/invalid_emails/{email}"
    },
    "headers": {
      "on-behalf-of": {
        "$": "on-behalf-of"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Invalid Email list"
        },
        "A specific invalid email.",
        "**Key properties:**",
        {
          "$ul": [
            "**created**: A Unix timestamp indicating when the email address was added to the invalid emails list.",
            "**email**: The email address that was marked as invalid.",
            "**reason**: The reason that the email address was marked as invalid."
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": "Item"
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
