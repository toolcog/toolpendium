{
  "name": "GET_categories-stats-sums",
  "description": "**This endpoint allows you to retrieve the total sum of each email statistic for every category over the given date range.**\n\nIf you do not define any query parameters, this endpoint will return a sum for each category in groups of 10.",
  "parameters": {
    "type": "object",
    "properties": {
      "sort_by_metric": {
        "default": "delivered",
        "type": "string",
        "description": "The metric that you want to sort by.  Must be a single metric."
      },
      "sort_by_direction": {
        "default": "desc",
        "enum": [
          "desc",
          "asc"
        ],
        "type": "string",
        "description": "The direction you want to sort."
      },
      "start_date": {
        "type": "string",
        "description": "The starting date of the statistics to retrieve. Must follow format YYYY-MM-DD."
      },
      "end_date": {
        "type": "string",
        "description": "The end date of the statistics to retrieve. Defaults to today. Must follow format YYYY-MM-DD."
      },
      "limit": {
        "default": 5,
        "type": "integer",
        "description": "Limits the number of results returned."
      },
      "offset": {
        "default": 0,
        "type": "integer",
        "description": "The point in the list to begin retrieving results."
      },
      "aggregated_by": {
        "enum": [
          "day",
          "week",
          "month"
        ],
        "type": "string",
        "description": "How to group the statistics. Must be either \"day\", \"week\", or \"month\"."
      },
      "on-behalf-of": {
        "default": "The subuser's username. This header generates the API call as if the subuser account was making the call.",
        "type": "string"
      }
    },
    "required": [
      "start_date"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://api.sendgrid.com/v3/categories/stats/sums{?sort_by_metric,sort_by_direction,start_date,end_date,limit,offset,aggregated_by}"
    },
    "headers": {
      "on-behalf-of": {
        "$": "on-behalf-of"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Stats: Category Stats"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**date**: The date the statistics were gathered.",
            "**stats**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
