{
  "name": "GET_templates-template_id-versions-version_id",
  "description": "**This endpoint allows you to retrieve a specific version of a template.**",
  "parameters": {
    "type": "object",
    "properties": {
      "on-behalf-of": {
        "default": "The subuser's username. This header generates the API call as if the subuser account was making the call.",
        "type": "string"
      },
      "template_id": {
        "format": "uuid",
        "type": "string",
        "description": " The ID of the original template"
      },
      "version_id": {
        "format": "uuid",
        "type": "string",
        "description": "The ID of the template version"
      }
    },
    "required": [
      "template_id",
      "version_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://api.sendgrid.com/v3/templates/{template_id}/versions/{version_id}"
    },
    "headers": {
      "on-behalf-of": {
        "$": "on-behalf-of"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Transactional Templates: Version Create"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**warnings**",
            "**active**: Set the version as the active version associated with the template. Only one version of a template can be active. The first version created for a template will automatically be set to Active.",
            "**editor**: The editor used in the UI.",
            "**generate_plain_content**: If true, plain_content is always generated from html_content. If false, plain_content is not altered. (default: true)",
            "**html_content**: The HTML content of the version. Maximum of 1048576 bytes allowed.",
            "**name**: Name of the transactional template version.",
            "**plain_content**: Text/plain content of the transactional template version. Maximum of 1048576 bytes allowed. (default: <generated from html_content if left empty>)",
            "**subject**: Subject of the new transactional template version.",
            "**test_data**: For dynamic templates only, the mock json data that will be used for template preview and test sends.",
            "**id**: ID of the transactional template version.",
            "**template_id**: ID of the transactional template.",
            "**thumbnail_url**: A Thumbnail preview of the template's html content.",
            "**updated_at**: The date and time that this transactional template version was updated."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
