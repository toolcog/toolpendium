{
  "name": "GET_asm-groups",
  "description": "**This endpoint allows you to retrieve a list of all suppression groups created by this user.**\n\nThis endpoint can also return information for multiple group IDs that you include in your request. To add a group ID to your request, simply append `?id=123456&id=123456`, with the appropriate group IDs.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "integer"
      },
      "on-behalf-of": {
        "default": "The subuser's username. This header generates the API call as if the subuser account was making the call.",
        "type": "string"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://api.sendgrid.com/v3/asm/groups{?id}"
    },
    "headers": {
      "on-behalf-of": {
        "$": "on-behalf-of"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Suppressions: Suppression Group list"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**description**: A description of the suppression group.",
            "**id**: The id of the suppression group.",
            "**is_default**: Indicates if this is the default suppression group. (default: false)",
            "**last_email_sent_at**",
            "**name**: The name of the suppression group. Each group created by a user must have a unique name.",
            "**unsubscribes**: The unsubscribes associated with this group."
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.name"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
