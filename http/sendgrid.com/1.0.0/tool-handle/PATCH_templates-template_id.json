{
  "name": "PATCH_templates-template_id",
  "description": "**This endpoint allows you to edit the name of a transactional template.**\n\nTo edit the template itself, [create a new transactional template version](https://sendgrid.api-docs.io/v3.0/transactional-templates-versions/create-a-new-transactional-template-version).",
  "parameters": {
    "type": "object",
    "properties": {
      "on-behalf-of": {
        "default": "The subuser's username. This header generates the API call as if the subuser account was making the call.",
        "type": "string"
      },
      "template_id": {
        "type": "string"
      },
      "body": {
        "example": {
          "name": "new_example_name"
        },
        "properties": {
          "name": {
            "description": "The name of the transactional template.",
            "maxLength": 100,
            "type": "string"
          }
        },
        "type": "object"
      }
    },
    "required": [
      "template_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PATCH",
    "url": {
      "$uri": "http://api.sendgrid.com/v3/templates/{template_id}"
    },
    "headers": {
      "on-behalf-of": {
        "$": "on-behalf-of"
      }
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Transactional Templates: Template Lean"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**generation**: Defines the generation of the template.",
            "**id**: The ID of the transactional template.",
            "**name**: The name for the transactional template.",
            "**updated_at **: The date and time that this transactional template version was updated.",
            "**versions**: The different versions of this transactional template.",
            [
              "**warning**",
              {
                "$ul": [
                  "**message**: Warning message for the user"
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
