{
  "name": "usergroups_users_list",
  "description": "List all users in a User Group",
  "parameters": {
    "type": "object",
    "properties": {
      "token": {
        "type": "string",
        "description": "Authentication token. Requires scope: `usergroups:read`"
      },
      "include_disabled": {
        "type": "boolean",
        "description": "Allow results that involve disabled User Groups."
      },
      "usergroup": {
        "type": "string",
        "description": "The encoded ID of the User Group to update."
      }
    },
    "required": [
      "token",
      "usergroup"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://slack.com/api/usergroups.users.list{?token,include_disabled,usergroup}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "usergroups.users.list schema"
        },
        "Schema for successful response from usergroups.users.list method",
        "**Key properties:**",
        {
          "$ul": [
            "**ok**",
            "**users**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "usergroups.users.list error schema"
        },
        "Schema for error response from usergroups.users.list method",
        "**Key properties:**",
        {
          "$ul": [
            "**callstack**: Note: PHP callstack is only visible in dev/qa",
            "**error**",
            "**ok**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
