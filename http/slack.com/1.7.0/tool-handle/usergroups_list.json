{
  "name": "usergroups_list",
  "description": "List all User Groups for a team",
  "parameters": {
    "type": "object",
    "properties": {
      "include_users": {
        "type": "boolean",
        "description": "Include the list of users for each User Group."
      },
      "token": {
        "type": "string",
        "description": "Authentication token. Requires scope: `usergroups:read`"
      },
      "include_count": {
        "type": "boolean",
        "description": "Include the number of users in each User Group."
      },
      "include_disabled": {
        "type": "boolean",
        "description": "Include disabled User Groups."
      }
    },
    "required": [
      "token"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://slack.com/api/usergroups.list{?include_users,token,include_count,include_disabled}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "usergroups.list schema"
        },
        "Schema for successful response from usergroups.list method",
        "**Key properties:**",
        {
          "$ul": [
            "**ok**",
            "**usergroups**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "usergroups.list error schema"
        },
        "Schema for error response from usergroups.list method",
        "**Key properties:**",
        {
          "$ul": [
            "**callstack**: Note: PHP callstack is only visible in dev/qa",
            "**error**",
            "**ok**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
