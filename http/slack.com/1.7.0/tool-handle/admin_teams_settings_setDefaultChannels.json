{
  "name": "admin_teams_settings_setDefaultChannels",
  "description": "Set the default channels of a workspace.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "properties": {
          "channel_ids": {
            "description": "An array of channel IDs.",
            "type": "string"
          },
          "team_id": {
            "description": "ID for the workspace to set the default channel for.",
            "type": "string"
          },
          "token": {
            "description": "Authentication token. Requires scope: `admin.teams:write`",
            "type": "string"
          }
        },
        "required": [
          "token",
          "team_id",
          "channel_ids"
        ],
        "type": "object"
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://slack.com/api/admin.teams.settings.setDefaultChannels"
    },
    "body": {
      "$": "body",
      "encode": "urlencoded"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Default success template"
        },
        "This method either only returns a brief _OK_ response or a verbose schema is not available for this method.",
        "**Key properties:**",
        {
          "$ul": [
            "**ok**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Default error template"
        },
        "This method either only returns a brief _not OK_ response or a verbose schema is not available for this method.",
        "**Key properties:**",
        {
          "$ul": [
            "**ok**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
