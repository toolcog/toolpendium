{
  "name": "admin_usergroups_listChannels",
  "description": "List the channels linked to an org-level IDP group (user group).",
  "parameters": {
    "type": "object",
    "properties": {
      "token": {
        "type": "string",
        "description": "Authentication token. Requires scope: `admin.usergroups:read`"
      },
      "usergroup_id": {
        "type": "string",
        "description": "ID of the IDP group to list default channels for."
      },
      "team_id": {
        "type": "string",
        "description": "ID of the the workspace."
      },
      "include_num_members": {
        "type": "boolean",
        "description": "Flag to include or exclude the count of members per channel."
      }
    },
    "required": [
      "token",
      "usergroup_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://slack.com/api/admin.usergroups.listChannels{?usergroup_id,team_id,include_num_members}"
    },
    "headers": {
      "token": {
        "$": "token"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Default success template"
        },
        "This method either only returns a brief _OK_ response or a verbose schema is not available for this method.",
        "**Key properties:**",
        {
          "$ul": [
            "**ok**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Default error template"
        },
        "This method either only returns a brief _not OK_ response or a verbose schema is not available for this method.",
        "**Key properties:**",
        {
          "$ul": [
            "**ok**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
