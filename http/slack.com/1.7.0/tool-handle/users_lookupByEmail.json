{
  "name": "users_lookupByEmail",
  "description": "Find a user with an email address.",
  "parameters": {
    "type": "object",
    "properties": {
      "token": {
        "type": "string",
        "description": "Authentication token. Requires scope: `users:read.email`"
      },
      "email": {
        "type": "string",
        "description": "An email address belonging to a user in the workspace"
      }
    },
    "required": [
      "token",
      "email"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://slack.com/api/users.lookupByEmail{?token,email}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "users.lookupByEmail success schema"
        },
        "Schema for successful response from users.lookupByEmail method",
        "**Key properties:**",
        {
          "$ul": [
            "**ok**",
            "**user**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "users.lookupByEmail error schema"
        },
        "Schema for error response from users.lookupByEmail method",
        "**Key properties:**",
        {
          "$ul": [
            "**callstack**: Note: PHP callstack is only visible in dev/qa",
            "**error**",
            "**ok**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
