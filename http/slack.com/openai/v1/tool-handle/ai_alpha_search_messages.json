{
  "name": "ai_alpha_search_messages",
  "description": "Search for messages matching a query",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/searchRequest"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "searchRequest": {
        "properties": {
          "query": {
            "description": "Search query",
            "type": "string"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://slack.com/api/ai.alpha.search.messages"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**ok**: Boolean indicating whether or not the request was successful",
            "**results**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
