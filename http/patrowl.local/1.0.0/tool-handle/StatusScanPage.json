{
  "name": "StatusScanPage",
  "description": "Status of a scan identified by id.",
  "parameters": {
    "type": "object",
    "properties": {
      "scanId": {
        "type": "integer",
        "description": "Numeric ID of the scan to get status"
      }
    },
    "required": [
      "scanId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://patrowl.local/status/{scanId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**page**",
            "**status**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
