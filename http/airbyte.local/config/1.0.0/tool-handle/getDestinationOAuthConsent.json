{
  "name": "getDestinationOAuthConsent",
  "description": "Given a destination connector definition ID, return the URL to the consent screen where to redirect the user to.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/DestinationOauthConsentRequest"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "DestinationOauthConsentRequest": {
        "properties": {
          "destinationDefinitionId": {
            "$ref": "#/$defs/DestinationDefinitionId"
          },
          "destinationId": {
            "$ref": "#/$defs/DestinationId"
          },
          "oAuthInputConfiguration": {
            "$ref": "#/$defs/OAuthInputConfiguration"
          },
          "redirectUrl": {
            "description": "The url to redirect to after getting the user consent",
            "type": "string"
          },
          "workspaceId": {
            "$ref": "#/$defs/WorkspaceId"
          }
        },
        "required": [
          "destinationDefinitionId",
          "workspaceId",
          "redirectUrl"
        ],
        "type": "object"
      },
      "DestinationDefinitionId": {
        "format": "uuid",
        "type": "string"
      },
      "DestinationId": {
        "format": "uuid",
        "type": "string"
      },
      "OAuthInputConfiguration": {
        "$ref": "#/$defs/OAuthConfiguration"
      },
      "OAuthConfiguration": {
        "description": "The values required to configure OAuth flows. The schema for this must match the `OAuthConfigSpecification.oauthUserInputFromConnectorConfigSpecification` schema."
      },
      "WorkspaceId": {
        "format": "uuid",
        "type": "string"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://airbyte.local/v1/destination_oauths/get_consent_url"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**consentUrl**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "404": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**exceptionClassName**",
            "**exceptionStack**",
            "**id**",
            "**message**",
            "**rootCauseExceptionClassName**",
            "**rootCauseExceptionStack**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "422": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**exceptionClassName**",
            "**exceptionStack**",
            "**message**",
            "**validationErrors**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
