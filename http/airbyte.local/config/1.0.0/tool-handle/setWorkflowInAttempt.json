{
  "name": "setWorkflowInAttempt",
  "description": "For worker to register the workflow id in attempt.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/SetWorkflowInAttemptRequestBody"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "SetWorkflowInAttemptRequestBody": {
        "properties": {
          "attemptNumber": {
            "$ref": "#/$defs/AttemptNumber"
          },
          "jobId": {
            "$ref": "#/$defs/JobId"
          },
          "processingTaskQueue": {
            "default": "",
            "type": "string"
          },
          "workflowId": {
            "$ref": "#/$defs/WorkflowId"
          }
        },
        "required": [
          "jobId",
          "attemptNumber",
          "workflowId"
        ],
        "type": "object"
      },
      "AttemptNumber": {
        "format": "int32",
        "type": "integer"
      },
      "JobId": {
        "format": "int64",
        "type": "integer"
      },
      "WorkflowId": {
        "type": "string"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://airbyte.local/v1/attempt/set_workflow_in_attempt"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**succeeded**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
