{
  "name": "executeDestinationCheckConnection",
  "description": "Run check connection for a given destination configuration",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/DestinationCoreConfig"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "DestinationCoreConfig": {
        "properties": {
          "connectionConfiguration": {
            "$ref": "#/$defs/DestinationConfiguration"
          },
          "destinationDefinitionId": {
            "$ref": "#/$defs/DestinationDefinitionId"
          },
          "destinationId": {
            "$ref": "#/$defs/DestinationId"
          },
          "workspaceId": {
            "$ref": "#/$defs/WorkspaceId"
          }
        },
        "required": [
          "workspaceId",
          "destinationDefinitionId",
          "connectionConfiguration"
        ],
        "type": "object"
      },
      "DestinationConfiguration": {
        "description": "The values required to configure the destination. The schema for this must match the schema return by destination_definition_specifications/get for the destinationDefinition.",
        "example": {
          "user": "charles"
        }
      },
      "DestinationDefinitionId": {
        "format": "uuid",
        "type": "string"
      },
      "DestinationId": {
        "format": "uuid",
        "type": "string"
      },
      "WorkspaceId": {
        "format": "uuid",
        "type": "string"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://airbyte.local/v1/scheduler/destinations/check_connection"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**jobInfo**",
              {
                "$ul": [
                  "**configId**: only present if a config id was provided.",
                  "**configType**",
                  "**connectorConfigurationUpdated** (default: false)",
                  "**createdAt**",
                  "**endedAt**",
                  "**id**",
                  [
                    "**logs**",
                    {
                      "$ul": [
                        "**logLines**"
                      ]
                    }
                  ],
                  "**succeeded**"
                ]
              }
            ],
            "**message**",
            "**status**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "422": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**exceptionClassName**",
            "**exceptionStack**",
            "**message**",
            "**validationErrors**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
