{
  "name": "getAttemptNormalizationStatusesForJob",
  "description": "Get normalization status to determine if we can bypass normalization phase",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/JobIdRequestBody"
      }
    },
    "$defs": {
      "JobIdRequestBody": {
        "properties": {
          "id": {
            "$ref": "#/$defs/JobId"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "JobId": {
        "format": "int64",
        "type": "integer"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://airbyte.local/v1/jobs/get_normalization_status"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**attemptNormalizationStatuses**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
