{
  "name": "delVideoPlaylistVideo",
  "description": "Delete an element from a playlist",
  "parameters": {
    "type": "object",
    "properties": {
      "playlistId": {
        "$ref": "#/components/schemas/VideoPlaylist/properties/id",
        "description": "Playlist id"
      },
      "playlistElementId": {
        "$ref": "#/components/schemas/id",
        "description": "Playlist element id"
      }
    },
    "required": [
      "playlistId",
      "playlistElementId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://peertube2.cpy.re/api/v1/video-playlists/{playlistId}/videos/{playlistElementId}"
    }
  },
  "responses": {}
}
