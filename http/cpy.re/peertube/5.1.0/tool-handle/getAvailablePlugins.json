{
  "name": "getAvailablePlugins",
  "description": "List available plugins",
  "parameters": {
    "type": "object",
    "properties": {
      "search": {
        "type": "string"
      },
      "pluginType": {
        "type": "integer"
      },
      "currentPeerTubeEngine": {
        "type": "string"
      },
      "start": {
        "minimum": 0,
        "type": "integer",
        "description": "Offset used to paginate results"
      },
      "count": {
        "default": 15,
        "maximum": 100,
        "minimum": 1,
        "type": "integer",
        "description": "Number of items to return"
      },
      "sort": {
        "example": "-createdAt",
        "type": "string",
        "description": "Sort column"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://peertube2.cpy.re/api/v1/plugins/available{?search,pluginType,currentPeerTubeEngine,start,count,sort}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**data**",
            "**total**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
