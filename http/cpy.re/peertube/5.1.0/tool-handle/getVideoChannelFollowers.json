{
  "name": "getVideoChannelFollowers",
  "description": "List followers of a video channel",
  "parameters": {
    "type": "object",
    "properties": {
      "channelHandle": {
        "example": "my_username | my_username@example.com",
        "type": "string",
        "description": "The video channel handle"
      },
      "start": {
        "minimum": 0,
        "type": "integer",
        "description": "Offset used to paginate results"
      },
      "count": {
        "default": 15,
        "maximum": 100,
        "minimum": 1,
        "type": "integer",
        "description": "Number of items to return"
      },
      "sort": {
        "enum": [
          "createdAt"
        ],
        "type": "string",
        "description": "Sort followers by criteria"
      },
      "search": {
        "type": "string",
        "description": "Plain text search, applied to various parts of the model depending on endpoint"
      }
    },
    "required": [
      "channelHandle"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://peertube2.cpy.re/api/v1/video-channels/{channelHandle}/followers{?start,count,sort,search}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**data**",
            "**total**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
