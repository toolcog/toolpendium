{
  "name": "delMirroredVideo",
  "description": "Delete a mirror done on a video",
  "parameters": {
    "type": "object",
    "properties": {
      "redundancyId": {
        "type": "string",
        "description": "id of an existing redundancy on a video"
      }
    },
    "required": [
      "redundancyId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://peertube2.cpy.re/api/v1/server/redundancy/videos/{redundancyId}"
    }
  },
  "responses": {}
}
