{
  "name": "watchlistScreeningEntityHitList",
  "description": "List all hits for the entity watchlist screening.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/WatchlistScreeningEntityHitListRequest"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "WatchlistScreeningEntityHitListRequest": {
        "description": "Request input for listing hits for an entity watchlist screening",
        "properties": {
          "client_id": {
            "$ref": "#/$defs/APIClientID"
          },
          "cursor": {
            "$ref": "#/$defs/Cursor"
          },
          "entity_watchlist_screening_id": {
            "$ref": "#/$defs/EntityWatchlistScreeningID"
          },
          "secret": {
            "$ref": "#/$defs/APISecret"
          }
        },
        "required": [
          "entity_watchlist_screening_id"
        ],
        "type": "object"
      },
      "APIClientID": {
        "description": "Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body.",
        "title": "ClientID",
        "type": "string"
      },
      "Cursor": {
        "description": "An identifier that determines which page of results you receive.",
        "example": "eyJkaXJlY3Rpb24iOiJuZXh0Iiwib2Zmc2V0IjoiMTU5NDM",
        "nullable": true,
        "type": "string"
      },
      "EntityWatchlistScreeningID": {
        "description": "ID of the associated entity screening.",
        "example": "entscr_52xR9LKo77r1Np",
        "title": "EntityWatchlistScreeningID",
        "type": "string"
      },
      "APISecret": {
        "description": "Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body.",
        "title": "APISecret",
        "type": "string"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://production.plaid.com/watchlist_screening/entity/hit/list"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Paginated list of entity watchlist screening hits",
        "**Key properties:**",
        {
          "$ul": [
            "**entity_watchlist_screening_hits**: List of entity watchlist screening hits",
            "**next_cursor**: An identifier that determines which page of results you receive.",
            "**request_id**: A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
