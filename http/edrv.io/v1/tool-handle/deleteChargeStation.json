{
  "name": "deleteChargeStation",
  "description": "Use to delete a charge station",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The charge station id that needs to be deleted"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api.edrv.io/v1/chargestations/{id}"
    }
  },
  "responses": {}
}
