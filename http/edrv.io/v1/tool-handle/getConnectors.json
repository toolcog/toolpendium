{
  "name": "getConnectors",
  "description": "List connectors",
  "parameters": {
    "type": "object",
    "properties": {
      "paginate_limit": {
        "default": 20,
        "maximum": 1000,
        "minimum": 1,
        "type": "integer",
        "description": "Number of results per page"
      },
      "paginate_page": {
        "type": "string",
        "description": "The queried page index"
      },
      "paginate_enabled": {
        "default": true,
        "type": "boolean",
        "description": "Enable pagination"
      },
      "sort_by": {
        "default": "createdAt",
        "type": "string",
        "description": "Sort data by this key"
      },
      "sort_order": {
        "default": "desc",
        "enum": [
          "desc",
          "asc"
        ],
        "type": "string",
        "description": "asc to sort ascending (default is desc - descending)"
      },
      "createdAt[$gte]": {
        "format": "date-time",
        "type": "string",
        "description": "Date as ISO String"
      },
      "createdAt[$lte]": {
        "format": "date-time",
        "type": "string",
        "description": "Date as ISO String"
      },
      "updatedAt[$gte]": {
        "format": "date-time",
        "type": "string",
        "description": "Date as ISO String"
      },
      "updatedAt[$lte]": {
        "format": "date-time",
        "type": "string",
        "description": "Date as ISO String"
      },
      "include_evse": {
        "type": "boolean",
        "description": "Populate evse"
      },
      "include_organization": {
        "type": "boolean",
        "description": "Populate organization"
      },
      "include_rate": {
        "type": "boolean",
        "description": "Populate rate"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api.edrv.io/v1/connectors{?paginate_limit,paginate_page,paginate_enabled,sort_by,sort_order,createdAt[$gte],createdAt[$lte],updatedAt[$gte],updatedAt[$lte],include_evse,include_organization,include_rate}"
    }
  },
  "responses": {}
}
