{
  "name": "getTransactionCost",
  "description": "Get a specific transaction's cost",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The transaction id that needs to be fetched"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api.edrv.io/v1/transactions/{id}/cost"
    }
  },
  "responses": {}
}
