{
  "name": "getVehicleBattery",
  "description": "Get a vehicle's battery",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The vehicle id that needs to be fetched"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api.edrv.io/v1/vehicles/{id}/battery"
    }
  },
  "responses": {}
}
