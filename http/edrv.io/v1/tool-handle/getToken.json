{
  "name": "getToken",
  "description": "Get a single token's data",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The token id that needs to be fetched"
      },
      "include_driver": {
        "type": "boolean",
        "description": "Populate driver"
      },
      "include_organization": {
        "type": "boolean",
        "description": "Populate organization"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api.edrv.io/v1/tokens/{id}{?include_driver,include_organization}"
    }
  },
  "responses": {}
}
