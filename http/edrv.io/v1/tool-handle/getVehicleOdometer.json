{
  "name": "getVehicleOdometer",
  "description": "Get a vehicle's odometer",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The vehicle id that needs to be fetched"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api.edrv.io/v1/vehicles/{id}/odometer"
    }
  },
  "responses": {}
}
