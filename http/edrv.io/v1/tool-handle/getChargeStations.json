{
  "name": "getChargeStations",
  "description": "List all Chargestations",
  "parameters": {
    "type": "object",
    "properties": {
      "organization": {
        "type": "string",
        "description": "Filter by Org. Id"
      },
      "location": {
        "type": "string",
        "description": "Filter by Location Id"
      },
      "online": {
        "type": "boolean",
        "description": "Filter by Online Status"
      },
      "active": {
        "type": "boolean",
        "description": "Chargestations that have been activated/deactivated by the admin"
      },
      "public": {
        "type": "boolean",
        "description": "Chargestations that are public"
      },
      "paginate_limit": {
        "default": 20,
        "maximum": 1000,
        "minimum": 1,
        "type": "integer",
        "description": "Number of results per page"
      },
      "paginate_page": {
        "type": "string",
        "description": "The queried page index"
      },
      "paginate_enabled": {
        "default": true,
        "type": "boolean",
        "description": "Enable pagination"
      },
      "sort_by": {
        "default": "createdAt",
        "type": "string",
        "description": "Sort data by this key"
      },
      "sort_order": {
        "default": "desc",
        "enum": [
          "desc",
          "asc"
        ],
        "type": "string",
        "description": "asc to sort ascending (default is desc - descending)"
      },
      "createdAt[$gte]": {
        "format": "date-time",
        "type": "string",
        "description": "Date as ISO String"
      },
      "createdAt[$lte]": {
        "format": "date-time",
        "type": "string",
        "description": "Date as ISO String"
      },
      "updatedAt[$gte]": {
        "format": "date-time",
        "type": "string",
        "description": "Date as ISO String"
      },
      "updatedAt[$lte]": {
        "format": "date-time",
        "type": "string",
        "description": "Date as ISO String"
      },
      "include_location": {
        "type": "boolean",
        "description": "Populate location"
      },
      "include_evses": {
        "type": "boolean",
        "description": "Populate evses"
      },
      "include_organization": {
        "type": "boolean",
        "description": "Populate organization"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api.edrv.io/v1/chargestations{?organization,location,online,active,public,paginate_limit,paginate_page,paginate_enabled,sort_by,sort_order,createdAt[$gte],createdAt[$lte],updatedAt[$gte],updatedAt[$lte],include_location,include_evses,include_organization}"
    }
  },
  "responses": {}
}
