{
  "name": "deleteConnector",
  "description": "Delete a connector",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The connector id that needs to be deleted"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api.edrv.io/v1/connectors/{id}"
    }
  },
  "responses": {}
}
