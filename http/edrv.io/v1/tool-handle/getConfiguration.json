{
  "name": "getConfiguration",
  "description": "Get one Configuration data",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of Configuration that needs to be fetched"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api.edrv.io/v1/configurations/{id}"
    }
  },
  "responses": {}
}
