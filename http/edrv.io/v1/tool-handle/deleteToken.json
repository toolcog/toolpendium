{
  "name": "deleteToken",
  "description": "Use to delete a token",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The token id that needs to be deleted"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//api.edrv.io/v1/tokens/{id}"
    }
  },
  "responses": {}
}
