{
  "name": "getRealtime",
  "description": "Use to request a Websockets handshake",
  "parameters": {
    "type": "object",
    "properties": {
      "sec-websocket-protocol": {
        "type": "string",
        "description": "The JWT token to use for auth"
      }
    },
    "required": [
      "sec-websocket-protocol"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//api.edrv.io/v1/realtime"
    },
    "headers": {
      "sec-websocket-protocol": {
        "$": "sec-websocket-protocol"
      }
    }
  },
  "responses": {}
}
