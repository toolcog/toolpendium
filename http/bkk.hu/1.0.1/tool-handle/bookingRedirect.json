{
  "name": "bookingRedirect",
  "parameters": {
    "type": "object",
    "properties": {
      "routeId": {
        "type": "string",
        "description": "A járat azonosítója."
      },
      "directionId": {
        "type": "string",
        "description": "A járat íránya."
      },
      "tripId": {
        "type": "string",
        "description": "A menet azonosítója."
      },
      "serviceDate": {
        "type": "string",
        "description": "A menet üzem napja."
      },
      "boardStopId": {
        "type": "string",
        "description": "A felszállási megálló azonosítója."
      },
      "alightStopId": {
        "type": "string",
        "description": "A leszállási megálló azonosítója."
      },
      "version": {
        "$ref": "#/components/schemas/ApiVersion",
        "description": "Az API verziója."
      },
      "appVersion": {
        "type": "string",
        "description": "A kliensalkalmazás verziója."
      },
      "dialect": {
        "$ref": "#/components/schemas/Dialect",
        "description": "Az API referenciáinak dialektusa."
      }
    },
    "required": [
      "dialect"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://futar.bkk.hu/api/query/v1/ws/{dialect}/api/where/booking-redirect{?routeId,directionId,tripId,serviceDate,boardStopId,alightStopId,version,appVersion}"
    }
  },
  "responses": {}
}
