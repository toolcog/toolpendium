{
  "name": "detectMultipart",
  "description": "Returns labels, probabilities, and bounding box coordinates for items detected in the specified local image file.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/ObjectDetectionRequest"
      }
    },
    "$defs": {
      "ObjectDetectionRequest": {
        "properties": {
          "modelId": {
            "description": "ID of the model that makes the detection.",
            "example": "YCQ4ZACEPJFGXZNRA6ERF3GL5E",
            "type": "string"
          },
          "sampleBase64Content": {
            "description": "The image contained in a base64 string.",
            "example": "SomeBase64EncodedImage",
            "type": "string"
          },
          "sampleId": {
            "description": "String that you can pass in to tag the prediction. Optional. Can be any value, and is returned in the response.",
            "type": "string"
          },
          "sampleLocation": {
            "description": "URL of the image file.",
            "type": "string"
          }
        },
        "required": [
          "modelId"
        ],
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://salesforce.local/v2/vision/detect"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**object**",
            "**probabilities**",
            "**sampleId**: Value passed in when the prediction call was made. Returned only if the sampleId request parameter is provided."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
