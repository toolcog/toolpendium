{
  "name": "getExamplesByLabel_1",
  "description": "Returns all the examples for the specified label. Returns both uploaded examples and feedback examples.",
  "parameters": {
    "type": "object",
    "properties": {
      "labelId": {
        "type": "string",
        "description": "Label Id"
      },
      "offset": {
        "default": "0",
        "type": "string",
        "description": "Index of the example from which you want to start paging."
      },
      "count": {
        "default": "100",
        "type": "string",
        "description": "Number of examples to return."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://salesforce.local/v2/vision/examples{?labelId,offset,count}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**data**",
            "**object**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
