{
  "name": "revokeRefreshTokenV2",
  "description": "Delete a Refresh Token",
  "parameters": {
    "type": "object",
    "properties": {
      "token": {
        "type": "string",
        "description": "the token to revoke"
      }
    },
    "required": [
      "token"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "http://salesforce.local/v2/oauth2/tokens/{token}"
    }
  },
  "responses": {}
}
