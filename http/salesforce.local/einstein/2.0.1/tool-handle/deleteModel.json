{
  "name": "deleteModel",
  "description": "Deletes the specified model.",
  "parameters": {
    "type": "object",
    "properties": {
      "modelId": {
        "type": "string",
        "description": "Model Id"
      }
    },
    "required": [
      "modelId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "http://salesforce.local/v2/language/models/{modelId}"
    }
  },
  "responses": {
    "201": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**deletedObjectId**",
            "**id**",
            "**message**",
            "**object**",
            "**organizationId**",
            "**progress**",
            "**status**",
            "**type**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
