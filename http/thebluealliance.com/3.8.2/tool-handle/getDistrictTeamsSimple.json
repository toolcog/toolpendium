{
  "name": "getDistrictTeamsSimple",
  "description": "Gets a short-form list of `Team` objects that competed in events in the given district.",
  "parameters": {
    "type": "object",
    "properties": {
      "If-None-Match": {
        "type": "string",
        "description": "Value of the `ETag` header in the most recently cached response by the client."
      },
      "district_key": {
        "type": "string",
        "description": "TBA District Key, eg `2016fim`"
      }
    },
    "required": [
      "district_key"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://www.thebluealliance.com/api/v3/district/{district_key}/teams/simple"
    },
    "headers": {
      "If-None-Match": {
        "$": "If-None-Match"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**city**: City of team derived from parsing the address registered with FIRST.",
            "**country**: Country of team derived from parsing the address registered with FIRST.",
            "**key**: TBA team key with the format `frcXXXX` with `XXXX` representing the team number.",
            "**name**: Official long name registered with FIRST.",
            "**nickname**: Team nickname provided by FIRST.",
            "**state_prov**: State of team derived from parsing the address registered with FIRST.",
            "**team_number**: Official team number issued by FIRST."
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.name"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
