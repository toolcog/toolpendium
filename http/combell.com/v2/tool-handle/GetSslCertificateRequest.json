{
  "name": "GetSslCertificateRequest",
  "description": "Detail of a SSL certificate request",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "description": "The id of the certificate request.",
        "format": "int32",
        "type": "integer"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/v2/sslcertificaterequests/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**certificate_type**: The type of the certificate:\r",
            "**common_name**: The common name of the certificate request.",
            "**id**: The id of the certificate request.",
            "**order_code**: The order code of the certificate request.",
            "**subject_alt_names**: The list of all supported domains in the certificate.",
            "**validation_level**: The validation level of the certificate:\r",
            "**validations**: The list of dns names to be validated with the information related to domain verification.",
            "**vendor**: The vendor of the certificate."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
