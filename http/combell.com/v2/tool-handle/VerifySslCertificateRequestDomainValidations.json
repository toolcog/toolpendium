{
  "name": "VerifySslCertificateRequestDomainValidations",
  "description": "Verify the SSL certificate request domain validations",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "description": "The id of the certificate request.",
        "format": "int32",
        "type": "integer"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "/v2/sslcertificaterequests/{id}"
    }
  },
  "responses": {}
}
