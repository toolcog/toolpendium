{
  "name": "ListRooms",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "filter[date_created_at][eq]": {
        "example": "2021-04-25",
        "format": "date",
        "type": "string",
        "description": "ISO 8601 date for filtering rooms created on that date."
      },
      "filter[date_created_at][gte]": {
        "example": "2021-04-25",
        "format": "date",
        "type": "string",
        "description": "ISO 8601 date for filtering rooms created after that date."
      },
      "filter[date_created_at][lte]": {
        "example": "2021-04-25",
        "format": "date",
        "type": "string",
        "description": "ISO 8601 date for filtering rooms created before that date."
      },
      "filter[date_updated_at][eq]": {
        "example": "2021-04-25",
        "format": "date",
        "type": "string",
        "description": "ISO 8601 date for filtering rooms updated on that date."
      },
      "filter[date_updated_at][gte]": {
        "example": "2021-04-25",
        "format": "date",
        "type": "string",
        "description": "ISO 8601 date for filtering rooms updated after that date."
      },
      "filter[date_updated_at][lte]": {
        "example": "2021-04-25",
        "format": "date",
        "type": "string",
        "description": "ISO 8601 date for filtering rooms updated before that date."
      },
      "filter[unique_name]": {
        "example": "my_video_room",
        "type": "string",
        "description": "Unique_name for filtering rooms."
      },
      "include_sessions": {
        "example": true,
        "type": "boolean",
        "description": "To decide if room sessions should be included in the response."
      },
      "page[size]": {
        "default": 20,
        "maximum": 250,
        "minimum": 1,
        "type": "integer",
        "description": "The size of the page."
      },
      "page[number]": {
        "default": 1,
        "minimum": 1,
        "type": "integer",
        "description": "The page number to load."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.telnyx.com/v2/rooms{?filter[date_created_at][eq],filter[date_created_at][gte],filter[date_created_at][lte],filter[date_updated_at][eq],filter[date_updated_at][gte],filter[date_updated_at][lte],filter[unique_name],include_sessions,page[size],page[number]}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**data**",
            [
              "**meta**",
              {
                "$ul": [
                  "**page_number**",
                  "**page_size**",
                  "**total_pages**",
                  "**total_results**"
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
