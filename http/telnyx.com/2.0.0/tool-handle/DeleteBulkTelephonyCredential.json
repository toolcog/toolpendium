{
  "name": "DeleteBulkTelephonyCredential",
  "description": "Delete several credentials in bulk.",
  "parameters": {
    "type": "object",
    "properties": {
      "filter[tag]": {
        "type": "string",
        "description": "Filter by tag, required by bulk operations."
      }
    },
    "required": [
      "filter[tag]"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.telnyx.com/v2/actions/bulk/telephony_credentials{?filter[tag]}"
    }
  },
  "responses": {
    "201": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Bulk Credential Response"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**data**",
              {
                "$ul": [
                  "**credentials**: Amount of credentials affected"
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
