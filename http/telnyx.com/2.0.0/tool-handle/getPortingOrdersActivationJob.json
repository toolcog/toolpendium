{
  "name": "getPortingOrdersActivationJob",
  "description": "Returns a porting activation job.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "uuid",
        "type": "string",
        "description": "Porting Order id"
      },
      "activationJobId": {
        "format": "uuid",
        "type": "string",
        "description": "Activation Job Identifier"
      }
    },
    "required": [
      "id",
      "activationJobId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.telnyx.com/v2/porting_orders/{id}/activation_jobs/{activationJobId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**data**",
              {
                "$ul": [
                  "**created_at**: ISO 8601 formatted date indicating when the resource was created.",
                  "**id**: Uniquely identifies this activation job",
                  "**record_type**: Identifies the type of the resource.",
                  "**status**: Specifies the status of this activation job",
                  "**updated_at**: ISO 8601 formatted date indicating when the resource was created."
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
