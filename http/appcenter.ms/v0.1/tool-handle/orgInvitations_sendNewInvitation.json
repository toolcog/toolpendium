{
  "name": "orgInvitations_sendNewInvitation",
  "description": "Cancels an existing organization invitation for the user and sends a new one",
  "parameters": {
    "type": "object",
    "properties": {
      "org_name": {
        "type": "string",
        "description": "The organization's name"
      },
      "email": {
        "type": "string",
        "description": "The email address of the user to send the password reset mail to."
      },
      "body": {
        "properties": {
          "role": {
            "description": "The role of the user to be added",
            "enum": [
              "admin",
              "collaborator",
              "member"
            ],
            "type": "string"
          }
        },
        "description": "The role of the user to be added"
      }
    },
    "required": [
      "org_name",
      "email"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.appcenter.ms/v0.1/orgs/{org_name}/invitations/{email}/resend"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**error**",
              {
                "$ul": [
                  "**code**",
                  "**message**"
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
