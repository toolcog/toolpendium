{
  "name": "test_deleteTestSeries",
  "description": "Deletes a single test series",
  "parameters": {
    "type": "object",
    "properties": {
      "test_series_slug": {
        "type": "string",
        "description": "The slug of the test series"
      },
      "owner_name": {
        "type": "string",
        "description": "The name of the owner"
      },
      "app_name": {
        "type": "string",
        "description": "The name of the application"
      }
    },
    "required": [
      "test_series_slug",
      "owner_name",
      "app_name"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.appcenter.ms/v0.1/apps/{owner_name}/{app_name}/test_series/{test_series_slug}"
    }
  },
  "responses": {}
}
