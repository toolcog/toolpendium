{
  "name": "releases_getSparkleFeed",
  "description": "Gets the sparkle feed of the releases that are distributed to all the public distribution groups.",
  "parameters": {
    "type": "object",
    "properties": {
      "app_secret": {
        "type": "string",
        "description": "The secret of the application."
      }
    },
    "required": [
      "app_secret"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.appcenter.ms/v0.1/public/sparkle/apps/{app_secret}"
    }
  },
  "responses": {
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**code**",
            "**message**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
