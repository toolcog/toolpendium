{
  "name": "Errors_AvailableVersions",
  "description": "Get all available versions in the time range.",
  "parameters": {
    "type": "object",
    "properties": {
      "start": {
        "format": "date-time",
        "type": "string",
        "description": "Start date time in data in ISO 8601 date time format"
      },
      "end": {
        "format": "date-time",
        "type": "string",
        "description": "Last date time in data in ISO 8601 date time format"
      },
      "$top": {
        "default": 30,
        "format": "int64",
        "maximum": 1000,
        "minimum": 0,
        "type": "integer",
        "description": "The maximum number of results to return. (0 will fetch all results till the max number.)"
      },
      "$skip": {
        "default": 0,
        "format": "int64",
        "minimum": 0,
        "type": "integer",
        "description": "The offset (starting at 0) of the first result to return. This parameter along with limit is used to perform pagination."
      },
      "$filter": {
        "format": "string",
        "type": "string",
        "description": "A filter as specified in https://github.com/Microsoft/api-guidelines/blob/master/Guidelines.md#97-filtering."
      },
      "$inlinecount": {
        "default": "none",
        "enum": [
          "allpages",
          "none"
        ],
        "type": "string",
        "description": "Controls whether or not to include a count of all the items across all pages."
      },
      "errorType": {
        "enum": [
          "all",
          "unhandledError",
          "handledError"
        ],
        "type": "string",
        "description": "Type of error (handled vs unhandled), including All"
      },
      "owner_name": {
        "type": "string",
        "description": "The name of the owner"
      },
      "app_name": {
        "type": "string",
        "description": "The name of the application"
      }
    },
    "required": [
      "start",
      "owner_name",
      "app_name"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.appcenter.ms/v0.1/apps/{owner_name}/{app_name}/errors/available_versions{?start,end,$top,$skip,$filter,$inlinecount,errorType}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**total_count**: The full number of versions across all pages.",
            "**versions**: List of available versions."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**error**",
              {
                "$ul": [
                  "**code**",
                  "**message**"
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
