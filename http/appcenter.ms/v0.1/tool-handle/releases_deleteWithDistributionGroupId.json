{
  "name": "releases_deleteWithDistributionGroupId",
  "description": "Deletes a release with id 'release_id' in a given distribution group.",
  "parameters": {
    "type": "object",
    "properties": {
      "owner_name": {
        "type": "string",
        "description": "The name of the app owner"
      },
      "app_name": {
        "type": "string",
        "description": "The name of the app"
      },
      "distribution_group_name": {
        "type": "string",
        "description": "The name of the distribution group."
      },
      "release_id": {
        "type": "integer",
        "description": "The ID identifying the unique release."
      }
    },
    "required": [
      "owner_name",
      "app_name",
      "distribution_group_name",
      "release_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.appcenter.ms/v0.1/apps/{owner_name}/{app_name}/distribution_groups/{distribution_group_name}/releases/{release_id}"
    }
  },
  "responses": {}
}
