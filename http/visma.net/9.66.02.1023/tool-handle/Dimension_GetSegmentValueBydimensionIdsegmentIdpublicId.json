{
  "name": "Dimension_GetSegmentValueBydimensionIdsegmentIdpublicId",
  "description": "Get a specific SegmentValue for a specific Segment for a specific Dimension",
  "parameters": {
    "type": "object",
    "properties": {
      "dimensionId": {
        "type": "string",
        "description": "Identifies the Dimension"
      },
      "segmentId": {
        "format": "int32",
        "type": "integer",
        "description": "Identifies the Segment"
      },
      "publicId": {
        "format": "uuid",
        "type": "string",
        "description": "Identifies the SegmentValue by its publicId"
      },
      "erp-api-background": {
        "type": "string",
        "description": "If a URL value is provided, then API endpoint will be queued and executed in background. When the execution of the background operation is finished, the system will POST the response to the specified URL. The endpoint itself responds in this case with a 202-Accepted status and a reference to a state document for the background operation."
      }
    },
    "required": [
      "dimensionId",
      "segmentId",
      "publicId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/API/controller/api/v1/dimension/{dimensionId}/{segmentId}/publicid/{publicId}"
    },
    "headers": {
      "erp-api-background": {
        "$": "erp-api-background"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**active**: The table &gt; Active &gt; A check box that indicates (if selected) that the value is active.",
            "**description**: The table &gt; Description &gt; The user-defined segment description.",
            "**lastModified**",
            "**mappedSegValue**",
            "**publicId**",
            "**segmentId**: The top part &gt; Segment ID &gt; The system-generated number representing the order of the segment in the segmented key.",
            "**timeStamp**",
            "**valueId**: The table &gt; Value &gt; The string that is the value of the selected segment."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "202": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**id**",
            "**stateLocation**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
