{
  "name": "get_node_resource",
  "description": "A node is an abstract representation of some kind of entity. The entity may be a physical thing such as a patient,\na molecular entity such as a gene or protein, or a conceptual entity such as a class from an ontology.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "CURIE e.g. HP:0000465"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/graph/node/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**category**",
            "**id**: ID or CURIE e.g. MGI:1201606",
            "**iri**: IRI",
            "**label**: RDFS Label",
            "**consider**",
            "**deprecated**: True if the node is deprecated/obsoleted.",
            "**description**: Descriptive text for the entity. For ontology classes, this will be a definition.",
            "**replaced_by**",
            "**synonyms**: list of synonyms or alternate labels",
            "**types**",
            "**association_counts**: association counts",
            [
              "**taxon**",
              {
                "$ul": [
                  "**id**: CURIE ID, e.g. NCBITaxon:9606",
                  "**label**: RDFS Label"
                ]
              }
            ],
            "**xrefs**: Database cross-references. These are usually CURIEs, but may also be URLs. E.g. ENSEMBL:ENSG00000099940 "
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.id"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
