{
  "name": "get_prefix_expand",
  "description": "Returns expanded URI",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of entity to be contracted to URI, e.g \"MGI:1\""
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/identifier/prefixes/expand/{id}"
    }
  },
  "responses": {}
}
