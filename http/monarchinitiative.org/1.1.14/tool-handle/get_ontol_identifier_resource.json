{
  "name": "get_ontol_identifier_resource",
  "description": "Fetches a map from CURIEs/IDs to labels",
  "parameters": {
    "type": "object",
    "properties": {
      "label": {
        "items": {
          "type": "string"
        },
        "type": "array",
        "description": "List of labels"
      }
    },
    "required": [
      "label"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/ontol/identifier/{?label}"
    }
  },
  "responses": {}
}
