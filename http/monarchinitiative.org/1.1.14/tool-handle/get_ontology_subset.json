{
  "name": "get_ontology_subset",
  "description": "Returns meta data of an ontology subset (slim)",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "name of a slim subset, e.g. goslim_agr, goslim_generic"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/ontology/subset/{id}"
    }
  },
  "responses": {}
}
