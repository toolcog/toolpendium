{
  "name": "get_model_property_values",
  "description": "Returns list property-values for all models",
  "parameters": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "string to search for in title of model"
      },
      "contributor": {
        "type": "string",
        "description": "string to search for in contributor of model"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/cam/model/property_values{?title,contributor}"
    }
  },
  "responses": {}
}
