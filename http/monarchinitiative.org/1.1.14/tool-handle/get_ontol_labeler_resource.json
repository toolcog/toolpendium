{
  "name": "get_ontol_labeler_resource",
  "description": "Fetches a map from CURIEs/IDs to labels",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "items": {
          "type": "string"
        },
        "type": "array",
        "description": "List of ids"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/ontol/labeler/{?id}"
    }
  },
  "responses": {}
}
