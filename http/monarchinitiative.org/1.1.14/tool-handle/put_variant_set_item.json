{
  "name": "put_variant_set_item",
  "description": "Updates a variant set",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string"
      },
      "body": {
        "$ref": "#/$defs/variant_set"
      }
    },
    "required": [
      "id",
      "body"
    ],
    "$defs": {
      "variant_set": {
        "properties": {
          "body": {
            "description": "Article content",
            "type": "string"
          },
          "category": {
            "type": "string"
          },
          "category_id": {
            "type": "integer"
          },
          "id": {
            "description": "The unique identifier of a variant set",
            "type": "integer"
          },
          "pub_date": {
            "format": "date-time",
            "type": "string"
          },
          "title": {
            "description": "Article title",
            "type": "string"
          }
        },
        "required": [
          "body",
          "title"
        ],
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "/api/variation/set/{id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
