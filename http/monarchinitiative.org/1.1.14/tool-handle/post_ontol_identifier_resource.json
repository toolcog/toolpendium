{
  "name": "post_ontol_identifier_resource",
  "description": "Takes 'label' list argument either as a querystring argument or as a key\nin the POST body when content-type is application/json.",
  "parameters": {
    "type": "object",
    "properties": {
      "label": {
        "items": {
          "type": "string"
        },
        "type": "array",
        "description": "List of labels"
      }
    },
    "required": [
      "label"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "/api/ontol/identifier/{?label}"
    }
  },
  "responses": {}
}
