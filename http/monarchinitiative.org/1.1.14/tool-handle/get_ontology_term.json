{
  "name": "get_ontology_term",
  "description": "Returns meta data of an ontology term",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "CURIE identifier of a GO term, e.g. GO:0003677"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/ontology/term/{id}"
    }
  },
  "responses": {}
}
