{
  "name": "get_variant_sets_archive_collection",
  "description": "Returns list of variant sets from a specified time period",
  "parameters": {
    "type": "object",
    "properties": {
      "page": {
        "default": 1,
        "type": "integer",
        "description": "Page number"
      },
      "per_page": {
        "default": 10,
        "enum": [
          2,
          10,
          20,
          30,
          40,
          50
        ],
        "type": "integer",
        "description": "Results per page {error_msg}"
      },
      "year": {
        "type": "integer"
      },
      "month": {
        "type": "integer"
      },
      "day": {
        "type": "integer"
      }
    },
    "required": [
      "year",
      "month",
      "day"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/variation/set/archive/{year}/{month}/{day}{?page,per_page}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**page**: Number of this page of results",
            "**pages**: Total number of pages of results",
            "**per_page**: Number of items per page of results",
            "**total**: Total number of results",
            "**items**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
