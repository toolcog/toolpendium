{
  "name": "get_ontology_term_subsets",
  "description": "Returns subsets (slims) associated to an ontology term",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "CURIE identifier of a GO term, e.g. GO:0006259"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/ontology/term/{id}/subsets"
    }
  },
  "responses": {}
}
