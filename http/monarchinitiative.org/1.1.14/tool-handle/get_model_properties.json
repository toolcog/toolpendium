{
  "name": "get_model_properties",
  "description": "Returns list of all properties used across all models",
  "parameters": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "string to search for in title of model"
      },
      "contributor": {
        "type": "string",
        "description": "string to search for in contributor of model"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/cam/model/properties{?title,contributor}"
    }
  },
  "responses": {}
}
