{
  "name": "get_ontology_terms_shared_ancestor",
  "description": "Returns the ancestor ontology terms shared by two ontology terms",
  "parameters": {
    "type": "object",
    "properties": {
      "subject": {
        "type": "string",
        "description": "CURIE identifier of a GO term, e.g. GO:0006259"
      },
      "object": {
        "type": "string",
        "description": "CURIE identifier of a GO term, e.g. GO:0046483"
      }
    },
    "required": [
      "subject",
      "object"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/ontology/shared/{subject}/{object}"
    }
  },
  "responses": {}
}
