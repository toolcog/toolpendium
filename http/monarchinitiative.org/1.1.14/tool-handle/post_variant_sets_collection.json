{
  "name": "post_variant_sets_collection",
  "description": "Creates a new variant set",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/variant_set"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "variant_set": {
        "properties": {
          "body": {
            "description": "Article content",
            "type": "string"
          },
          "category": {
            "type": "string"
          },
          "category_id": {
            "type": "integer"
          },
          "id": {
            "description": "The unique identifier of a variant set",
            "type": "integer"
          },
          "pub_date": {
            "format": "date-time",
            "type": "string"
          },
          "title": {
            "description": "Article title",
            "type": "string"
          }
        },
        "required": [
          "body",
          "title"
        ],
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "/api/variation/set/"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
