{
  "name": "get_activity_collection",
  "description": "Returns list of models",
  "parameters": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "string to search for in title of model"
      },
      "contributor": {
        "type": "string",
        "description": "string to search for in contributor of model"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/cam/activity{?title,contributor}"
    }
  },
  "responses": {}
}
