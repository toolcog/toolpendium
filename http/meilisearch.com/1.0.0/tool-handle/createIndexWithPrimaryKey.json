{
  "name": "createIndexWithPrimaryKey",
  "description": "Create index with primary key",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "properties": {
          "primaryKey": {
            "example": "number",
            "type": "string"
          },
          "uid": {
            "example": "books",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://localhost:7700//indexes"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
