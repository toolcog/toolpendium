{
  "name": "updateIndex",
  "description": "Can only change the document identifier if it has not already been added before.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "properties": {
          "primaryKey": {
            "example": "title",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "PATCH",
    "url": {
      "$uri": "http://localhost:7700//indexes/books"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
