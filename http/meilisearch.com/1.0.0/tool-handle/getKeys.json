{
  "name": "getKeys",
  "description": "Get keys",
  "parameters": {
    "type": "object",
    "properties": {
      "offset": {
        "example": "0",
        "type": "string"
      },
      "limit": {
        "example": "10",
        "type": "string"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://localhost:7700//keys{?offset,limit}"
    }
  },
  "responses": {}
}
