{
  "name": "getDEP",
  "description": "Generates a DEP file.",
  "parameters": {
    "type": "object",
    "properties": {
      "registrierkasseUuid": {
        "type": "string",
        "description": "The `_uuid` of the `Registrierkasse` to retrieve the DEP file."
      }
    },
    "required": [
      "registrierkasseUuid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/v1/registrierkassen/{registrierkasseUuid}/dep"
    }
  },
  "responses": {}
}
