{
  "name": "createIncomingWebhook",
  "description": "Create a new webhook. Conversation must exist and creater has to be participant.\nOauthScopes: WRITE_CONVERSATIONS, MANAGE_CONVERSATIONS",
  "parameters": {
    "type": "object",
    "properties": {
      "conversationId": {
        "maxLength": 2048,
        "type": "string",
        "description": "The id of the conversation."
      },
      "name": {
        "maxLength": 2048,
        "type": "string",
        "description": "The name of the webhook"
      },
      "userId": {
        "maxLength": 2048,
        "type": "string",
        "description": "The id of the user of the webhook"
      },
      "description": {
        "maxLength": 2048,
        "type": "string",
        "description": "A short description of the webhook"
      }
    },
    "required": [
      "conversationId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://circuitsandbox.net/rest/v2/webhooks/incoming/create/{conversationId}{?name,userId,description}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**conversationId**: The conversation where the posts go to",
            "**creationTime**: Milliseconds since 1.1.1900 when the hook was created",
            "**creatorId**: Th e user who created the webhook",
            "**description**: A short plain text to describe the webhook",
            "**modificationTime**: Milliseconds since 1.1.1900 when the hook was modified",
            "**name**: A name to identify the webhook",
            "**status**: Status may be ENABLED or DISABLED. A disabled hook cannot be used",
            "**tenantId**: The tenant where the conversation and users belong to",
            "**url**: The url where the webhokk can be used",
            "**userId**: The user who will occur as author of the posts",
            "**webhookId**: Unique id of the webhook"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
