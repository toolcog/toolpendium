{
  "name": "unlikeItem",
  "description": "Removes a \"like\" from the given message in the given conversation\nOauthScopes: WRITE_CONVERSATIONS, UPDATE_CONVERSATION_CONTENT",
  "parameters": {
    "type": "object",
    "properties": {
      "convId": {
        "maxLength": 2048,
        "type": "string",
        "description": "The ID of the conversation to which the item belongs"
      },
      "itemId": {
        "maxLength": 2048,
        "type": "string",
        "description": "The ID of the item that will be unliked"
      }
    },
    "required": [
      "convId",
      "itemId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://circuitsandbox.net/rest/v2/conversations/{convId}/messages/{itemId}/like"
    }
  },
  "responses": {}
}
