{
  "name": "getJoinDetails",
  "description": "Gets the conference details of the given conversation. Conference details include the URL, which is used to join the conference through a web or mobile application, as well as the dial-in phone numbers and conference PIN, which are used to join the conference by phone.\nOauthScopes: READ_CONVERSATIONS",
  "parameters": {
    "type": "object",
    "properties": {
      "convId": {
        "maxLength": 2048,
        "type": "string",
        "description": "The ID of the conversation for which the join details should be returned"
      }
    },
    "required": [
      "convId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://circuitsandbox.net/rest/v2/conversations/{convId}/conversationdetails"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**bridgeNumbers**: A list of bridge numbers which can be used to dial in to the real time session via phone",
            "**convId**: missing documentation",
            "**conversationCreatorId**: The user ID of the conversation creator",
            "**isModerationAllowed**: Indicates if the conversation is allowed to be moderated",
            "**isRecordingAllowed**: Indicates if the conversation is allowed to be recorded",
            "**link**: A link that can be used in a browser to join the session",
            "**pin**: The PIN to join a real time session via phone"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
