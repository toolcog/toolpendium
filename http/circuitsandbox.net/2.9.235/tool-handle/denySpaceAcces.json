{
  "name": "denySpaceAcces",
  "description": "Deny access for a space\nOauthScopes: WRITE_SPACE, MANAGE_SPACE",
  "parameters": {
    "type": "object",
    "properties": {
      "spaceId": {
        "maxLength": 2048,
        "type": "string",
        "description": "Id of the space"
      },
      "participantId": {
        "maxLength": 2048,
        "type": "string",
        "description": "Id of the participant"
      },
      "body": {
        "properties": {
          "reason": {
            "description": "Reason why the request has been denied",
            "maxLength": 2048,
            "type": "string"
          }
        },
        "type": "object"
      }
    },
    "required": [
      "spaceId",
      "participantId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://circuitsandbox.net/rest/v2/spaces/{spaceId}/participant/{participantId}/deny"
    },
    "body": {
      "$": "body",
      "encode": "urlencoded"
    }
  },
  "responses": {}
}
