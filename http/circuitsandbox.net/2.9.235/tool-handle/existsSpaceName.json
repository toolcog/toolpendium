{
  "name": "existsSpaceName",
  "description": "Find out if a space name already exists for non-secret spaces.\nOauthScopes: READ_SPACE",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "maxLength": 2048,
        "type": "string",
        "description": "The name to check for existence."
      }
    },
    "required": [
      "name"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://circuitsandbox.net/rest/v2/spaces/exists/{name}"
    }
  },
  "responses": {}
}
