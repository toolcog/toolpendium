{
  "name": "setUserPresence",
  "description": "Updates the presence status of the authenticated user.\nOauthScopes: WRITE_USER_PROFILE, MANAGE_PRESENCE",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "properties": {
          "clearDND": {
            "default": false,
            "description": "Clear the DND of the user.",
            "type": "boolean"
          },
          "dndUntil": {
            "description": "Timestamp until the DND state of the user is active. This field is mandatory when the state is set to DND.",
            "format": "date-time",
            "type": "string"
          },
          "state": {
            "description": "The user's presence.",
            "maxLength": 2048,
            "type": "string"
          },
          "statusMessage": {
            "description": "An optional status message that is displayed instead of the location",
            "maxLength": 2048,
            "type": "string"
          }
        },
        "required": [
          "state"
        ],
        "type": "object"
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://circuitsandbox.net/rest/v2/users/presence"
    },
    "body": {
      "$": "body",
      "encode": "urlencoded"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**dndUntil**: Timestamp until the DND state of the user is active",
            "**isOptedOut**: Indicates whether the user has opted out of their presence",
            "**latitude**: The latitude of the geo location of the user",
            "**locationText**: The geo location of the user in clear text",
            "**longitude**: The longitude of the geo location of the user",
            "**mobile**: Identifies whether the presence state is based on the state information of a mobile device or not",
            "**poor**: n/a",
            "**state**: The presence state of the user. It can be one of the following: AVAILABLE, OFFLINE, AWAY, BUSY, or DND (Do-Not-Disturb)",
            "**statusMessage**: A free formed status message of the user",
            "**timeZoneOffset**: The timezone offset of the user compared to UTC",
            "**userId**: The ID of the user"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
