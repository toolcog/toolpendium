{
  "name": "removeWebHook",
  "description": "Unregisters the webHook with the given ID.\nOauthScopes: READ_CONVERSATIONS, READ_USER",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "maxLength": 2048,
        "type": "string",
        "description": "The unique ID of the webHook to remove"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://circuitsandbox.net/rest/v2/webhooks/{id}"
    }
  },
  "responses": {}
}
