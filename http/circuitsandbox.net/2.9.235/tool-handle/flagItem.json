{
  "name": "flagItem",
  "description": "Adds a flag to the given message in the given conversation.\nOauthScopes: WRITE_CONVERSATIONS, ORGANIZE_CONVERSATIONS",
  "parameters": {
    "type": "object",
    "properties": {
      "convId": {
        "maxLength": 2048,
        "type": "string",
        "description": "The ID of the conversation to which the item belongs"
      },
      "itemId": {
        "maxLength": 2048,
        "type": "string",
        "description": "The ID of the item that will be flagged"
      },
      "body": {
        "properties": {
          "itemCreationTime": {
            "description": "The time when the item was created",
            "maxLength": 2048,
            "type": "string"
          },
          "parentId": {
            "description": "The ID of the item's parent",
            "maxLength": 2048,
            "type": "string"
          }
        },
        "type": "object"
      }
    },
    "required": [
      "convId",
      "itemId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://circuitsandbox.net/rest/v2/conversations/{convId}/messages/{itemId}/flag"
    },
    "body": {
      "$": "body",
      "encode": "urlencoded"
    }
  },
  "responses": {}
}
