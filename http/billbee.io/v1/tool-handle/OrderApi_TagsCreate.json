{
  "name": "OrderApi_TagsCreate",
  "description": "When a tag is already attached, it is ignored. Tags are not case sensitive. All given tags are added to the existing tags.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int64",
        "type": "integer",
        "description": "The internal id of the order"
      },
      "body": {
        "$ref": "#/components/schemas/Rechnungsdruck.WebApp.Controllers.Api.OrderTagCreate",
        "description": "Tags to attach"
      }
    },
    "required": [
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://app.billbee.io/api/v1/orders/{id}/tags"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
