{
  "name": "WebHookManagement_Put",
  "description": "Updates an existing WebHook registration.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The WebHook ID."
      },
      "body": {
        "$ref": "#/components/schemas/Rechnungsdruck.WebApp.Controllers.Api.WebHookApiModel",
        "description": "The new webhook to use."
      }
    },
    "required": [
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://app.billbee.io/api/v1/webhooks/{id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**Description**",
            "**Filters**",
            "**Headers**",
            "**Id**",
            "**IsPaused**",
            "**Properties**",
            "**Secret**",
            "**WebHookUri**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
