{
  "name": "deleteSubmissionComment",
  "description": "Delete a feedback comment to a submission",
  "parameters": {
    "type": "object",
    "properties": {
      "class": {
        "type": "string",
        "description": "Unique identifier of the class"
      },
      "assignment": {
        "type": "string",
        "description": "Unique identifier of the assignment"
      },
      "submission": {
        "type": "string",
        "description": "Unique identifier of the submission"
      },
      "comment": {
        "type": "string",
        "description": "Unique identifier of the comment"
      }
    },
    "required": [
      "class",
      "assignment",
      "submission",
      "comment"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.flat.io/v2/classes/{class}/assignments/{assignment}/submissions/{submission}/comments/{comment}"
    }
  },
  "responses": {
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "An API Error response",
        "**Key properties:**",
        {
          "$ul": [
            "**code**: A corresponding code for this error",
            "**id**: An unique error identifier generated for the request",
            "**message**: A printable message for this message",
            "**param**: The related parameter that caused the error"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
