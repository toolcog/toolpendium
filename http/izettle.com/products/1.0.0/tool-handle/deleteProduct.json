{
  "name": "deleteProduct",
  "description": "Delete a single product",
  "parameters": {
    "type": "object",
    "properties": {
      "organizationUuid": {
        "format": "uuid",
        "type": "string"
      },
      "productUuid": {
        "format": "uuid",
        "type": "string"
      }
    },
    "required": [
      "organizationUuid",
      "productUuid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://products.izettle.com/organizations/{organizationUuid}/products/{productUuid}"
    }
  },
  "responses": {}
}
