{
  "name": "deleteCategory",
  "description": "Delete a category",
  "parameters": {
    "type": "object",
    "properties": {
      "organizationUuid": {
        "format": "uuid",
        "type": "string"
      },
      "categoryUuid": {
        "format": "uuid",
        "type": "string"
      }
    },
    "required": [
      "organizationUuid",
      "categoryUuid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://products.izettle.com/organizations/{organizationUuid}/categories/v2/{categoryUuid}"
    }
  },
  "responses": {}
}
