{
  "name": "OrderApi_Delete",
  "description": "Delete an existing order",
  "parameters": {
    "type": "object",
    "properties": {
      "x-auth-key": {
        "default": "[authentication key]",
        "type": "string",
        "description": ""
      },
      "x-auth-secret": {
        "default": "[authentication secret]",
        "type": "string",
        "description": ""
      },
      "body": {
        "$ref": "#/$defs/OrderDeleteApiModel"
      }
    },
    "required": [
      "x-auth-key",
      "x-auth-secret",
      "body"
    ],
    "$defs": {
      "OrderDeleteApiModel": {
        "properties": {
          "Id": {
            "description": "Id of order to be deleted",
            "format": "int32",
            "type": "integer"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://www.envoice.in/api/order/delete"
    },
    "headers": {
      "x-auth-key": {
        "$": "x-auth-key"
      },
      "x-auth-secret": {
        "$": "x-auth-secret"
      }
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
