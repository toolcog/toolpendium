{
  "name": "WorkTypeApi_Update",
  "description": "Update an existing work type",
  "parameters": {
    "type": "object",
    "properties": {
      "x-auth-key": {
        "default": "[authentication key]",
        "type": "string",
        "description": ""
      },
      "x-auth-secret": {
        "default": "[authentication secret]",
        "type": "string",
        "description": ""
      },
      "body": {
        "$ref": "#/$defs/WorkTypeUpdateApiModel"
      }
    },
    "required": [
      "x-auth-key",
      "x-auth-secret",
      "body"
    ],
    "$defs": {
      "WorkTypeUpdateApiModel": {
        "properties": {
          "Id": {
            "description": "Entity id",
            "format": "int32",
            "type": "integer"
          },
          "Title": {
            "description": "Indicates the title of of the work type (Logo design, development...)",
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://www.envoice.in/api/worktype/update"
    },
    "headers": {
      "x-auth-key": {
        "$": "x-auth-key"
      },
      "x-auth-secret": {
        "$": "x-auth-secret"
      }
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
