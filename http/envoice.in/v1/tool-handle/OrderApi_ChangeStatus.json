{
  "name": "OrderApi_ChangeStatus",
  "description": "Change order status",
  "parameters": {
    "type": "object",
    "properties": {
      "x-auth-key": {
        "default": "[authentication key]",
        "type": "string",
        "description": ""
      },
      "x-auth-secret": {
        "default": "[authentication secret]",
        "type": "string",
        "description": ""
      },
      "body": {
        "$ref": "#/$defs/ChangeOrderStatusApiModel"
      }
    },
    "required": [
      "x-auth-key",
      "x-auth-secret",
      "body"
    ],
    "$defs": {
      "ChangeOrderStatusApiModel": {
        "properties": {
          "Id": {
            "description": "Order Id",
            "format": "int32",
            "type": "integer"
          },
          "Reason": {
            "description": "Reason for status change",
            "type": "string"
          },
          "Status": {
            "description": "New status of the order",
            "enum": [
              "PendingPayment",
              "Processing",
              "Shipped",
              "Completed",
              "OnHold",
              "Cancelled",
              "Refunded",
              "Failed"
            ],
            "type": "string"
          }
        },
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://www.envoice.in/api/order/changestatus"
    },
    "headers": {
      "x-auth-key": {
        "$": "x-auth-key"
      },
      "x-auth-secret": {
        "$": "x-auth-secret"
      }
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
