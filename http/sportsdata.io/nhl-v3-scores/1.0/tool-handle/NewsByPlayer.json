{
  "name": "NewsByPlayer",
  "description": "News by Player",
  "parameters": {
    "type": "object",
    "properties": {
      "format": {
        "default": "XML",
        "enum": [
          "XML",
          "JSON"
        ],
        "type": "string",
        "description": "Desired response format. Valid entries are <code>XML</code> or <code>JSON</code>."
      },
      "playerid": {
        "type": "string",
        "description": "Unique FantasyData Player ID.\n Example:<code>10000507</code>.\n "
      }
    },
    "required": [
      "format",
      "playerid"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://azure-api.sportsdata.io/v3/nhl/scores/{format}/NewsByPlayerID/{playerid}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**Content**",
            "**NewsID**",
            "**PlayerID**",
            "**Source**",
            "**Team**",
            "**TeamID**",
            "**TermsOfUse**",
            "**Title**",
            "**Updated**",
            "**Url**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": "Item"
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
