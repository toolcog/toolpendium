{
  "name": "Teams",
  "description": "Teams",
  "parameters": {
    "type": "object",
    "properties": {
      "format": {
        "default": "XML",
        "enum": [
          "XML",
          "JSON"
        ],
        "type": "string",
        "description": "Desired response format. Valid entries are <code>xml</code> or <code>json</code>."
      }
    },
    "required": [
      "format"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://azure-api.sportsdata.io/v3/cfb/scores/{format}/Teams"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**Active**",
            "**ApRank**",
            "**CoachesRank**",
            "**Conference**",
            "**ConferenceID**",
            "**ConferenceLosses**",
            "**ConferenceWins**",
            "**GlobalTeamID**",
            "**Key**",
            "**Losses**",
            "**Name**",
            "**PlayoffRank**",
            "**RankSeason**",
            "**RankSeasonType**",
            "**RankWeek**",
            "**School**",
            "**ShortDisplayName**",
            "**StadiumID**",
            "**TeamID**",
            "**TeamLogoUrl**",
            "**Wins**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": "Item"
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
