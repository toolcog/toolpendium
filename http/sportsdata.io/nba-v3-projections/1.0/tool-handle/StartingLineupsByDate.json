{
  "name": "StartingLineupsByDate",
  "description": "This endpoint provides the projected & confirmed starting lineups for NBA games on a given date.",
  "parameters": {
    "type": "object",
    "properties": {
      "format": {
        "default": "XML",
        "enum": [
          "XML",
          "JSON"
        ],
        "type": "string",
        "description": "Desired response format. Valid entries are <code>XML</code> or <code>JSON</code>."
      },
      "date": {
        "type": "string",
        "description": "The date of the game(s). <br> Examples: <code>2021-OCT-12</code>, <code>2021-DEC-09</code>."
      }
    },
    "required": [
      "format",
      "date"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://azure-api.sportsdata.io/v3/nba/projections/{format}/StartingLineupsByDate/{date}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**AwayLineup**",
            "**AwayTeam**",
            "**AwayTeamID**",
            "**DateTime**",
            "**Day**",
            "**GameID**",
            "**HomeLineup**",
            "**HomeTeam**",
            "**HomeTeamID**",
            "**Season**",
            "**SeasonType**",
            "**Status**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": "Item"
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
