{
  "name": "UpcomingDfsSlatesByCompetition",
  "description": "Upcoming Dfs Slates By Competition",
  "parameters": {
    "type": "object",
    "properties": {
      "format": {
        "enum": [
          "json",
          "xml"
        ],
        "type": "string",
        "description": "Desired response format. Valid entries are <code>XML</code> or <code>JSON</code>."
      },
      "competitionId": {
        "type": "string",
        "description": "The Competition Id.\n<br>Examples: <code>3</code>"
      }
    },
    "required": [
      "format",
      "competitionId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://azure-api.sportsdata.io/v3/soccer/projections/{format}/UpcomingDfsSlatesByCompetition/{competitionId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**CompetitionId**",
            "**DfsSlateGames**",
            "**DfsSlatePlayers**",
            "**IsMultiDaySlate**",
            "**NumberOfGames**",
            "**Operator**",
            "**OperatorDay**",
            "**OperatorGameType**",
            "**OperatorName**",
            "**OperatorSlateID**",
            "**OperatorStartTime**",
            "**RemovedByOperator**",
            "**SalaryCap**",
            "**SlateID**",
            "**SlateRosterSlots**"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": "Item"
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
