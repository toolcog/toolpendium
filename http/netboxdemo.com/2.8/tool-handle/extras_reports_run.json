{
  "name": "extras_reports_run",
  "description": "Run a Report and create a new ReportResult, overwriting any previous result for the Report.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://netboxdemo.com/api/extras/reports/{id}/run/"
    }
  },
  "responses": {}
}
