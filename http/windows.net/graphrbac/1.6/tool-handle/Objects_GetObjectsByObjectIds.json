{
  "name": "Objects_GetObjectsByObjectIds",
  "description": "Gets the directory objects specified in a list of object IDs. You can also specify which resource collections (users, groups, etc.) should be searched by specifying the optional types parameter.",
  "parameters": {
    "type": "object",
    "properties": {
      "api-version": {
        "type": "string",
        "description": "Client API version."
      },
      "tenantID": {
        "type": "string",
        "description": "The tenant ID."
      },
      "body": {
        "$ref": "#/components/schemas/GetObjectsParameters",
        "description": "Objects filtering parameters."
      }
    },
    "required": [
      "api-version",
      "tenantID",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://graph.windows.net/{tenantID}/getObjectsByObjectIds{?api-version}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "DirectoryObject list operation result.",
        "**Key properties:**",
        {
          "$ul": [
            "**odata.nextLink**: The URL to get the next set of results.",
            "**value**: A collection of DirectoryObject."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
