{
  "name": "ServicePrincipals_UpdateKeyCredentials",
  "description": "Update the keyCredentials associated with a service principal.",
  "parameters": {
    "type": "object",
    "properties": {
      "objectId": {
        "type": "string",
        "description": "The object ID for which to get service principal information."
      },
      "api-version": {
        "type": "string",
        "description": "Client API version."
      },
      "tenantID": {
        "type": "string",
        "description": "The tenant ID."
      },
      "body": {
        "$ref": "#/components/schemas/KeyCredentialsUpdateParameters",
        "description": "Parameters to update the keyCredentials of an existing service principal."
      }
    },
    "required": [
      "objectId",
      "api-version",
      "tenantID",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PATCH",
    "url": {
      "$uri": "https://graph.windows.net/{tenantID}/servicePrincipals/{objectId}/keyCredentials{?api-version}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Active Directory error information.",
        "**Key properties:**",
        {
          "$ul": [
            [
              "**odata.error**: Active Directory OData error information.",
              {
                "$ul": [
                  "**code**: Error code.",
                  [
                    "**message**: Active Directory error message.",
                    {
                      "$ul": [
                        "**value**: Error message value."
                      ]
                    }
                  ]
                ]
              }
            ]
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
