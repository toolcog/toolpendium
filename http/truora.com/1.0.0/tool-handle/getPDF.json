{
  "name": "getPDF",
  "description": "Downloads the PDF in the specified language, Spanish by default.",
  "parameters": {
    "type": "object",
    "properties": {
      "lang": {
        "type": "string",
        "description": "Used to specify the language for the PDF, if not specified the PDF will be downloaded in Spanish."
      },
      "check_id": {
        "type": "string",
        "description": "ID of the check"
      }
    },
    "required": [
      "check_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.truora.com/v1/checks/{check_id}/pdf{?lang}"
    }
  },
  "responses": {}
}
