{
  "name": "CreatePDF",
  "description": "Creates a PDF containing the background check results.",
  "parameters": {
    "type": "object",
    "properties": {
      "check_id": {
        "type": "string",
        "description": "ID of the check"
      }
    },
    "required": [
      "check_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.truora.com/v1/checks/{check_id}/pdf"
    }
  },
  "responses": {}
}
