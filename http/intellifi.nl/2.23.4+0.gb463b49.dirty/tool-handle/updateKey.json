{
  "name": "updateKey",
  "description": "Can only be used by an **administrative** user.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Unique identifier"
      },
      "body": {
        "$ref": "#/components/schemas/Key",
        "description": "A JSON object containing key information"
      }
    },
    "required": [
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://brain.intellifi.cloud/api/keys/{id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**resource**",
              {
                "$ul": [
                  "**id**: Unique identifier for resource.",
                  "**url**: Url to the individual resource."
                ]
              }
            ],
            "**status**: Status code"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
