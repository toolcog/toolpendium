{
  "name": "deleteItemIdFromItemList",
  "description": "Delete item from list",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Unique identifier"
      },
      "itemId": {
        "type": "string",
        "description": "Item ID."
      }
    },
    "required": [
      "id",
      "itemId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://brain.intellifi.cloud/api/sets/itemlists/{id}/ids/{itemId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**status**: Status code",
            "**total**: Total items in the list"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
