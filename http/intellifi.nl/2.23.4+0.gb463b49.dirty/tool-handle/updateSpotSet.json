{
  "name": "updateSpotSet",
  "description": "Update existing spotset",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Unique identifier"
      },
      "setId": {
        "type": "string",
        "description": "Unique spot set identifier"
      },
      "body": {
        "$ref": "#/components/schemas/SpotSetUpdate",
        "description": "A JSON object containing spotset information"
      }
    },
    "required": [
      "id",
      "setId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://brain.intellifi.cloud/api/spots/{id}/sets/{setId}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**resource**",
              {
                "$ul": [
                  "**id**: Unique identifier for resource.",
                  "**url**: Url to the individual resource."
                ]
              }
            ],
            "**status**: Status code"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
