{
  "name": "delete_collection",
  "description": "This operation deletes an existing collection.",
  "parameters": {
    "type": "object",
    "properties": {
      "collectionName": {
        "$ref": "#/$defs/CollectionName"
      }
    },
    "required": [
      "collectionName"
    ],
    "$defs": {
      "CollectionName": {
        "description": "The unique name of a collection.",
        "example": "example",
        "format": "CollectionName",
        "nullable": false,
        "type": "string"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://controller.us-east1-gcp.pinecone.io/collections/{collectionName}"
    }
  },
  "responses": {
    "404": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    },
    "500": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
