{
  "name": "Update_an_Email_Template",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "template_id": {
        "type": "number",
        "description": "The id of the template to be updated."
      },
      "body": {
        "properties": {
          "body": {
            "description": "Your template body.",
            "type": "string"
          },
          "template_name": {
            "description": "The intended name for the new template.",
            "type": "string"
          }
        },
        "required": [
          "template_name",
          "body"
        ],
        "type": "object"
      }
    },
    "required": [
      "template_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://rest.clicksend.com/v3/email/templates/{template_id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
