{
  "name": "Search_Locations",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "country": {
        "type": "string",
        "description": "Country code."
      },
      "query": {
        "type": "string",
        "description": "A postal code or place name."
      }
    },
    "required": [
      "country",
      "query"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://rest.clicksend.com/v3/post/direct-mail/locations/search/{country}/?q={query}"
    }
  },
  "responses": {}
}
