{
  "name": "Get_Voice_History",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "date_from": {
        "type": "string",
        "description": "Timestamp (from) used to show records by date."
      },
      "date_to": {
        "type": "string",
        "description": "Timestamp (to) used to show recrods by date."
      }
    },
    "required": [
      "date_from",
      "date_to"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://rest.clicksend.com/v3/voice/history?date_from={date_from}&date_to={date_to}"
    }
  },
  "responses": {}
}
