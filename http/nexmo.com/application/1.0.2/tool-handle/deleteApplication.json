{
  "name": "deleteApplication",
  "description": "You use a `DELETE` request to delete a single application.",
  "parameters": {
    "type": "object",
    "properties": {
      "app_id": {
        "type": "string",
        "description": "The ID allocated to your application by Nexmo."
      }
    },
    "required": [
      "app_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.nexmo.com/v1/applications/{app_id}"
    }
  },
  "responses": {}
}
