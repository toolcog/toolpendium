{
  "name": "Top_OND",
  "description": "Returns LH Top routes per country or across all countries",
  "parameters": {
    "type": "object",
    "properties": {
      "Accept": {
        "type": "string",
        "description": "Mandatory http header:  application/xml or application/json"
      },
      "catalogues": {
        "default": "LH",
        "type": "string",
        "description": "Carrier for which the OND will be retrieved (e.g. 'LH')"
      },
      "origin": {
        "type": "string",
        "description": "Enter the origin country code (e.g. 'DE'). Leave empty to search Top OND across all countries"
      }
    },
    "required": [
      "Accept"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.lufthansa.com/v1/offers/ond/top{?catalogues,origin}"
    },
    "headers": {
      "Accept": {
        "$": "Accept"
      }
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
