{
  "name": "blockCard",
  "description": "Updates status of an existing card to block which prevents any transactions being carried out with that card.",
  "parameters": {
    "type": "object",
    "properties": {
      "cardId": {
        "description": "The cardid of the card to block",
        "format": "int64",
        "type": "integer"
      }
    },
    "required": [
      "cardId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.fire.com/business/v1/cards/{cardId}/block"
    }
  },
  "responses": {}
}
