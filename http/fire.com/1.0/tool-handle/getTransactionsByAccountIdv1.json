{
  "name": "getTransactionsByAccountIdv1",
  "description": "Retrieve a list of transactions against an account. Recommended to use the v3 endpoint instead.",
  "parameters": {
    "type": "object",
    "properties": {
      "ican": {
        "description": "The ican of the account to retrieve",
        "format": "int64",
        "type": "integer"
      },
      "limit": {
        "description": "The number of records to return. Defaults to 10 - max is 200.",
        "format": "int64",
        "type": "integer"
      },
      "offset": {
        "description": "The page offset. Defaults to 0. This is the record number that the returned list will start at. E.g. offset = 40 and limit = 20 will return records 40 to 59.",
        "format": "int64",
        "type": "integer"
      }
    },
    "required": [
      "ican",
      "limit",
      "offset"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.fire.com/business/v1/accounts/{ican}/transactions{?limit,offset}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "cardTransactionsv1"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**dateRangeTo**: milisecond timestamp of date range to value.",
            "**total**: The total number of card transactions in the list.",
            "**transactions**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
