{
  "name": "addAccount",
  "description": "Creates a new fire.com account.\n\n**Please note there is a charge associated with creating a new account.**\n",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "properties": {
          "acceptFeesAndCharges": {
            "description": "a field to indicate you accept the fee for a new account",
            "type": "boolean"
          },
          "accountName": {
            "description": "Name to give the new account",
            "example": "Operating Account",
            "maxLength": 50,
            "type": "string"
          },
          "currency": {
            "description": "The currency of the new account",
            "enum": [
              "EUR",
              "GBP"
            ],
            "type": "string"
          }
        },
        "title": "newAccount",
        "type": "object",
        "description": "Details of the new account"
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.fire.com/business/v1/accounts"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "201": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Account"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**balance**: the balance of the account (in minor currency units - pence, cent etc. 434050 == 4,340.50 GBP for a GBP account).",
            "**cbic**: the BIC of the account (provided if currency is EUR).",
            "**ccan**: the Account Number of the account.",
            "**ciban**: the IBAN of the account (provided if currency is EUR).",
            "**cnsc**: the Sort Code of the account.",
            "**colour**: Internal Use",
            [
              "**currency**: The currency.",
              {
                "$ul": [
                  "**code**: The three letter code for the currency - either `EUR` or `GBP`.",
                  "**description**: The name of the currency"
                ]
              }
            ],
            "**defaultAccount**: true if this is the default account for this currency. This will be the account that general fees are taken from (as opposed to per-transaction fees).",
            "**directDebitsAllowed**: Whether or not direct debits can be set up on this account.",
            "**fopOnly**: Indicates that this account is for collecting Fire Open Payments only. All other payments to this account will be returned.",
            "**ican**: identifier for the fire.com account (assigned by fire.com)",
            "**name**: the name the user gives to the account to help them identify it.",
            "**status**: Live accounts can be used as normal. Migrated accounts were used before Brexit and are read-only."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
