{
  "name": "deleteOrganizationAdmin",
  "description": "Revoke all access for a dashboard administrator within this organization",
  "parameters": {
    "type": "object",
    "properties": {
      "organizationId": {
        "type": "string"
      },
      "adminId": {
        "type": "string"
      }
    },
    "required": [
      "organizationId",
      "adminId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.meraki.com/api/v0/organizations/{organizationId}/admins/{adminId}"
    }
  },
  "responses": {}
}
