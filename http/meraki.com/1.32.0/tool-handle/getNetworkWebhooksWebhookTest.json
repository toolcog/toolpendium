{
  "name": "getNetworkWebhooksWebhookTest",
  "description": "Return the status of a webhook test for a network",
  "parameters": {
    "type": "object",
    "properties": {
      "networkId": {
        "type": "string"
      },
      "webhookTestId": {
        "type": "string"
      }
    },
    "required": [
      "networkId",
      "webhookTestId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.meraki.com/api/v1/networks/{networkId}/webhooks/webhookTests/{webhookTestId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**id**: Webhook delivery identifier",
            "**status**: Current status of the webhook delivery",
            "**url**: URL where the webhook was delivered"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
