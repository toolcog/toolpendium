{
  "name": "getNetworkCellularGatewayDhcp",
  "description": "List common DHCP settings of MGs",
  "parameters": {
    "type": "object",
    "properties": {
      "networkId": {
        "type": "string"
      }
    },
    "required": [
      "networkId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.meraki.com/api/v1/networks/{networkId}/cellularGateway/dhcp"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**dhcpLeaseTime**: DHCP Lease time for all MG in the network.",
            "**dnsCustomNameservers**: List of fixed IPs representing the the DNS Name servers when the mode is 'custom'.",
            "**dnsNameservers**: DNS name servers mode for all MG in the network."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
