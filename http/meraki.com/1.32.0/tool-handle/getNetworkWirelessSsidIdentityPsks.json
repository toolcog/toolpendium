{
  "name": "getNetworkWirelessSsidIdentityPsks",
  "description": "List all Identity PSKs in a wireless network",
  "parameters": {
    "type": "object",
    "properties": {
      "networkId": {
        "type": "string"
      },
      "number": {
        "type": "string"
      }
    },
    "required": [
      "networkId",
      "number"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.meraki.com/api/v1/networks/{networkId}/wireless/ssids/{number}/identityPsks"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**email**: The email associated with the System's Manager User",
            "**expiresAt**: Timestamp for when the Identity PSK expires, or 'null' to never expire",
            "**groupPolicyId**: The group policy to be applied to clients",
            "**id**: The unique identifier of the Identity PSK",
            "**name**: The name of the Identity PSK",
            "**passphrase**: The passphrase for client authentication",
            "**wifiPersonalNetworkId**: The WiFi Personal Network unique identifier"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.name"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
