{
  "name": "custom_vitals_read",
  "description": "Retrieve an existing custom vital type",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "description": "",
        "title": "",
        "type": "string"
      },
      "doctor": {
        "description": "",
        "title": "doctor",
        "type": "integer"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://app.drchrono.com/api/custom_vitals/{id}{?doctor}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**allowed_values**: If `data_type` is `single_sel`, this is the array of values in the select's dropdown.",
            "**archived**: Indicates that the object has been soft-deleted and should not be used",
            "**data_type**: One of `text`, `number`, or `single_sel`",
            "**description**: ",
            "**doctor**: ID of the doctor who created the custom vital",
            "**fraction_delimiter**: If `is_fraction_field` is true, this is the character separating the numerator and denominator",
            "**id**: ",
            "**is_fraction_field**: ",
            "**name**: ",
            "**unit**: "
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
