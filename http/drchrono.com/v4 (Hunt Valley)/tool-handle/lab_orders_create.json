{
  "name": "lab_orders_create",
  "description": "Create lab orders. An example lab workflow is as following:\n\n- When you get orders, submit them via `/api/lab_orders`, such that doctors can see them in drchrono.\n\n- When results come in, submit the result document PDF via `/api/lab_documents` and submit the results data via `/api/lab_results`\n\n- Update `/api/lab_orders` status\n",
  "parameters": {
    "type": "object",
    "properties": {
      "since": {
        "description": "",
        "title": "since",
        "type": "string"
      },
      "doctor": {
        "description": "",
        "title": "doctor",
        "type": "integer"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://app.drchrono.com/api/lab_orders{?since,doctor}"
    }
  },
  "responses": {
    "201": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**accession_number**: For external use only",
            "**doctor**: ",
            "**documents**: Associated `/lab_documents` objects",
            "**icd10_codes**: ICD-10 codes of the conditions which the tests concerns.",
            "**id**: ",
            "**notes**: ",
            "**patient**: ",
            "**priority**: `\"Normal\"` or `\"STAT\"`. Default `\"Normal\"`",
            "**requisition_id**: The ID printed on the requisition PDF. Generally the same as id.",
            "**status**: Equivalent to HL7's ORC.5. Defaults to `\"N\"`.",
            "**sublab**: ",
            "**timestamp**: Time at which the order was submitted. Defaults to now"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
