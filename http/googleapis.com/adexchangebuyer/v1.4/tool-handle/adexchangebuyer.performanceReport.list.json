{
  "name": "adexchangebuyer_performanceReport_list",
  "description": "Retrieves the authenticated user's list of performance metrics.",
  "parameters": {
    "type": "object",
    "properties": {
      "accountId": {
        "type": "string",
        "description": "The account id to get the reports."
      },
      "endDateTime": {
        "type": "string",
        "description": "The end time of the report in ISO 8601 timestamp format using UTC."
      },
      "startDateTime": {
        "type": "string",
        "description": "The start time of the report in ISO 8601 timestamp format using UTC."
      },
      "maxResults": {
        "maximum": 1000,
        "minimum": 1,
        "type": "integer",
        "description": "Maximum number of entries returned on one result page. If not set, the default is 100. Optional."
      },
      "pageToken": {
        "type": "string",
        "description": "A continuation token, used to page through performance reports. To retrieve the next page, set this parameter to the value of \"nextPageToken\" from the previous response. Optional."
      },
      "alt": {
        "enum": [
          "json"
        ],
        "type": "string",
        "description": "Data format for the response."
      },
      "fields": {
        "type": "string",
        "description": "Selector specifying which fields to include in a partial response."
      },
      "key": {
        "type": "string",
        "description": "API key. Your API key identifies your project and provides you with API access, quota, and reports. Required unless you provide an OAuth 2.0 token."
      },
      "oauth_token": {
        "type": "string",
        "description": "OAuth 2.0 token for the current user."
      },
      "prettyPrint": {
        "type": "boolean",
        "description": "Returns response with indentations and line breaks."
      },
      "quotaUser": {
        "type": "string",
        "description": "An opaque string that represents a user for quota purposes. Must not exceed 40 characters."
      },
      "userIp": {
        "type": "string",
        "description": "Deprecated. Please use quotaUser instead."
      }
    },
    "required": [
      "accountId",
      "endDateTime",
      "startDateTime"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://www.googleapis.com/adexchangebuyer/v1.4/performancereport{?accountId,endDateTime,startDateTime,maxResults,pageToken,alt,fields,key,oauth_token,prettyPrint,quotaUser,userIp}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "The configuration data for an Ad Exchange performance report list.",
        "**Key properties:**",
        {
          "$ul": [
            "**kind**: Resource type. (default: adexchangebuyer#performanceReportList)",
            "**performanceReport**: A list of performance reports relevant for the account."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
