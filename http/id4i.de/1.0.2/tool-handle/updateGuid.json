{
  "name": "updateGuid",
  "description": "Allows ownership transfer.",
  "parameters": {
    "type": "object",
    "properties": {
      "id4n": {
        "type": "string",
        "description": "The GUID number"
      },
      "body": {
        "$ref": "#/components/schemas/Guid",
        "description": "request"
      }
    },
    "required": [
      "id4n",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PATCH",
    "url": {
      "$uri": "//backend.id4i.de/api/v1/guids/{id4n}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
