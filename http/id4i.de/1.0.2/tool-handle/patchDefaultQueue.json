{
  "name": "patchDefaultQueue",
  "parameters": {
    "type": "object",
    "properties": {
      "organizationId": {
        "type": "string",
        "description": "organizationId"
      },
      "body": {
        "$ref": "#/components/schemas/QueueUpdateRequest",
        "description": "request"
      }
    },
    "required": [
      "organizationId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PATCH",
    "url": {
      "$uri": "//backend.id4i.de/api/v1/organizations/{organizationId}/messaging"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
