{
  "name": "getProjects",
  "description": "Allows to retrieve the list of projects of the authenticated user. The list will be paginated and the provided query parameters allow filtering the returned projects.",
  "parameters": {
    "type": "object",
    "properties": {
      "from": {
        "description": "Query only projects updated after the given timestamp",
        "type": "string"
      },
      "gitForkProtection": {
        "description": "Specifies whether PRs from Git forks should require a team member's authorization before it can be deployed",
        "enum": [
          "1",
          "0"
        ],
        "examples": [
          "1"
        ],
        "type": "string"
      },
      "limit": {
        "description": "Limit the number of projects returned",
        "type": "string"
      },
      "search": {
        "description": "Search projects by the name field",
        "type": "string"
      },
      "repo": {
        "description": "Filter results by repo. Also used for project count",
        "type": "string"
      },
      "repoId": {
        "description": "Filter results by Repository ID.",
        "type": "string"
      },
      "repoUrl": {
        "description": "Filter results by Repository URL.",
        "examples": [
          "https://github.com/vercel/next.js"
        ],
        "type": "string"
      },
      "excludeRepos": {
        "description": "Filter results by excluding those projects that belong to a repo",
        "type": "string"
      },
      "edgeConfigId": {
        "description": "Filter results by connected Edge Config ID",
        "type": "string"
      },
      "edgeConfigTokenId": {
        "description": "Filter results by connected Edge Config Token ID",
        "type": "string"
      },
      "teamId": {
        "type": "string",
        "description": "The Team identifier or slug to perform the request on behalf of."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.vercel.com/v9/projects{?from,gitForkProtection,limit,search,repo,repoId,repoUrl,excludeRepos,edgeConfigId,edgeConfigTokenId,teamId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            [
              "**pagination**: This object contains information related to the pagination of the current request, including the necessary parameters to get the next or previous page of data.",
              {
                "$ul": [
                  "**count**: Amount of items in the current page.",
                  "**next**: Timestamp that must be used to request the next page.",
                  "**prev**: Timestamp that must be used to request the previous page."
                ]
              }
            ],
            "**projects**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
