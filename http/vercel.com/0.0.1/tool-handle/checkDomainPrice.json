{
  "name": "checkDomainPrice",
  "description": "Check the price to purchase a domain and how long a single purchase period is.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "description": "The name of the domain for which the price needs to be checked.",
        "examples": [
          "example.com"
        ],
        "type": "string"
      },
      "type": {
        "description": "In which status of the domain the price needs to be checked.",
        "enum": [
          "new",
          "renewal"
        ],
        "examples": [
          "new"
        ],
        "type": "string"
      },
      "teamId": {
        "type": "string",
        "description": "The Team identifier or slug to perform the request on behalf of."
      }
    },
    "required": [
      "name"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.vercel.com/v4/domains/price{?name,type,teamId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Successful response which returns the price of the domain and the period.",
        "**Key properties:**",
        {
          "$ul": [
            "**period**: The number of years the domain could be held before paying again.",
            "**price**: The domain price in USD."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
