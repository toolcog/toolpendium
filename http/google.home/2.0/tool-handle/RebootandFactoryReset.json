{
  "name": "RebootandFactoryReset",
  "description": "This can simply reboot the device (`params: \"now\"`) or factory reset the device (`params: \"fdr\"`).",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/$defs/RebootandFactoryResetRequest"
      }
    },
    "required": [
      "body"
    ],
    "$defs": {
      "RebootandFactoryResetRequest": {
        "example": {
          "params": "now"
        },
        "properties": {
          "params": {
            "type": "string"
          }
        },
        "required": [
          "params"
        ],
        "title": "RebootandFactoryResetRequest",
        "type": "object"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://example.com/setup/reboot"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
