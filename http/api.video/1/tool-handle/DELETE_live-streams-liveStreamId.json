{
  "name": "DELETE_live-streams-liveStreamId",
  "description": "Delete a live stream",
  "parameters": {
    "type": "object",
    "properties": {
      "liveStreamId": {
        "type": "string",
        "description": "The unique ID for the live stream that you want to remove."
      }
    },
    "required": [
      "liveStreamId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://ws.api.video/live-streams/{liveStreamId}"
    }
  },
  "responses": {}
}
