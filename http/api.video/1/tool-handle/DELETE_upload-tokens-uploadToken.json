{
  "name": "DELETE_upload-tokens-uploadToken",
  "description": "Delete an existing upload token. This is especially useful for tokens you may have created that do not expire.",
  "parameters": {
    "type": "object",
    "properties": {
      "uploadToken": {
        "type": "string",
        "description": "The unique identifier for the upload token you want to delete. Deleting a token will make it so the token can no longer be used for authentication."
      }
    },
    "required": [
      "uploadToken"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://ws.api.video/upload-tokens/{uploadToken}"
    }
  },
  "responses": {
    "404": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "NotFound"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**name**",
            "**status**",
            "**title**",
            "**type**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
