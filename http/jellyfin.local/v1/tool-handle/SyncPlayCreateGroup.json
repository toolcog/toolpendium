{
  "name": "SyncPlayCreateGroup",
  "description": "Create a new SyncPlay group.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/NewGroupRequestDto",
        "description": "The settings of the new group."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/SyncPlay/New"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
