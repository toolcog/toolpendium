{
  "name": "CancelTimer",
  "description": "Cancels a live tv timer.",
  "parameters": {
    "type": "object",
    "properties": {
      "timerId": {
        "description": "Timer id.",
        "type": "string"
      }
    },
    "required": [
      "timerId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "http://jellyfin.local/LiveTv/Timers/{timerId}"
    }
  },
  "responses": {}
}
