{
  "name": "Play",
  "description": "Instructs a session to play an item.",
  "parameters": {
    "type": "object",
    "properties": {
      "sessionId": {
        "description": "The session id.",
        "type": "string"
      },
      "playCommand": {
        "$ref": "#/components/schemas/PlayCommand",
        "description": "The type of play command to issue (PlayNow, PlayNext, PlayLast). Clients who have not yet implemented play next and play last may play now."
      },
      "itemIds": {
        "description": "The ids of the items to play, comma delimited.",
        "items": {
          "format": "uuid",
          "type": "string"
        },
        "type": "array"
      },
      "startPositionTicks": {
        "description": "The starting position of the first item.",
        "format": "int64",
        "nullable": true,
        "type": "integer"
      }
    },
    "required": [
      "sessionId",
      "playCommand",
      "itemIds"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/Sessions/{sessionId}/Playing{?playCommand,itemIds,startPositionTicks}"
    }
  },
  "responses": {}
}
