{
  "name": "SyncPlaySetPlaylistItem",
  "description": "Request to change playlist item in SyncPlay group.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/SetPlaylistItemRequestDto",
        "description": "The new item to play."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/SyncPlay/SetPlaylistItem"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
