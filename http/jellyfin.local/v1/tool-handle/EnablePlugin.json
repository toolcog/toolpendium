{
  "name": "EnablePlugin",
  "description": "Enables a disabled plugin.",
  "parameters": {
    "type": "object",
    "properties": {
      "pluginId": {
        "description": "Plugin id.",
        "format": "uuid",
        "type": "string"
      },
      "version": {
        "$ref": "#/components/schemas/Version",
        "description": "Plugin version."
      }
    },
    "required": [
      "pluginId",
      "version"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/Plugins/{pluginId}/{version}/Enable"
    }
  },
  "responses": {
    "404": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**detail**",
            "**instance**",
            "**status**",
            "**title**",
            "**type**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
