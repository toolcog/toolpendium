{
  "name": "SyncPlaySetRepeatMode",
  "description": "Request to set repeat mode in SyncPlay group.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/SetRepeatModeRequestDto",
        "description": "The new repeat mode."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/SyncPlay/SetRepeatMode"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
