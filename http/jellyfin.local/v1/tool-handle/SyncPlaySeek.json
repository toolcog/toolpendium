{
  "name": "SyncPlaySeek",
  "description": "Request seek in SyncPlay group.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/SeekRequestDto",
        "description": "The new playback position."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/SyncPlay/Seek"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
