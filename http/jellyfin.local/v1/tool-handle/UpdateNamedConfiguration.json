{
  "name": "UpdateNamedConfiguration",
  "description": "Updates named configuration.",
  "parameters": {
    "type": "object",
    "properties": {
      "key": {
        "description": "Configuration key.",
        "type": "string"
      }
    },
    "required": [
      "key"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/System/Configuration/{key}"
    }
  },
  "responses": {}
}
