{
  "name": "GetPostedPlaybackInfo",
  "description": "For backwards compatibility parameters can be sent via Query or Body, with Query having higher precedence.",
  "parameters": {
    "type": "object",
    "properties": {
      "itemId": {
        "description": "The item id.",
        "format": "uuid",
        "type": "string"
      },
      "userId": {
        "description": "The user id.",
        "format": "uuid",
        "nullable": true,
        "type": "string"
      },
      "maxStreamingBitrate": {
        "description": "The maximum streaming bitrate.",
        "format": "int32",
        "nullable": true,
        "type": "integer"
      },
      "startTimeTicks": {
        "description": "The start time in ticks.",
        "format": "int64",
        "nullable": true,
        "type": "integer"
      },
      "audioStreamIndex": {
        "description": "The audio stream index.",
        "format": "int32",
        "nullable": true,
        "type": "integer"
      },
      "subtitleStreamIndex": {
        "description": "The subtitle stream index.",
        "format": "int32",
        "nullable": true,
        "type": "integer"
      },
      "maxAudioChannels": {
        "description": "The maximum number of audio channels.",
        "format": "int32",
        "nullable": true,
        "type": "integer"
      },
      "mediaSourceId": {
        "description": "The media source id.",
        "nullable": true,
        "type": "string"
      },
      "liveStreamId": {
        "description": "The livestream id.",
        "nullable": true,
        "type": "string"
      },
      "autoOpenLiveStream": {
        "description": "Whether to auto open the livestream.",
        "nullable": true,
        "type": "boolean"
      },
      "enableDirectPlay": {
        "description": "Whether to enable direct play. Default: true.",
        "nullable": true,
        "type": "boolean"
      },
      "enableDirectStream": {
        "description": "Whether to enable direct stream. Default: true.",
        "nullable": true,
        "type": "boolean"
      },
      "enableTranscoding": {
        "description": "Whether to enable transcoding. Default: true.",
        "nullable": true,
        "type": "boolean"
      },
      "allowVideoStreamCopy": {
        "description": "Whether to allow to copy the video stream. Default: true.",
        "nullable": true,
        "type": "boolean"
      },
      "allowAudioStreamCopy": {
        "description": "Whether to allow to copy the audio stream. Default: true.",
        "nullable": true,
        "type": "boolean"
      },
      "body": {
        "$ref": "#/components/schemas/PlaybackInfoDto",
        "description": "The playback info."
      }
    },
    "required": [
      "itemId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/Items/{itemId}/PlaybackInfo{?userId,maxStreamingBitrate,startTimeTicks,audioStreamIndex,subtitleStreamIndex,maxAudioChannels,mediaSourceId,liveStreamId,autoOpenLiveStream,enableDirectPlay,enableDirectStream,enableTranscoding,allowVideoStreamCopy,allowAudioStreamCopy}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Class PlaybackInfoResponse.",
        "**Key properties:**",
        {
          "$ul": [
            "**ErrorCode**",
            "**MediaSources**: Gets or sets the media sources.",
            "**PlaySessionId**: Gets or sets the play session identifier."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
