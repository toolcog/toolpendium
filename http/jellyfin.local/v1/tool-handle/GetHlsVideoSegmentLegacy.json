{
  "name": "GetHlsVideoSegmentLegacy",
  "description": "Gets a hls video segment.",
  "parameters": {
    "type": "object",
    "properties": {
      "itemId": {
        "description": "The item id.",
        "type": "string"
      },
      "playlistId": {
        "description": "The playlist id.",
        "type": "string"
      },
      "segmentId": {
        "description": "The segment id.",
        "type": "string"
      },
      "segmentContainer": {
        "description": "The segment container.",
        "type": "string"
      }
    },
    "required": [
      "itemId",
      "playlistId",
      "segmentId",
      "segmentContainer"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://jellyfin.local/Videos/{itemId}/hls/{playlistId}/{segmentId}.{segmentContainer}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    },
    "404": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**detail**",
            "**instance**",
            "**status**",
            "**title**",
            "**type**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
