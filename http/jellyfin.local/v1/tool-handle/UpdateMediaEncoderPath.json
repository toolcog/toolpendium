{
  "name": "UpdateMediaEncoderPath",
  "description": "Updates the path to the media encoder.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/MediaEncoderPathDto",
        "description": "Media encoder path form body."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/System/MediaEncoder/Path"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
