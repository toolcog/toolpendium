{
  "name": "CancelPackageInstallation",
  "description": "Cancels a package installation.",
  "parameters": {
    "type": "object",
    "properties": {
      "packageId": {
        "description": "Installation Id.",
        "format": "uuid",
        "type": "string"
      }
    },
    "required": [
      "packageId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "http://jellyfin.local/Packages/Installing/{packageId}"
    }
  },
  "responses": {}
}
