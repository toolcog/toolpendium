{
  "name": "SetItemImage",
  "description": "Set item image.",
  "parameters": {
    "type": "object",
    "properties": {
      "itemId": {
        "description": "Item id.",
        "format": "uuid",
        "type": "string"
      },
      "imageType": {
        "$ref": "#/components/schemas/ImageType",
        "description": "Image type."
      }
    },
    "required": [
      "itemId",
      "imageType"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/Items/{itemId}/Images/{imageType}"
    }
  },
  "responses": {
    "404": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**detail**",
            "**instance**",
            "**status**",
            "**title**",
            "**type**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
