{
  "name": "CloseLiveStream",
  "description": "Closes a media source.",
  "parameters": {
    "type": "object",
    "properties": {
      "liveStreamId": {
        "description": "The livestream id.",
        "type": "string"
      }
    },
    "required": [
      "liveStreamId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/LiveStreams/Close{?liveStreamId}"
    }
  },
  "responses": {}
}
