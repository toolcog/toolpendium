{
  "name": "GetContentDirectory",
  "description": "Gets Dlna content directory xml.",
  "parameters": {
    "type": "object",
    "properties": {
      "serverId": {
        "description": "Server UUID.",
        "type": "string"
      }
    },
    "required": [
      "serverId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://jellyfin.local/Dlna/{serverId}/ContentDirectory"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
