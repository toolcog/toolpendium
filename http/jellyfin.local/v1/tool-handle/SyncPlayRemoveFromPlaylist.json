{
  "name": "SyncPlayRemoveFromPlaylist",
  "description": "Request to remove items from the playlist in SyncPlay group.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/RemoveFromPlaylistRequestDto",
        "description": "The items to remove."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/SyncPlay/RemoveFromPlaylist"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
