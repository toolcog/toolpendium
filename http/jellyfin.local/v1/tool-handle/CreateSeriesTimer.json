{
  "name": "CreateSeriesTimer",
  "description": "Creates a live tv series timer.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/SeriesTimerInfoDto",
        "description": "New series timer info."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/LiveTv/SeriesTimers"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
