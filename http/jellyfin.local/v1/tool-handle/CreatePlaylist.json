{
  "name": "CreatePlaylist",
  "description": "For backwards compatibility parameters can be sent via Query or Body, with Query having higher precedence.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "description": "The playlist name.",
        "nullable": true,
        "type": "string"
      },
      "ids": {
        "description": "The item ids.",
        "items": {
          "format": "uuid",
          "type": "string"
        },
        "nullable": true,
        "type": "array"
      },
      "userId": {
        "description": "The user id.",
        "format": "uuid",
        "nullable": true,
        "type": "string"
      },
      "mediaType": {
        "description": "The media type.",
        "nullable": true,
        "type": "string"
      },
      "body": {
        "$ref": "#/components/schemas/CreatePlaylistDto",
        "description": "The create playlist payload."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/Playlists{?name,ids,userId,mediaType}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**Id**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
