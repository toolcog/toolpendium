{
  "name": "SyncPlayQueue",
  "description": "Request to queue items to the playlist of a SyncPlay group.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/QueueRequestDto",
        "description": "The items to add."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/SyncPlay/Queue"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
