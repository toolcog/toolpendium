{
  "name": "RevokeKey",
  "description": "Remove an api key.",
  "parameters": {
    "type": "object",
    "properties": {
      "key": {
        "description": "The access token to delete.",
        "type": "string"
      }
    },
    "required": [
      "key"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "http://jellyfin.local/Auth/Keys/{key}"
    }
  },
  "responses": {}
}
