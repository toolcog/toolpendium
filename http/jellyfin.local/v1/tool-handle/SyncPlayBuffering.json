{
  "name": "SyncPlayBuffering",
  "description": "Notify SyncPlay group that member is buffering.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/BufferRequestDto",
        "description": "The player status."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/SyncPlay/Buffering"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
