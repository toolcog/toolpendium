{
  "name": "SyncPlayPing",
  "description": "Update session ping.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/PingRequestDto",
        "description": "The new ping."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/SyncPlay/Ping"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
