{
  "name": "CreateProfile",
  "description": "Creates a profile.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/DeviceProfile",
        "description": "Device profile."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/Dlna/Profiles"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
