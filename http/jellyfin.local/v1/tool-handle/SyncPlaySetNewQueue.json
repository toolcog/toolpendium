{
  "name": "SyncPlaySetNewQueue",
  "description": "Request to set new playlist in SyncPlay group.",
  "parameters": {
    "type": "object",
    "properties": {
      "body": {
        "$ref": "#/components/schemas/PlayRequestDto",
        "description": "The new playlist to play in the group."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/SyncPlay/SetNewQueue"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
