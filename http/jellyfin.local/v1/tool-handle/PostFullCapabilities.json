{
  "name": "PostFullCapabilities",
  "description": "Updates capabilities for a device.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "description": "The session id.",
        "nullable": true,
        "type": "string"
      },
      "body": {
        "$ref": "#/components/schemas/ClientCapabilitiesDto",
        "description": "The MediaBrowser.Model.Session.ClientCapabilities."
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://jellyfin.local/Sessions/Capabilities/Full{?id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
