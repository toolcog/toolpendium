{
  "name": "deleteCheckpointFirewall",
  "description": "Delete a checkpoint firewall data source",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "entity id"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "http://vmware.local/data-sources/checkpoint-firewalls/{id}"
    }
  },
  "responses": {}
}
