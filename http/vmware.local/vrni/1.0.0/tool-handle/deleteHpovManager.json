{
  "name": "deleteHpovManager",
  "description": "Delete a hp oneview data source",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "entity id"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "http://vmware.local/data-sources/hpov-managers/{id}"
    }
  },
  "responses": {}
}
