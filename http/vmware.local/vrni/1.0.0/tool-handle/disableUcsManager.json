{
  "name": "disableUcsManager",
  "description": "Disable an ucs manager data source",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "entity id"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "http://vmware.local/data-sources/ucs-managers/{id}/disable"
    }
  },
  "responses": {}
}
