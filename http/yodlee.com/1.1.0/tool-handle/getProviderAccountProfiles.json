{
  "name": "getProviderAccountProfiles",
  "description": "The get provider accounts profile service is used to return the user profile details that are associated to the provider account. <br>",
  "parameters": {
    "type": "object",
    "properties": {
      "providerAccountId": {
        "type": "string",
        "description": "Comma separated providerAccountIds."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//providerAccounts/profile{?providerAccountId}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "ProviderAccountUserProfileResponse"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**providerAccount**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
