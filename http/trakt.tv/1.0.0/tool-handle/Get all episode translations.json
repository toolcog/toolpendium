{
  "name": "Get_all_episode_translations",
  "description": "Returns all translations for an episode, including language and translated values for title and overview.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Trakt ID, Trakt slug, or IMDB ID"
      },
      "season": {
        "type": "integer",
        "description": "season number"
      },
      "episode": {
        "type": "integer",
        "description": "episode number"
      },
      "language": {
        "type": "string",
        "description": "2 character language code"
      },
      "trakt-api-version": {
        "type": "string",
        "description": "e.g. 2"
      },
      "trakt-api-key": {
        "type": "string",
        "description": "e.g. [client_id]"
      }
    },
    "required": [
      "id",
      "season",
      "episode",
      "language"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.trakt.tv/shows/{id}/seasons/{season}/episodes/{episode}/translations/{language}"
    },
    "headers": {
      "trakt-api-version": {
        "$": "trakt-api-version"
      },
      "trakt-api-key": {
        "$": "trakt-api-key"
      }
    }
  },
  "responses": {}
}
