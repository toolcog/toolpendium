{
  "name": "Undo_reset_show_progress",
  "description": "#### &#128274; OAuth Required 🔥 VIP Only\n\nUndo the reset and have watched progress use all watched history for the show.",
  "parameters": {
    "type": "object",
    "properties": {
      "trakt-api-version": {
        "type": "string",
        "description": "e.g. 2"
      },
      "trakt-api-key": {
        "type": "string",
        "description": "e.g. [client_id]"
      },
      "id": {
        "type": "string",
        "description": "Automatically added"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.trakt.tv/shows/{id}/progress/watched/reset"
    },
    "headers": {
      "trakt-api-version": {
        "$": "trakt-api-version"
      },
      "trakt-api-key": {
        "$": "trakt-api-key"
      }
    }
  },
  "responses": {}
}
