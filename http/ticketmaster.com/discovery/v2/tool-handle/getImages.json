{
  "name": "getImages",
  "description": "Get images for a specific event using the unique identifier for the event.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of the event"
      },
      "locale": {
        "default": "en",
        "type": "string",
        "description": "The locale in ISO code format. Multiple comma-separated values can be provided. When omitting the country part of the code (e.g. only 'en' or 'fr') then the first matching locale is used. When using a '*' it matches all locales. '*' can only be used at the end (e.g. 'en-us,en,*') "
      },
      "includeLicensedContent": {
        "default": "no",
        "enum": [
          "yes",
          " no"
        ],
        "pattern": "^\\s*|yes|no$",
        "type": "string",
        "description": "True if you want to display licensed content"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//www.ticketmaster.com/discovery/v2/discovery/v2/events/{id}/images{?locale,includeLicensedContent}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "This class defines an Event with only images view on the Discovery API",
        "**Key properties:**",
        {
          "$ul": [
            "**id**: Unique id of the entity in the discovery API",
            "**images**: Images of the entity",
            "**type**: Type of the entity"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
