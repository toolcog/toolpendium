{
  "name": "postSetPassword",
  "parameters": {
    "type": "object",
    "properties": {
      "old": {
        "type": "string"
      },
      "plain": {
        "type": "string"
      },
      "verify": {
        "type": "string"
      }
    },
    "required": [
      "old",
      "plain",
      "verify"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//crx/explorer/ui/setpassword.jsp{?old,plain,verify}"
    }
  },
  "responses": {
    "default": {
      "$encode": "markdown",
      "$lang": "json",
      "$code": {
        "$encode": "json",
        "$indent": true,
        "$content": {
          "$": "$.body"
        }
      }
    }
  }
}
