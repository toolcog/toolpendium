{
  "name": "getFeaturedImageCollection",
  "description": "This endpoint gets more detailed information about a featured collection, including its cover image and timestamps for its creation and most recent update. To get the images, use `GET /v2/images/collections/featured/{id}/items`.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Collection ID"
      },
      "embed": {
        "enum": [
          "share_url"
        ],
        "type": "string",
        "description": "Which sharing information to include in the response, such as a URL to the collection"
      },
      "asset_hint": {
        "default": "1x",
        "enum": [
          "1x",
          "2x"
        ],
        "type": "string",
        "description": "Cover image size"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.shutterstock.com/v2/images/collections/featured/{id}{?embed,asset_hint}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Metadata about a featured collection",
        "**Key properties:**",
        {
          "$ul": [
            [
              "**cover_item**: Featured collection cover item metadata",
              {
                "$ul": [
                  "**url**: URL of the collection cover item"
                ]
              }
            ],
            "**created_time**: Date that the collection was created",
            [
              "**hero_item**: Featured collection cover item metadata",
              {
                "$ul": [
                  "**url**: URL of the collection cover item"
                ]
              }
            ],
            "**id**: Collection ID",
            "**items_updated_time**: Date that an item in the collection was last added or removed",
            "**name**: Name of the collection",
            "**share_url**: Unique share url for the collection",
            "**total_item_count**: Total number of items in the collection",
            "**updated_time**: Date that the collection was last modified"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
