{
  "name": "getContributorCollectionItems",
  "description": "This endpoint lists the IDs of items in a contributor's collection and the date that each was added.",
  "parameters": {
    "type": "object",
    "properties": {
      "contributor_id": {
        "type": "string",
        "description": "Contributor ID"
      },
      "id": {
        "type": "string",
        "description": "Collection ID that belongs to the contributor"
      },
      "page": {
        "default": 1,
        "minimum": 1,
        "type": "integer",
        "description": "Page number"
      },
      "per_page": {
        "default": 20,
        "maximum": 50,
        "minimum": 1,
        "type": "integer",
        "description": "Number of results per page"
      },
      "sort": {
        "enum": [
          "newest",
          "oldest"
        ],
        "type": "string",
        "description": "Sort order"
      }
    },
    "required": [
      "contributor_id",
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.shutterstock.com/v2/contributors/{contributor_id}/collections/{id}/items{?page,per_page,sort}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "List of items in a collection",
        "**Key properties:**",
        {
          "$ul": [
            "**data**: Assets in the collection",
            "**errors**: Error list; appears only if there was an error",
            "**message**: Server-generated message, if any",
            "**page**: The current page of results",
            "**per_page**: The number of results per page",
            "**total_count**: The total number of results across all pages"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
