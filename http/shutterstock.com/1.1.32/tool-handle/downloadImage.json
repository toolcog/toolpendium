{
  "name": "downloadImage",
  "description": "This endpoint redownloads images that you have already received a license for. The download links in the response are valid for 8 hours.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "License ID"
      },
      "body": {
        "$ref": "#/components/schemas/RedownloadImage",
        "description": "Information about the images to redownload"
      }
    },
    "required": [
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.shutterstock.com/v2/images/licenses/{id}/downloads"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "URL object",
        "**Key properties:**",
        {
          "$ul": [
            "**url**: URL that can be used to download the unwatermarked, licensed asset"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
