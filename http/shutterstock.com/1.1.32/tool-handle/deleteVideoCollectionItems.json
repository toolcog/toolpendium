{
  "name": "deleteVideoCollectionItems",
  "description": "This endpoint removes one or more videos from a collection.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The ID of the Collection from which items will be deleted"
      },
      "item_id": {
        "items": {
          "type": "string"
        },
        "type": "array",
        "description": "One or more video IDs to remove from the collection"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.shutterstock.com/v2/videos/collections/{id}/items{?item_id}"
    }
  },
  "responses": {}
}
