{
  "name": "deleteVideoCollection",
  "description": "This endpoint deletes a collection.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The ID of the collection to delete"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.shutterstock.com/v2/videos/collections/{id}"
    }
  },
  "responses": {}
}
