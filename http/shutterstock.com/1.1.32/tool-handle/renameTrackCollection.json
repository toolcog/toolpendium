{
  "name": "renameTrackCollection",
  "description": "This endpoint sets a new name for a collection.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Collection ID"
      },
      "body": {
        "$ref": "#/components/schemas/CollectionUpdateRequest",
        "description": "Collection changes"
      }
    },
    "required": [
      "id",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://api.shutterstock.com/v2/audio/collections/{id}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
