{
  "name": "FileActionMove",
  "description": "Move file/folder",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path to operate on."
      },
      "body": {
        "properties": {
          "destination": {
            "description": "Move destination path.",
            "type": "string",
            "x-ms-summary": "Move destination path."
          }
        },
        "required": [
          "destination"
        ],
        "type": "object"
      }
    },
    "required": [
      "path",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//app.files.com/api/rest/v1/file_actions/move/{path}"
    },
    "body": {
      "$": "body",
      "encode": "multipart"
    }
  },
  "responses": {
    "201": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Move file/folder",
        "**Key properties:**",
        {
          "$ul": [
            "**file_migration_id**: If status is pending, this is the id of the FileMigration to check for status updates.",
            "**status**: Status of file operation."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
