{
  "name": "GetSiteUsage",
  "description": "Get the most recent usage snapshot (usage data for billing purposes) for a Site.",
  "parameters": {
    "type": "object"
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//app.files.com/api/rest/v1/site/usage"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "List Usage Snapshots",
        "**Key properties:**",
        {
          "$ul": [
            "**bytes_sent**: Transfer Usage for period - Outbound GB from Files Native Storage",
            "**current_storage**: Current total Storage Usage GB as of end date (not necessarily high water mark, which is used for billing)",
            "**deleted_files_counted_in_minimum**: Storage Usage for files that are deleted but uploaded within last 30 days as of end date (not necessarily high water mark, which is used for billing)",
            "**deleted_files_storage**: Storage Usage for files that are deleted but retained as backups as of end date (not necessarily high water mark, which is used for billing)",
            "**end_at**: Usage snapshot end date/time",
            "**high_water_storage**: Highest Storage Usage GB recorded in time period (used for billing)",
            "**high_water_user_count**: Highest user count number in time period",
            "**id**: Usage snapshot ID",
            "**root_storage**: Storage Usage for root folder as of end date (not necessarily high water mark, which is used for billing)",
            "**start_at**: Usage snapshot start date/time",
            "**sync_bytes_received**: Transfer Usage for period - Inbound GB to Remote Servers (Sync/Mount)",
            "**sync_bytes_sent**: Transfer Usage for period - Outbound GB from Remote Servers (Sync/Mount)",
            "**total_billable_transfer_usage**: Transfer usage for period - Total Billable amount",
            "**total_billable_usage**: Storage + Transfer Usage - Total Billable amount",
            "**usage_by_top_level_dir**: Storage Usage - map of root folders to their usage as of end date (not necessarily high water mark, which is used for billing)"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
