{
  "name": "PostActionWebhookFailuresIdRetry",
  "description": "retry Action Webhook Failure",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int32",
        "type": "integer",
        "description": "Action Webhook Failure ID."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//app.files.com/api/rest/v1/action_webhook_failures/{id}/retry"
    }
  },
  "responses": {}
}
