{
  "name": "GetExternalEventsId",
  "description": "Show External Event",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int32",
        "type": "integer",
        "description": "External Event ID."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//app.files.com/api/rest/v1/external_events/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "List External Events",
        "**Key properties:**",
        {
          "$ul": [
            "**body**: Event body",
            "**body_url**: Link to log file.",
            "**bytes_synced**: For sync events, the total number of bytes synced.",
            "**created_at**: External event create date/time",
            "**errored_files**: For sync events, the number of files that encountered errors.",
            "**event_type**: Type of event being recorded.",
            "**folder_behavior_id**: Folder Behavior ID",
            "**id**: Event ID",
            "**remote_server_type**: Associated Remote Server type, if any",
            "**status**: Status of event.",
            "**successful_files**: For sync events, the number of files handled successfully."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
