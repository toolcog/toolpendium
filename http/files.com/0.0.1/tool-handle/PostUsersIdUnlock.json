{
  "name": "PostUsersIdUnlock",
  "description": "Unlock user who has been locked out due to failed logins.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int32",
        "type": "integer",
        "description": "User ID."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//app.files.com/api/rest/v1/users/{id}/unlock"
    }
  },
  "responses": {}
}
