{
  "name": "GetAutomationRunsId",
  "description": "Show Automation Run",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int32",
        "type": "integer",
        "description": "Automation Run ID."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//app.files.com/api/rest/v1/automation_runs/{id}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "List Automation Runs",
        "**Key properties:**",
        {
          "$ul": [
            "**automation_id**: ID of the associated Automation.",
            "**completed_at**: Automation run completion/failure date/time.",
            "**created_at**: Automation run start date/time.",
            "**id**: ID.",
            "**status**: The success status of the AutomationRun. One of `running`, `success`, `partial_failure`, or `failure`.",
            "**status_messages_url**: Link to status messages log file."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
