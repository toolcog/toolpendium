{
  "name": "PostUsersId2faReset",
  "description": "Trigger 2FA Reset process for user who has lost access to their existing 2FA methods.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int32",
        "type": "integer",
        "description": "User ID."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//app.files.com/api/rest/v1/users/{id}/2fa/reset"
    }
  },
  "responses": {}
}
