{
  "name": "PostSsoStrategiesIdSync",
  "description": "Synchronize provisioning data with the SSO remote server.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int32",
        "type": "integer",
        "description": "Sso Strategy ID."
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "//app.files.com/api/rest/v1/sso_strategies/{id}/sync"
    }
  },
  "responses": {}
}
