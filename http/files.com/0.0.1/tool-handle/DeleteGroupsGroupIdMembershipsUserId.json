{
  "name": "DeleteGroupsGroupIdMembershipsUserId",
  "description": "Delete Group User",
  "parameters": {
    "type": "object",
    "properties": {
      "group_id": {
        "format": "int32",
        "type": "integer",
        "description": "Group ID from which to remove user."
      },
      "user_id": {
        "format": "int32",
        "type": "integer",
        "description": "User ID to remove from group."
      }
    },
    "required": [
      "group_id",
      "user_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "//app.files.com/api/rest/v1/groups/{group_id}/memberships/{user_id}"
    }
  },
  "responses": {}
}
