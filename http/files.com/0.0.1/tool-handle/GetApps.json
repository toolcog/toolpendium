{
  "name": "GetApps",
  "description": "List Apps",
  "parameters": {
    "type": "object",
    "properties": {
      "cursor": {
        "type": "string",
        "description": "Used for pagination.  When a list request has more records available, cursors are provided in the response headers `X-Files-Cursor-Next` and `X-Files-Cursor-Prev`.  Send one of those cursor value here to resume an existing list from the next available record.  Note: many of our SDKs have iterator methods that will automatically handle cursor-based pagination."
      },
      "per_page": {
        "format": "int32",
        "type": "integer",
        "description": "Number of records to show per page.  (Max: 10,000, 1,000 or less is recommended)."
      },
      "sort_by": {
        "type": "object",
        "description": "If set, sort records by the specified field in either `asc` or `desc` direction (e.g. `sort_by[name]=desc`). Valid fields are `name` and `app_type`."
      },
      "filter": {
        "type": "object",
        "description": "If set, return records where the specified field is equal to the supplied value. Valid fields are `name` and `app_type`. Valid field combinations are `[ name, app_type ]` and `[ app_type, name ]`."
      },
      "filter_prefix": {
        "type": "object",
        "description": "If set, return records where the specified field is prefixed by the supplied value. Valid fields are `name`."
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "//app.files.com/api/rest/v1/apps{?cursor,per_page,sort_by,filter,filter_prefix}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "List"
        },
        "List Apps",
        "**Key properties:**",
        {
          "$ul": [
            "**app_type**: The type of the App",
            "**documentation_links**: Collection of named links to documentation",
            "**extended_description**: Long form description of the App",
            "**external_homepage_url**: Link to external homepage",
            "**featured**: Is featured on the App listing?",
            "**folder_behavior_type**: Associated Folder Behavior type, if any",
            "**icon_url**: App icon",
            "**logo_thumbnail_url**: Logo thumbnail for the App",
            "**logo_url**: Full size logo for the App",
            "**marketing_youtube_url**: Marketing video page",
            "**name**: Name of the App",
            "**remote_server_type**: Associated Remote Server type, if any",
            "**screenshot_list_urls**: Screenshots of the App",
            "**short_description**: Short description of the App",
            "**sso_strategy_type**: Associated SSO Strategy type, if any",
            "**tutorial_youtube_url**: Tutorial video page"
          ]
        },
        {
          "$each": "$.body.*",
          "$as": "item",
          "$block": [
            {
              "$h2": {
                "$": "$.item.name"
              }
            },
            {
              "$lang": "json",
              "$code": {
                "$encode": "json",
                "$indent": true,
                "$content": {
                  "$": "$.item"
                }
              }
            }
          ]
        }
      ]
    }
  }
}
