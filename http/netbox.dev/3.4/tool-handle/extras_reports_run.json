{
  "name": "extras_reports_run",
  "description": "Run a Report identified as \"<module>.<script>\" and return the pending JobResult as the result",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://demo.netbox.dev/api/extras/reports/{id}/run/"
    }
  },
  "responses": {}
}
