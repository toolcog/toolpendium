{
  "name": "ClickStream_Get",
  "description": "Retrieve the latest list of events of this account. Limited to last 100.",
  "parameters": {
    "type": "object",
    "properties": {
      "group": {
        "format": "int64",
        "type": "integer",
        "description": "Filter by this group id (mutually exclusive with \"datapoint\" and \"conversion\")"
      },
      "datapoint": {
        "format": "int64",
        "type": "integer",
        "description": "Filter by this datapoint id (mutually exclusive with \"group\" and \"conversion\")"
      },
      "conversion": {
        "format": "int64",
        "type": "integer",
        "description": "Filter by this conversion id (mutually exclusive with \"datapoint\" and \"group\")"
      },
      "pageSize": {
        "default": 50,
        "format": "int32",
        "type": "integer",
        "description": "Limit results to this number"
      },
      "filter": {
        "enum": [
          "",
          "spiders",
          "uniques",
          "nonuniques",
          "conversions"
        ],
        "type": "string",
        "description": "Filter event type (\"spiders\"/\"uniques\"/\"nonuniques\"/\"conversions\")"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://apiv2.clickmeter.com:80/clickstream{?group,datapoint,conversion,pageSize,filter}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**entities**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
