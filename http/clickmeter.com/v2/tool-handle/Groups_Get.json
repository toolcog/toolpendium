{
  "name": "Groups_Get",
  "description": "List of all the groups associated to the user.",
  "parameters": {
    "type": "object",
    "properties": {
      "offset": {
        "default": 0,
        "format": "int32",
        "minLength": 0,
        "type": "integer",
        "description": "Where to start when retrieving elements. Default is 0 if not specified."
      },
      "limit": {
        "default": 20,
        "format": "int32",
        "maxLength": 0,
        "minLength": 0,
        "type": "integer",
        "description": "Maximum elements to retrieve. Default to 20 if not specified."
      },
      "status": {
        "enum": [
          "deleted",
          "active"
        ],
        "type": "string",
        "description": "Status of the group"
      },
      "tags": {
        "type": "string",
        "description": "A comma separated list of tags you want to filter with."
      },
      "textSearch": {
        "type": "string",
        "description": "Filter fields by this pattern"
      },
      "createdAfter": {
        "type": "string",
        "description": "Exclude groups created before this date (YYYYMMDD)"
      },
      "createdBefore": {
        "type": "string",
        "description": "Exclude groups created after this date (YYYYMMDD)"
      },
      "write": {
        "type": "boolean",
        "description": "Write permission"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://apiv2.clickmeter.com:80/groups{?offset,limit,status,tags,textSearch,createdAfter,createdBefore,write}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**entities**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
