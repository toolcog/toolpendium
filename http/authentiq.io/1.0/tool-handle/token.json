{
  "name": "token",
  "description": "Exchange en authorization code for an ID Token or Access Token.\n\nThis endpoint supports both `client_secret_basic` (default) and `client_secret_basic` authentication methods, as specified by the client's `token_endpoint_auth_method`.\n",
  "parameters": {
    "type": "object",
    "properties": {
      "Authorization": {
        "type": "string",
        "description": "HTTP Basic authorization header.\n"
      },
      "body": {
        "properties": {
          "client_id": {
            "description": "The registered client ID.\n",
            "type": "string"
          },
          "client_secret": {
            "description": "The registered client ID secret.\n",
            "format": "password",
            "type": "string"
          },
          "code": {
            "description": "The authorization code previously obtained from the Authentication endpoint.\n",
            "type": "string"
          },
          "grant_type": {
            "description": "The authorization grant type, must be `authorization_code`.\n",
            "type": "string"
          },
          "redirect_uri": {
            "description": "The redirect URL that was used previously with the Authentication endpoint.\n",
            "type": "string"
          }
        },
        "required": [
          "client_id",
          "client_secret",
          "grant_type",
          "code",
          "redirect_uri"
        ],
        "type": "object"
      }
    },
    "required": [
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://connect.authentiq.io/token"
    },
    "headers": {
      "Authorization": {
        "$": "Authorization"
      }
    },
    "body": {
      "$": "body",
      "encode": "urlencoded"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Successful token response\n",
        "**Key properties:**",
        {
          "$ul": [
            "**access_token**: The access token issued by the authorization server.",
            "**expires_at**: The time the access token will expire in seconds since epoch.",
            "**expires_in**: The lifetime in seconds of the access token.",
            "**id_token**: ID Token value associated with the authenticated session.",
            "**refresh_token**: The refresh token issued to the client, if any.",
            "**scope**: The scope of the granted tokens.",
            "**token_type**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "400": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Error Response defined as in Section 5.2 of OAuth 2.0 [RFC6749].\n",
        "**Key properties:**",
        {
          "$ul": [
            "**error**",
            "**error_description**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    },
    "401": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "Error Response defined as in Section 5.2 of OAuth 2.0 [RFC6749].\n",
        "**Key properties:**",
        {
          "$ul": [
            "**error**",
            "**error_description**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
