{
  "name": "add_video_to_watch_later_alt1",
  "description": "Add a video to a user's Watch Later queue",
  "parameters": {
    "type": "object",
    "properties": {
      "video_id": {
        "example": 258684937,
        "type": "number",
        "description": "The ID of the video."
      }
    },
    "required": [
      "video_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://api.vimeo.com/me/watchlater/{video_id}"
    }
  },
  "responses": {}
}
