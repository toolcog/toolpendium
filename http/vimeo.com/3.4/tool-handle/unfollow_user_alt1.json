{
  "name": "unfollow_user_alt1",
  "description": "Unfollow a user",
  "parameters": {
    "type": "object",
    "properties": {
      "follow_user_id": {
        "example": 3766357,
        "type": "number",
        "description": "The ID of the following user."
      }
    },
    "required": [
      "follow_user_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.vimeo.com/me/following/{follow_user_id}"
    }
  },
  "responses": {}
}
