{
  "name": "delete_picture",
  "description": "Delete a user picture",
  "parameters": {
    "type": "object",
    "properties": {
      "portraitset_id": {
        "example": 12345,
        "type": "number",
        "description": "The ID of the picture."
      },
      "user_id": {
        "example": 152184,
        "type": "number",
        "description": "The ID of the user."
      }
    },
    "required": [
      "portraitset_id",
      "user_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.vimeo.com/users/{user_id}/pictures/{portraitset_id}"
    }
  },
  "responses": {}
}
