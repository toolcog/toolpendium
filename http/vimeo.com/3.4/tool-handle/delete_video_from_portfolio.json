{
  "name": "delete_video_from_portfolio",
  "description": "Remove a video from a portfolio",
  "parameters": {
    "type": "object",
    "properties": {
      "portfolio_id": {
        "example": 12345,
        "type": "number",
        "description": "The ID of the portfolio."
      },
      "user_id": {
        "example": 152184,
        "type": "number",
        "description": "The ID of the user."
      },
      "video_id": {
        "example": 258684937,
        "type": "number",
        "description": "The ID of the video."
      }
    },
    "required": [
      "portfolio_id",
      "user_id",
      "video_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.vimeo.com/users/{user_id}/portfolios/{portfolio_id}/videos/{video_id}"
    }
  },
  "responses": {
    "404": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Legacy Error"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**error**: User-friendly error message"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
