{
  "name": "delete_video_from_watch_later_alt1",
  "description": "Remove a video from a user's Watch Later queue",
  "parameters": {
    "type": "object",
    "properties": {
      "video_id": {
        "example": 258684937,
        "type": "number",
        "description": "The ID of the video."
      }
    },
    "required": [
      "video_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.vimeo.com/me/watchlater/{video_id}"
    }
  },
  "responses": {}
}
