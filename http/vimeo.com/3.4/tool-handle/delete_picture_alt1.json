{
  "name": "delete_picture_alt1",
  "description": "Delete a user picture",
  "parameters": {
    "type": "object",
    "properties": {
      "portraitset_id": {
        "example": 12345,
        "type": "number",
        "description": "The ID of the picture."
      }
    },
    "required": [
      "portraitset_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.vimeo.com/me/pictures/{portraitset_id}"
    }
  },
  "responses": {}
}
