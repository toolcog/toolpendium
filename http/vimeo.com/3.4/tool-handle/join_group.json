{
  "name": "join_group",
  "description": "Add a user to a group",
  "parameters": {
    "type": "object",
    "properties": {
      "group_id": {
        "example": 1108,
        "type": "number",
        "description": "The ID of the group."
      },
      "user_id": {
        "example": 152184,
        "type": "number",
        "description": "The ID of the user."
      }
    },
    "required": [
      "group_id",
      "user_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://api.vimeo.com/users/{user_id}/groups/{group_id}"
    }
  },
  "responses": {
    "403": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Legacy Error"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**error**: User-friendly error message"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
