{
  "name": "check_if_user_subscribed_to_category_alt1",
  "description": "Check if a user follows a category",
  "parameters": {
    "type": "object",
    "properties": {
      "category": {
        "example": "animation",
        "type": "string",
        "description": "The name of the category."
      }
    },
    "required": [
      "category"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://api.vimeo.com/me/categories/{category}"
    }
  },
  "responses": {}
}
