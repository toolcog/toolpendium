{
  "name": "getStatusCategory",
  "description": "Returns a status category. Status categories provided a mechanism for categorizing [statuses](#api-rest-api-3-status-idOrName-get).\n\n**[Permissions](#permissions) required:** Permission to access Jira.",
  "parameters": {
    "type": "object",
    "properties": {
      "idOrKey": {
        "type": "string",
        "description": "The ID or key of the status category."
      }
    },
    "required": [
      "idOrKey"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://your-domain.atlassian.net/rest/api/3/statuscategory/{idOrKey}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "A status category.",
        "**Key properties:**",
        {
          "$ul": [
            "**colorName**: The name of the color used to represent the status category.",
            "**id**: The ID of the status category.",
            "**key**: The key of the status category.",
            "**name**: The name of the status category.",
            "**self**: The URL of the status category."
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
