{
  "name": "deleteSharePermission",
  "description": "Deletes a share permission from a filter.\n\n**[Permissions](#permissions) required:** Permission to access Jira and the user must own the filter.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "format": "int64",
        "type": "integer",
        "description": "The ID of the filter."
      },
      "permissionId": {
        "format": "int64",
        "type": "integer",
        "description": "The ID of the share permission."
      }
    },
    "required": [
      "id",
      "permissionId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://your-domain.atlassian.net/rest/api/3/filter/{id}/permission/{permissionId}"
    }
  },
  "responses": {}
}
