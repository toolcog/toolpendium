{
  "name": "QuickTestImageUrl",
  "description": "Quick test an image url",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "format": "uuid",
        "type": "string",
        "description": "The project to evaluate against"
      },
      "iterationId": {
        "format": "uuid",
        "type": "string",
        "description": "Optional. Specifies the id of a particular iteration to evaluate against.\r\n            The default iteration for the project will be used when not specified."
      },
      "Training-Key": {
        "type": "string"
      },
      "body": {
        "$ref": "#/components/schemas/ImageUrl",
        "description": "An {Iris.Web.Api.Models.ImageUrl} that contains the url of the image to be evaluated"
      }
    },
    "required": [
      "projectId",
      "Training-Key",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://southcentralus.api.cognitive.microsoft.com/customvision/v2.1/Training/projects/{projectId}/quicktest/url{?iterationId}"
    },
    "headers": {
      "Training-Key": {
        "$": "Training-Key"
      }
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**created**",
            "**id**",
            "**iteration**",
            "**predictions**",
            "**project**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
