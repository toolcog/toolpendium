{
  "name": "transportation_incentives_laws_categories",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "output_format": {
        "default": "json",
        "enum": [
          "json",
          "xml"
        ],
        "type": "string",
        "description": "Response format"
      },
      "api_key": {
        "default": "DEMO_KEY",
        "type": "string",
        "description": "API Key"
      },
      "type": {
        "enum": [
          "tech",
          "user",
          "regulation",
          "incentive"
        ],
        "type": "string",
        "description": "Search by the category type."
      }
    },
    "required": [
      "output_format",
      "api_key"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "/api/transportation-incentives-laws/v1/category-list.{output_format}{?api_key,type}"
    }
  },
  "responses": {
    "200": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**inputs**: The parameters that were passed to the API",
            [
              "**metadata**: The identifying metadata.",
              {
                "$ul": [
                  "**count**: If the request returns an array of results, this is the number of results returned in the array",
                  "**version**: This is the version of the api"
                ]
              }
            ],
            "**result**: The array of law categories returned"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
