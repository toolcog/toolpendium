{
  "name": "payments_creditors_destroy",
  "description": "API endpoints related to creditor accounts.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "description": "Unique entry ID",
        "format": "uuid",
        "type": "string"
      }
    },
    "required": [
      "id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://ob.nordigen.com/api/v2/payments/creditors/{id}/"
    }
  },
  "responses": {}
}
