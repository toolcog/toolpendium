{
  "name": "assignVendor",
  "description": "Assigns vendor to a job in a project.",
  "parameters": {
    "type": "object",
    "properties": {
      "jobId": {
        "type": "string",
        "description": "job's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/AssignVendorDTO",
        "description": "Assigned vendor to a job in a project."
      }
    },
    "required": [
      "jobId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/jobs/{jobId}/vendor"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
