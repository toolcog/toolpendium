{
  "name": "delete",
  "description": "Removes a view. No content is returned upon success (204).",
  "parameters": {
    "type": "object",
    "properties": {
      "viewId": {
        "format": "int64",
        "type": "integer",
        "description": "view's internal identifier"
      }
    },
    "required": [
      "viewId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/browser/views/{viewId}"
    }
  },
  "responses": {}
}
