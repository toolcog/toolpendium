{
  "name": "updateCustomField_2",
  "description": "Updates a custom field with a specified key in a project",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "project's internal identifier"
      },
      "key": {
        "type": "string",
        "description": "custom field's key"
      },
      "body": {
        "$ref": "#/components/schemas/SmartCustomFieldDTO",
        "description": "Updated custom field with a specified key in a project."
      }
    },
    "required": [
      "projectId",
      "key",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/projects/{projectId}/customFields/{key}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
