{
  "name": "updateOrderedOn",
  "description": "Updates Order Date for a project.",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "project's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/TimeDTO",
        "description": "Updated Order Date for a project."
      }
    },
    "required": [
      "projectId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/projects/{projectId}/orderDate"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
