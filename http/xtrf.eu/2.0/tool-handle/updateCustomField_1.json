{
  "name": "updateCustomField_1",
  "description": "Updates given custom field of a given user.",
  "parameters": {
    "type": "object",
    "properties": {
      "userId": {
        "format": "int64",
        "type": "integer",
        "description": "user's internal identifier"
      },
      "customFieldKey": {
        "type": "string",
        "description": "custom field's key"
      },
      "body": {
        "$ref": "#/components/schemas/CustomFieldDTO",
        "description": "Updated custom fields of a given user."
      }
    },
    "required": [
      "userId",
      "customFieldKey",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/users/{userId}/customFields/{customFieldKey}"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {
    "default": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "**Key properties:**",
        {
          "$ul": [
            "**key**",
            "**name**",
            "**type**",
            "**value**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
