{
  "name": "delete_4",
  "description": "Removes a customer price list.",
  "parameters": {
    "type": "object",
    "properties": {
      "priceListId": {
        "format": "int64",
        "type": "integer",
        "description": "customer price list's internal identifier"
      }
    },
    "required": [
      "priceListId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/customers/priceLists/{priceListId}"
    }
  },
  "responses": {}
}
