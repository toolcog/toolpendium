{
  "name": "addExternalFileLinks",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "project's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/ExternalFileDto",
        "description": "Added file links to the project as added by PM."
      }
    },
    "required": [
      "projectId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/projects/{projectId}/files/addExternalLink"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
