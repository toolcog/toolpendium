{
  "name": "sendReminder",
  "description": "Sends reminder.",
  "parameters": {
    "type": "object",
    "properties": {
      "invoiceId": {
        "format": "int64",
        "type": "integer",
        "description": "client invoice's internal identifier"
      }
    },
    "required": [
      "invoiceId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/accounting/customers/invoices/{invoiceId}/sendReminder"
    }
  },
  "responses": {}
}
