{
  "name": "updateClientReferenceNumber",
  "description": "Updates Client Reference Number for a project.",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "project's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/StringDTO",
        "description": "Updated Client Reference Number for a project."
      }
    },
    "required": [
      "projectId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/projects/{projectId}/clientReferenceNumber"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
