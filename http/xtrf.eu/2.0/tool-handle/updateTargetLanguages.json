{
  "name": "updateTargetLanguages",
  "description": "Updates target languages for a project.",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "project's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/TargetLanguagesDTO",
        "description": "Updated target languages for a project."
      }
    },
    "required": [
      "projectId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/projects/{projectId}/targetLanguages"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
