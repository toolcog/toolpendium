{
  "name": "setPreferred",
  "description": "Marks report as preferred or not.",
  "parameters": {
    "type": "object",
    "properties": {
      "reportId": {
        "format": "int64",
        "type": "integer",
        "description": "report's internal identifier"
      }
    },
    "required": [
      "reportId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/reports/{reportId}/preferred"
    }
  },
  "responses": {}
}
