{
  "name": "delete_10",
  "description": "Removes a provider.",
  "parameters": {
    "type": "object",
    "properties": {
      "providerId": {
        "format": "int64",
        "type": "integer",
        "description": "provider's internal identifier"
      }
    },
    "required": [
      "providerId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/providers/{providerId}"
    }
  },
  "responses": {}
}
