{
  "name": "deleteReceivable_1",
  "description": "Deletes a receivable.",
  "parameters": {
    "type": "object",
    "properties": {
      "quoteId": {
        "type": "string",
        "description": "quoteId's internal identifier"
      },
      "receivableId": {
        "format": "int64",
        "type": "integer",
        "description": "receivable's internal identifier"
      }
    },
    "required": [
      "quoteId",
      "receivableId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/quotes/{quoteId}/finance/receivables/{receivableId}"
    }
  },
  "responses": {}
}
