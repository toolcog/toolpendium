{
  "name": "updateVolume_1",
  "description": "Updates volume for a quote.",
  "parameters": {
    "type": "object",
    "properties": {
      "quoteId": {
        "type": "string",
        "description": "quote's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/BigDecimalDTO",
        "description": "Updated volume for a quote."
      }
    },
    "required": [
      "quoteId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/quotes/{quoteId}/volume"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
