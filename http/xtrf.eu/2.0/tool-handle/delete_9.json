{
  "name": "delete_9",
  "description": "Removes a provider price list.",
  "parameters": {
    "type": "object",
    "properties": {
      "priceListId": {
        "format": "int64",
        "type": "integer",
        "description": "provider price list's internal identifier"
      }
    },
    "required": [
      "priceListId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/providers/priceLists/{priceListId}"
    }
  },
  "responses": {}
}
