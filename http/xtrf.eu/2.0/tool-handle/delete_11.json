{
  "name": "delete_11",
  "description": "Removes a report.",
  "parameters": {
    "type": "object",
    "properties": {
      "reportId": {
        "format": "int64",
        "type": "integer",
        "description": "report's internal identifier"
      }
    },
    "required": [
      "reportId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/reports/{reportId}"
    }
  },
  "responses": {}
}
