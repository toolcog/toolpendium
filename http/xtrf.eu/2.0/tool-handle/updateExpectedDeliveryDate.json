{
  "name": "updateExpectedDeliveryDate",
  "description": "Updates Expected Delivery Date for a quote.",
  "parameters": {
    "type": "object",
    "properties": {
      "quoteId": {
        "type": "string",
        "description": "quote's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/TimeDTO",
        "description": "Updated Expected Delivery Date for a quote."
      }
    },
    "required": [
      "quoteId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/quotes/{quoteId}/expectedDeliveryDate"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
