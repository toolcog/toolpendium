{
  "name": "deletePayable_1",
  "description": "Deletes a payable.",
  "parameters": {
    "type": "object",
    "properties": {
      "quoteId": {
        "type": "string",
        "description": "quoteId's internal identifier"
      },
      "payableId": {
        "format": "int64",
        "type": "integer",
        "description": "payable's internal identifier"
      }
    },
    "required": [
      "quoteId",
      "payableId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/quotes/{quoteId}/finance/payables/{payableId}"
    }
  },
  "responses": {}
}
