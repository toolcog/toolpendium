{
  "name": "setStatus",
  "description": "Changes invoice status to given status.",
  "parameters": {
    "type": "object",
    "properties": {
      "invoiceId": {
        "format": "int64",
        "type": "integer",
        "description": "provider invoice's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/StatusRequestDTO",
        "description": "Changed invoice status to given status."
      }
    },
    "required": [
      "invoiceId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/accounting/providers/invoices/{invoiceId}/status"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
