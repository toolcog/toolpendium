{
  "name": "deleteReceivable_2",
  "description": "Deletes a receivable.",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "project's internal identifier"
      },
      "receivableId": {
        "format": "int64",
        "type": "integer",
        "description": "receivable's internal identifier"
      }
    },
    "required": [
      "projectId",
      "receivableId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/projects/{projectId}/finance/receivables/{receivableId}"
    }
  },
  "responses": {}
}
