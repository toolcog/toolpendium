{
  "name": "delete_2",
  "description": "Removes a customer payment.",
  "parameters": {
    "type": "object",
    "properties": {
      "paymentId": {
        "format": "int64",
        "type": "integer",
        "description": "customer payment's internal identifier"
      }
    },
    "required": [
      "paymentId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/accounting/customers/payments/{paymentId}"
    }
  },
  "responses": {}
}
