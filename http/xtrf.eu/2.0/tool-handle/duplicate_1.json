{
  "name": "duplicate_1",
  "description": "Duplicates a report.",
  "parameters": {
    "type": "object",
    "properties": {
      "reportId": {
        "format": "int64",
        "type": "integer",
        "description": "report's internal identifier"
      }
    },
    "required": [
      "reportId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/reports/{reportId}/duplicate"
    }
  },
  "responses": {}
}
