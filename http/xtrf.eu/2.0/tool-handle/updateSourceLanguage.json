{
  "name": "updateSourceLanguage",
  "description": "Updates source language for a project.",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "project's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/SourceLanguageDTO",
        "description": "Updated source language for a project."
      }
    },
    "required": [
      "projectId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/projects/{projectId}/sourceLanguage"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
