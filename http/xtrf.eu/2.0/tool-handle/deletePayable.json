{
  "name": "deletePayable",
  "description": "Deletes a payable.",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "project's internal identifier"
      },
      "payableId": {
        "format": "int64",
        "type": "integer",
        "description": "payable's internal identifier"
      }
    },
    "required": [
      "projectId",
      "payableId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/projects/{projectId}/finance/payables/{payableId}"
    }
  },
  "responses": {}
}
