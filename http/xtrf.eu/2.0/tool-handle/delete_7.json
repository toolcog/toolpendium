{
  "name": "delete_7",
  "description": "Removes a provider payment.",
  "parameters": {
    "type": "object",
    "properties": {
      "paymentId": {
        "format": "int64",
        "type": "integer",
        "description": "provider payment's internal identifier"
      }
    },
    "required": [
      "paymentId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/accounting/providers/payments/{paymentId}"
    }
  },
  "responses": {}
}
