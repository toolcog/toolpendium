{
  "name": "updateQuoteExpiry",
  "description": "Updates Quote Expiry Date for a quote.",
  "parameters": {
    "type": "object",
    "properties": {
      "quoteId": {
        "type": "string",
        "description": "quote's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/TimeDTO",
        "description": "Updated Quote Expiry Date for a quote."
      }
    },
    "required": [
      "quoteId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/quotes/{quoteId}/quoteExpiry"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
