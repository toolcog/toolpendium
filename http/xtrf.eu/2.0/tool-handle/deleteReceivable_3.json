{
  "name": "deleteReceivable_3",
  "description": "Deletes a receivable.",
  "parameters": {
    "type": "object",
    "properties": {
      "quoteId": {
        "type": "string",
        "description": "quote's internal identifier"
      },
      "receivableId": {
        "format": "int64",
        "type": "integer",
        "description": "receivable's internal identifier"
      }
    },
    "required": [
      "quoteId",
      "receivableId"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/quotes/{quoteId}/finance/receivables/{receivableId}"
    }
  },
  "responses": {}
}
