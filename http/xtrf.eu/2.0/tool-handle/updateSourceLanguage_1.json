{
  "name": "updateSourceLanguage_1",
  "description": "Updates source language for a quote.",
  "parameters": {
    "type": "object",
    "properties": {
      "quoteId": {
        "type": "string",
        "description": "quote's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/SourceLanguageDTO",
        "description": "Updated source language for a quote."
      }
    },
    "required": [
      "quoteId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/quotes/{quoteId}/sourceLanguage"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
