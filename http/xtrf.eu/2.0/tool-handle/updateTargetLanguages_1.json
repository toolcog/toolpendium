{
  "name": "updateTargetLanguages_1",
  "description": "Updates target languages for a quote.",
  "parameters": {
    "type": "object",
    "properties": {
      "quoteId": {
        "type": "string",
        "description": "quote's internal identifier"
      },
      "body": {
        "$ref": "#/components/schemas/TargetLanguagesDTO",
        "description": "Updated target languages for a quote."
      }
    },
    "required": [
      "quoteId",
      "body"
    ]
  },
  "handler": "http",
  "request": {
    "method": "PUT",
    "url": {
      "$uri": "https://presentation.s.xtrf.eu/home-api/v2/quotes/{quoteId}/targetLanguages"
    },
    "body": {
      "$": "body",
      "encode": "json"
    }
  },
  "responses": {}
}
