{
  "name": "GET_DeleteDomain",
  "description": "<p> The <code>DeleteDomain</code> operation deletes a domain. Any items (and their attributes) in the domain are deleted as well. The <code>DeleteDomain</code> operation might take 10 or more seconds to complete. </p> <note> Running <code>DeleteDomain</code> on a domain that does not exist or running the function multiple times using the same domain name will not result in an error response. </note>",
  "parameters": {
    "type": "object",
    "properties": {
      "DomainName": {
        "type": "string",
        "description": "The name of the domain to delete."
      },
      "Action": {
        "type": "string",
        "enum": [
          "DeleteDomain"
        ]
      },
      "Version": {
        "type": "string",
        "enum": [
          "2009-04-15"
        ]
      },
      "AWSAccessKeyId": {
        "type": "string"
      },
      "SignatureMethod": {
        "type": "string"
      },
      "SignatureVersion": {
        "type": "string"
      },
      "Timestamp": {
        "type": "string"
      },
      "Signature": {
        "type": "string"
      }
    },
    "required": [
      "DomainName",
      "Action",
      "Version",
      "AWSAccessKeyId",
      "SignatureMethod",
      "SignatureVersion",
      "Timestamp",
      "Signature"
    ]
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "http://sdb.amazonaws.com/#Action=DeleteDomain{?DomainName,Action,Version,AWSAccessKeyId,SignatureMethod,SignatureVersion,Timestamp,Signature}"
    }
  },
  "responses": {
    "400": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Object"
        },
        "The request must contain the specified missing parameter.",
        "**Key properties:**",
        {
          "$ul": [
            "**BoxUsage**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
