{
  "name": "getCustomPages",
  "description": "Returns a list of custom pages associated with the project API key",
  "parameters": {
    "type": "object",
    "properties": {
      "perPage": {
        "default": 10,
        "maximum": 100,
        "minimum": 1,
        "type": "integer",
        "description": "Number of items to include in pagination (up to 100, defaults to 10)"
      },
      "page": {
        "default": 1,
        "minimum": 1,
        "type": "integer",
        "description": "Used to specify further pages (starts at 1)"
      }
    }
  },
  "handler": "http",
  "request": {
    "method": "GET",
    "url": {
      "$uri": "https://dash.readme.io/api/v1/custompages{?perPage,page}"
    }
  },
  "responses": {}
}
