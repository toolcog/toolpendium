{
  "name": "deleteCustomPage",
  "description": "Delete the custom page with this slug",
  "parameters": {
    "type": "object",
    "properties": {
      "slug": {
        "type": "string",
        "description": "Slug of custom page"
      }
    },
    "required": [
      "slug"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://dash.readme.io/api/v1/custompages/{slug}"
    }
  },
  "responses": {}
}
