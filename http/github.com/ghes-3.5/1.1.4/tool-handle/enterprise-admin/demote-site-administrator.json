{
  "name": "enterprise-admin_demote-site-administrator",
  "description": "You can demote any user account except your own.",
  "parameters": {
    "type": "object",
    "properties": {
      "username": {
        "type": "string",
        "description": "The handle for the GitHub user account."
      }
    },
    "required": [
      "username"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://github.com//users/{username}/site_admin"
    }
  },
  "responses": {}
}
