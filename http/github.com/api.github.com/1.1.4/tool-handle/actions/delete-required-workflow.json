{
  "name": "actions_delete-required-workflow",
  "description": "Deletes a required workflow configured in an organization.\n\nYou must authenticate using an access token with the `admin:org` scope to use this endpoint.\n\nFor more information, see \"[Required Workflows](https://docs.github.com/actions/using-workflows/required-workflows).\"",
  "parameters": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "description": "The organization name. The name is not case sensitive."
      },
      "required_workflow_id": {
        "type": "integer",
        "description": "The unique identifier of the required workflow."
      }
    },
    "required": [
      "org",
      "required_workflow_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://api.github.com/orgs/{org}/actions/required_workflows/{required_workflow_id}"
    }
  },
  "responses": {}
}
