{
  "name": "orgs_remove-public-membership-for-authenticated-user",
  "description": "",
  "parameters": {
    "type": "object",
    "properties": {
      "org": {
        "type": "string",
        "description": "The organization name. The name is not case sensitive."
      },
      "username": {
        "type": "string",
        "description": "The handle for the GitHub user account."
      }
    },
    "required": [
      "org",
      "username"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "https://github.com//orgs/{org}/public_members/{username}"
    }
  },
  "responses": {}
}
