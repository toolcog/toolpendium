{
  "name": "secret-scanning_post-security-product-enablement-for-enterprise",
  "description": "Enables or disables the specified security feature for all repositories in an enterprise.\n\nTo use this endpoint, you must be an administrator of the enterprise, and you must use an access token with the `admin:enterprise` scope.",
  "parameters": {
    "type": "object",
    "properties": {
      "enterprise": {
        "type": "string",
        "description": "The slug version of the enterprise name. You can also substitute this value with the enterprise id."
      },
      "security_product": {
        "enum": [
          "advanced_security",
          "secret_scanning",
          "secret_scanning_push_protection"
        ],
        "type": "string",
        "description": "The security feature to enable or disable."
      },
      "enablement": {
        "enum": [
          "enable_all",
          "disable_all"
        ],
        "type": "string",
        "description": "The action to take.\n\n`enable_all` means to enable the specified security feature for all repositories in the enterprise.\n`disable_all` means to disable the specified security feature for all repositories in the enterprise."
      }
    },
    "required": [
      "enterprise",
      "security_product",
      "enablement"
    ]
  },
  "handler": "http",
  "request": {
    "method": "POST",
    "url": {
      "$uri": "https://github.com//enterprises/{enterprise}/{security_product}/{enablement}"
    }
  },
  "responses": {
    "404": {
      "$encode": "markdown",
      "$block": [
        {
          "$h1": "Basic Error"
        },
        "Basic Error",
        "**Key properties:**",
        {
          "$ul": [
            "**documentation_url**",
            "**message**",
            "**status**",
            "**url**"
          ]
        },
        {
          "$lang": "json",
          "$code": {
            "$encode": "json",
            "$indent": true,
            "$content": {
              "$": "$.body"
            }
          }
        }
      ]
    }
  }
}
