{
  "name": "repos_delete-deploy-key",
  "description": "Deploy keys are immutable. If you need to update a key, remove the key and create a new one instead.",
  "parameters": {
    "type": "object",
    "properties": {
      "owner": {
        "type": "string"
      },
      "repo": {
        "type": "string"
      },
      "key_id": {
        "type": "integer",
        "description": "key_id parameter"
      }
    },
    "required": [
      "owner",
      "repo",
      "key_id"
    ]
  },
  "handler": "http",
  "request": {
    "method": "DELETE",
    "url": {
      "$uri": "http://HOSTNAME/api/v3/repos/{owner}/{repo}/keys/{key_id}"
    }
  },
  "responses": {}
}
